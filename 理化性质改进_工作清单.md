# 产品理化性质改进 · 工作清单

> 2026-09-11 · 合并 A1–A11 / C1–C3 / D1–D3 三组待确认项
> 状态：**已交付**（P1–P5 全按推荐执行；专项回归 81/81，全量 30/30 全绿）
> 范围口径：**指标库与 SDS 不接线**，联动项归零（见第九节口径修正记录）
>
> ⚠️ **分片命名修正**：原计划的 `29-*` 已改为 `27z1/27z2/27z3`。
> 原因：`28-js-boot.js` 在**加载时**就读取 `location.hash` 定位页面，
> 编号大于 28 的分片注册晚于 boot，会导致「在详情页刷新后掉回首页」。
> `27z*` 排在 `27z-js-doc-folder.js` 之后、`28-js-boot.js` 之前，与既有 `27z` 同族，无此问题。

---

## 零、讨论已定的结论（不再讨论）

| 议题 | 结论 |
|---|---|
| 类别模板要不要独立页面 | **要**。它是唯一能体现"可配置"的抓手 |
| 落位 | **基础数据 → 理化性质配置**（二级菜单），下挂「指标库」「类别模板」；与 `bd:exptpl 实验模板` 同族。**顶层菜单顺序不变** |
| 指标库定位 | **产品理化性质专用的指标主数据**。消费者收敛为唯一一个：产品详情页理化性质区（口径修正，原"6 处消费者"作废） |
| 类别模板定位 | **决定"测什么"**（指标集 + 标准值）；是产品详情页的数据来源，不是额外消费者 |
| 与既有实验模板的关系 | 本轮**不接线**。实验模板保持原样（连 `physCat` 占位字段都不加） |
| 与 SDS 第 9 章的关系 | **两套独立数据，互不影响**。SDS 的 `PHYS_PROPS` 保持硬编码不动 |

### 字段规模口径
源文件 `产品理化性质表头.xlsx` 为 3 行 × **58 列**，其中 `pH值` 占 3 列（1% / 10% / 100% 水溶液）
→ 按 **P2 选"拆 3 条"**，指标总数为 **58 个**。

| 分组 | 数量 | 字段 |
|---|---|---|
| 感官指标 | 2 | 外观、气味 |
| 常规理化 | 12 | pH 值(1%)、pH 值(10%)、pH 值(100%)、干燥失重%≤、固含量(105℃)、固含量(140℃)、有效物%、粘度(25℃)、粒径nm、粒径μm、溶解性、粒径/PDI |
| 特征成分含量 | 18 | 残留甲醛、戊二醛、活性物、三氧化铝、NCO、有效氯、含氢量、金属氧化物、DMF、丙酮、硫酸钠、双氧水、酸值、环氧值、碘值、磺化值、阻聚剂(MEHQ)、溴值 |
| 应用与成膜性能 | 16 | 膜模量、膜断裂强度、膜伸长率、固化时间、灰卡、色差 ΔE、耐太阳光黄变、成膜、乳化力、耐硬水、过滤、刮板、丝棒刮板、渗透性、配伍性、洗手 |
| 酶活力 | 3 | 脂肪酶活力、中性和碱性蛋白酶活力、酸性蛋白酶活力 |
| 稳定性 | 7 | 离心稳定性、乳液稳定性(10%溶液)、放置稳定性(40/50℃)、常温稳定性(30天)、放置稳定性(3-5℃)、放置稳定性(-5/-15℃)、防腐性能 |
| **合计** | **58** | |

---

## 一、新增（5 项，全部放新分片，不动旧代码）

| # | 交付物 | 分片 | 说明 |
|---|---|---|---|
| N1 | **指标库页面** | `src/27z1-js-phys-lib.js` | 58 指标定义（名称 / 单位 / 上下限 / 分组）；列表 + 新增编辑弹窗（名称 + 单位 + 上下限 + 分组，4 个输入框）。**每条带稳定 ID（`IND-0001` 起）** |
| N2 | **类别模板页面** | `src/27z2-js-phys-tpl.js` | 9 个产品类别（8 个现有 `cat` + 新增「酶制剂类」）× 默认指标集，勾选式编辑，页头显示「已选 N / 共 58」 |
| N3 | **产品详情页** | `src/27z3-js-phys-prod.js` | `prod:detail`：基础信息区 + **理化性质区**（按分组渲染卡片，组内**小三列表格**：指标｜标准值｜实测值；不适用的分组不出现） |
| N4 | **mock 数据** | 同 N1–N3 | 58 指标定义 + 8 类模板 + 2 个产品实例（含来源小标签） |
| N5 | **样式分片** | `src/05-css-phys.css` | 分组卡片 / 小三列表格 / 来源小标签（`NCC 同步 · 日期`｜`导入 · 日期`｜`手动 · 姓名`） |

> ⚠️ **构建规则**：JS 分片只要以 `2` 开头，就会被 `build.sh` 的 `for f in "$SRC"/2*.js` 自动收进去，**不用改脚本**；
> 但 **CSS 分片必须在 `build.sh` 第 25 行之后手动登记**，否则样式整片丢失。

---

## 二、改造（3 项，最小侵入）

| # | 文件 | 改动 | 风险 |
|---|---|---|---|
| M1 | `src/22-js-router.js` | `bd` 下新增 `{id:'bd:phys',name:'理化性质配置',children:[指标库, 类别模板]}` | 低。不动顶层顺序 |
| M2 | `src/27-js-pages-b.js` | `prod:list` 的「查看」按钮与行点击：`detailSoon(o.unit)` → `showPage('prod:detail',{code:r.code})` | 低。仅 2 行 |
| M3 | `src/23z1-js-persist.js` | `wzReset` 链加 `_physReset()`（清 localStorage + 恢复出厂 mock） | 低。与 `_aiReset()` 同模式 |

---

## 三、联动（本轮 **0 项**）

| # | 消费者 | 动作 | 本轮 |
|---|---|---|---|
| L1 | SDS 生成向导 · 第 9 章理化特性 | `23z3-js-sds-sections.js` 的 `PHYS_PROPS` **一个字不改**，继续用硬编码字符串 | ❌ **不做** |
| L2 | 质量检测 / DOE / 原料 / 客户需求 4 处 | 不动 | ❌ 不做 |
| L3 | 实验模板 `NORMAL_EXP_TEMPLATES` | 不动，**不加** `physCat` 占位 | ❌ 不做 |

**唯一代价与对策**：同一指标（外观 / pH / 水溶性 / 运动粘度）在 SDS 第 9 章与产品详情页各有一份值，可能不一致。
→ **对策：mock 时把这 4 项的值对齐**（零代码）。这是内容录入问题，不是功能缺陷。
→ mock 数据的这 4 条指标旁写一行注释标明 `// 与 SDS PHYS_PROPS 第 N 项对齐`，将来真要接线时不用翻代码找对应。

---

## 四、本轮明确不做（划出范围，防止膨胀）

- 检测 / DOE / 原料 / 客户需求 4 处消费者的实际接线
- SDS 第 9 章按产品类别裁剪（酶制剂显示酶活力、乳液类隐藏）
- **SDS 映射层**（法规项字典 + 映射表），见第九节
- 类别模板生成 Excel 模板的**真实**上传解析（只做弹窗示意）
- 指标库的权限控制 / 审批流
- 指标与检测方法 SOP、设备能力（`EQCAPS`）的关联
- 58 个指标的全量真实数据（只填 2 个 mock 产品实际用到的那部分）

---

## 五、已拍板（P1–P5 全按推荐，2026-09-11 15:10）

| # | 事项 | 拍板结果 |
|---|---|---|
| **P1** | 产品详情页本轮从零搭？ | **从零搭完整详情页**（基础信息 + 理化性质两个区域） |
| **P2** | pH 值占 3 列怎么落？ | **拆成 3 条独立指标**（1% / 10% / 100%），总数 58 |
| **P3** | mock 用哪两个类别？ | **现有「涂饰树脂」+ 新增一个「酶制剂类」产品**（`PRD-2026-013`） |
| **P4** | SDS 第 9 章要不要联动？ | **不联动**（原推荐"联动 4 项"作废，理由见第九节） |
| **P5** | 实验模板本轮改造？ | **不动**，连 `physCat` 占位字段也不加 |

---

## 六、已按推荐默认执行（无需拍板）

| 事项 | 取值 |
|---|---|
| 六组映射 | 见第零节表格 |
| 非数值字段形态 | 允许文本，如「乳白均一液体」「无缩孔」「可与水混溶」 |
| 限值字段表达 | 文本「≤0.5」，不拆上下限双列 |
| 来源标签记名 | 手动编辑固定记当前 mock 用户「王研究员」 |
| Excel 导入 | 按钮 → 弹窗示意（列出"将按 XX 类模板生成 N 列"），不做真上传解析 |
| 单条移除指标 | 做。详情页每条指标一个移除入口 |
| ~~指标库「适用对象」~~ | **撤销**（因已无原料侧消费者，见第九节） |
| 不适用分组 | 不渲染，不留 N/A 空行 |
| 演示脚本 | 加一个固定环节：**现场新增一个指标 → 模板勾选 → 回产品详情页看到它出现** |

---

## 七、验收标准与实测结果

| 项 | 标准 | 实测 |
|---|---|---|
| `smoke_phys.py` | 覆盖指标库 CRUD / 模板勾选 / 详情页分组渲染 / 不适用分组不出现 / 来源标签 / 添加移除 / 持久化 / 重置钩子 / SDS 未改动 | ✅ **81/81 PASS** |
| 全量回归 | 现有 29 个 smoke + 新增 1 个 | ✅ **30/30 全绿** |
| 单文件产物 | `PLM3.0全系统演示原型.html` 可直接双击打开，零依赖 | ✅ 2.07 MB / 16090 行 / script 标签配平 31:31 |
| 存量兼容 | `smoke_law_split.py` 的「基础数据菜单」快照断言已同步更新（预期内变更） | ✅ 82/82 |

### 交付过程中发现并修复的问题

**分片全局符号重名（会导致远程模块静默损坏）。** 新分片里写的 `physVal(code,indId)` 覆盖了
`23z3-js-sds-sections.js` 既有的 `physVal(p)`，使 SDS 第 9 章理化表整表渲染成 `[object Object]`。
两个分片**单独跑 `node --check` 都是通过的**，只有全量回归能发现。

- 修复：重命名为 `physProdVal(code,indId)`。
- 加固：`smoke_phys.py` 第 11 节从「只查数据」改为「查渲染结果」（`physTableHtml()` 输出必须仍含出厂值、`Not available`，且不得含 `[object Object]`）。
- 守卫：新增 `check_names.py`（扫描 920 个全局符号，已知有意覆盖 5 处已登记白名单），
  并接入 `run_smoke.sh` 作为回归前置检查。

---

## 八、分片总览

```
新增：
  src/27z1-js-phys-lib.js      指标库页面 + 58 项 mock 定义
  src/27z2-js-phys-tpl.js      类别模板页面 + 9 类模板
  src/27z3-js-phys-prod.js     产品详情页 prod:detail + 2 个产品实例 + 持久化/重置
  src/05-css-phys.css          样式（需在 build.sh 手动登记，已登记）

改造（共约 12 行）：
  src/22-js-router.js          bd 下加 bd:phys 二级菜单；HL_MAP 加 prod:detail → prod:list
  src/27-js-pages-b.js         prod:list 的「查看」/ 行点击 → prodOpen(code)；
                               新增产品 PRD-2026-013（酶制剂类）；类别筛选补 2 个选项
  src/23z1-js-persist.js       wzReset 链加 _physReset()

工具：
  smoke_phys.py                本模块专项回归（81 断言）
  check_names.py               分片符号重名守卫（已接入 run_smoke.sh 前置检查）

不动：
  src/23z3-js-sds-sections.js  SDS 第 9 章保持硬编码
  src/24-js-doe.js             实验模板不接线
```

---

## 九、口径修正记录：为什么 SDS 不联动（2026-09-11）

原方案（"SDS 第 9 章 4 项改读指标库"）在 Cayla 追问下被判定为**结构性错误**，改为不联动。

**① 两者不是同一层的东西。**

| | SDS 第 9 章 | 指标库 |
|---|---|---|
| 性质 | **法规闭集** | 业务开集 |
| 依据 | GB/T 16483 / CLP 附录 II | 内部检测口径 |
| 项数 | 固定 25 项，顺序也不能动 | 58 项，随时增删 |
| 名称 | **法定**中文名 + 法定英文名 | 内部习惯叫法 |
| 谁说了算 | 法规 / 客户审计 | 研发 |

`23z3-js-sds-sections.js` 里每项都带 `cn` + `en`，例如 `{cn:'运动粘度', en:'Kinematic viscosity'}`。
**法定英文名是法规义务，不能塞进业务指标库让研发维护。**

**② 若将来要联动，正确形态是「三层 + 映射表」，不是直连。**

```
SDS 法规项字典（25 项，只读，法规决定）
        ↕  映射层：SDS-018 → IND-0007（只存 ID，不存名称）
指标库（58 项，业务开集）
```

三条关键规则（本轮不做，仅备忘）：
- **法规新增项而指标库没有时** → 先加法规项 1 条数据，暂无映射；取值走**兜底**（手填 / Not available）。**映射缺失是合法状态，不阻塞出报告。** 需常态化时再「从法规项沉淀为指标」，方向是 法规 → 指标。
- **名称不一致**（`运动粘度` ↔ `粘度(25℃)`）→ 关联**只用 ID，名称永不参与**。报告显示法定名，产品页显示内部名；指标改名不影响 SDS。旧叫法（固含 / 固含量 / 固含量（105℃））靠 `aliases` 收敛。
- **取值是格式化，不是复制字符串** → 指标库存 `min/max/unit/cond`，报告要 `120 ～ 260 mPa·s（25℃）`，由格式化函数拼。

**③ 本轮决定不做的理由（价值判断优先）。**

| 砍掉 | 省下 | 放弃 | 是否后悔 |
|---|---|---|---|
| SDS 映射层 | 1 个分片 + 1 张表 + 约 120 行 | "数据链打通"演示话术 | 不会。演示环节是「现场新增指标 → 模板勾选 → 详情页出现」，不需要 SDS 支撑 |
| 指标库「适用对象」 | 每项少一个下拉 | 原料侧复用 | 不会。原料侧本来没提需求 |
| 实验模板 `physCat` | 一个字段 | — | 不会 |

**④ 不联动不会堵死将来的路。** 法规项与业务指标本为两套独立体系，映射层是**纯加法**，将来接线的成本不会比现在做更高。
唯一要预留的是：**指标 ID 保持稳定**（已纳入 N1）。
