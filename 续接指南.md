# PLM 3.0 演示原型 · 新会话续接指南

> **这是新会话的第一入口。** 读完本文即可开工，无需恢复对话历史。
> **详细技术细节**：第 24 轮起各轮收口记录也都在本文（§九~§十四）；更早的轮次查 `进度交接.md`（2026-09-07 后未再更新）。
> **最后更新**：2026-09-24（阶段 6 封板：OEL 第 8 章、中国 GHS 人工路径、运输第 14 章人工结论与双市场端到端验收）。当前构建 **27165 行 / script 55:55**，共 **47 个 smoke 脚本**；验收见 §二十八。**合规管理原型已封板。**
>
> _（第四十轮：合规模块**阶段 2**——新增 `src/23z6c-js-clp-lifecycle.js`，建立 CLP Annex I 规则**版本生命周期**（版本化存储 + 增量 Diff + 发布门禁 + **单一法规专员**审核发布 + 按日期解析活动版本）；`CLP_RULES` 降级为「活动版本兼容投影」；导入向导 Annex I 分支第 4 / 5 步改为**系统实时计算**（不再写死检查结果）。新增 `smoke_clp_lifecycle.py` **72/72**，**默认 SDS 行为保持 Diff 0 项**，全量 **41/41 全绿**，产物 **25850 行 / script 48:48**）_
>
> _（第三十九轮：建立**最小真实计算引擎内核**——新增 `src/23z6b-js-clp-engine.js`（方法注册表 + 四个执行器 + 统一上下文 / 返回结构 + 兼容适配器），`clpEvaluateMixture()` 由「计算函数」改为「**规则包调度器**」，调度改由 `rule.method` 驱动；**行为保持型重构**，已用旧产物逐项比对证明结果零变化。新增 `smoke_compliance_engine.py` **91/91**，全量 **40/40 全绿**，产物 **24790 行 / script 47:47**）_
>
> _（第三十八轮：SDS 第 3 步新增两个「一键补录演示数据」按钮；修 **aqua 矛盾**——加和法输入可靠性门禁让第 4 步真正兑现第 3 步「会落到待人工判断」的承诺；补录后自动恢复计算）_
>
> _（第三十七轮：引入 **DEMO_TODAY 演示时间锚点** 统一全站「今天」，演示数据按「本周一 − 基准日 2026-09-14」平移，`smoke_weekly` **91/0**。详见 §十七）_
>
> _（第三十六轮：SDS 生成向导第 3 / 4 步改造——异常优先数据准备页 + 分类结论三态口径 + 计算依据折叠 + 一键采纳系统建议；`smoke_deliver` **100/100**、`smoke_sds` 0 错、`smoke_clp` 209/0、`smoke_mfactor` 63/63、`smoke_reach` 160/0，产物 **24154 行 / script 46:46 / 69 注册页**）
>
> ✅ **第三十三~三十七轮已跑全量回归**：39 个 smoke **全绿**（含此前唯一失败的 `smoke_weekly`）。
>
> ✅ **（2026-09-22 第三十七轮已修复）** ~~`smoke_weekly` 1 条失败~~ —— 改为引入 `DEMO_TODAY` 演示时间锚点后 **91/0 全绿**。原问题记录保留在 §16.5，改法见 §十七。
>
> **路径**：本文一律用「项目根目录」指代 `/Users/dowell/Desktop/code/workbuddy/PLM/`。旧版写死的 `/workspace/` 是已失效的沙箱路径，见到请忽略。

---

## 一、项目是什么

**PLM 3.0（产品全生命周期管理）单文件演示原型**——皮革化工企业的研发协同系统，用于向客户/内部演示业务全流程。

| 项 | 值 |
|---|---|
| 产物 | `项目根目录/PLM3.0全系统演示原型.html` |
| 规模 | **27165 行 / 69 个注册页面 / 11 个一级菜单 · 50 个叶子项**（2026-09-24 阶段 6；`check_pages.py`：菜单叶子 50 · 分组 18 · 已注册页面 69 · 跳转目标 50） |
| 特性 | 单文件、零依赖、双击即开；ECharts 已内联；数据存 localStorage |
| 构建 | `bash build.sh`（拼接 `src/` 下 **70 个分片**：16 CSS + 2 HTML + 52 业务 JS，另内联 `vendor/echarts.min.js`；含 script 标签配平校验，当前 **53:53**） |
| 版本 | 已初始化 git（分支 `main`），基线 commit `9bd7238`；最新提交见 `git log`；**改动前先 `git status`，改完提交** |

**定位**：演示原型，**不讲化学专业原理，只做功能与交互演示**。原理性内容只写进交接文档，不进原型。

### 📚 文档清单（**项目根目录**下共 3 份）

| 文档 | 体积 / 最后更新 | 用途 | 何时读 |
|---|---|---|---|
| **`续接指南.md`**（本文） | 158 KB / **2026-09-23** | 精简入口，读完即可开工 | **新会话第一份读** |
| `进度交接.md` | 67 KB / ⚠️ **2026-09-07 后未再更新** | 早期详细技术档案（S0~S23 轮实现细节） | 只在查早期轮次时读 |
| `SDS缺失项清单.md` | 17 KB / 2026-09-07 | SDS 模块对照**真实 SDS 示例文档**（CIRS 欧盟 CLP SDS）的差距分析，含 P0/P1/P2 分级与推进批次建议 | 只在做 SDS 模块时读 |

**⚠️ 项目外资料（Obsidian Vault，不在项目根目录）**——`~/Desktop/work/03 Documents/Obsidian Vault/PLM/需求讨论/`：

| 文档 | 更新 | 用途 | 何时读 |
|---|---|---|---|
| **`法规库与SDS调用合同（设计基准）.md`** | **2026-09-21** | ⭐ **法规相关改动的设计基准**：三方分工 / 库分四类 / 合同 A（专员字段）·B（计算方法库 5 个）·C（SDS 调用返回）/ 6 条不变量。**改这份 = 改合同，需 kaka 确认** | **做法规相关改动前必读**（要点与不符项清单见本文 §十五） |
| `法规库速查卡（设计视角）.md` | 2026-09-21 | 合同的速查版 | 快速对齐口径时 |
| `SDS法规库数据清单.md` | 2026-09-17 | 法规库数据项清单 | 做法规库数据层时 |
| `欧盟SDS必须法规待办.md` | 2026-09-18 | 欧盟 SDS 必须法规的待办 | 排法规库优先级时 |

> ⚠️ **第 24 轮（2026-09-15）起的实现细节都只写在本文**（各章节 + §九~§十四收口记录），`进度交接.md` 没跟上。

> ⚠️ **项目资料库（Project Drive）目前是空的** —— 只有 1 条系统欢迎留言、0 个事项。
> 上述 3 份文档都在**项目根目录下**，不在项目侧；若需团队共享，需主动上传。

---

## 二、当前进度总览

### 📌 阶段状态（**原型规模数据已于 2026-09-22 第三十七轮刷新**）

| 阶段 | 状态 | 说明 |
|---|---|---|
| 原型迭代 | ✅ **阶段 6 原型封板** | **27165 行 / 69 注册页 / 72 分片（54 JS + 16 CSS + 2 HTML）**，产物 55:55 配平（2026-09-24）。未新增页面或菜单；全量 **47 个 smoke**，结果见 §二十八。 |
| PRD 输出 | 🟡 **刚起步（0 篇完成）** | 2026-09-09 切换主线，首个模块待定 |
| 团队共享 | ⬜ **未开始** | 项目资料库（Project Drive）仍为空，0 个事项 |

> 下表是**原型**模块成熟度（历史状态）。PRD 阶段仅作背景参考——素材完备度高的模块，PRD 写得更快。

| 一级模块 | 成熟度 | 说明 |
|---|---|---|
| 🔬 **实验管理** | ★★★★★ | 三层模型（方案 / 执行 / 总结）+ 三条并行路径，全项目最完整 |
| 🛡 **合规管理 / SDS** | ★★★★☆ | S7 对照真实 SDS 文档完整改造（6 步向导 + 组分库 + 法规库）；2026-09-18 菜单重构 + **CLP / REACH / OEL 三个法规库均升级为统一页面**（各 Tab 独立版本信息条）、CLP「导入新版本」改「专员整理稿 + 结构化规则表」五步向导（新分片 `23z6a`）；2026-09-20 修复 REACH 向导二级弹窗覆写；**2026-09-21 第三十三~三十五轮（Codex 并行改码）**：CLP 导入校验改可执行修正闭环 + Annex V 象形图素材维护入口 + 移除 ZDHC 重复注册 |
| 📋 **项目管理** | ★★★★☆ | S5 改造：6 Tab + 右侧阶段导航 + 多对多关联 |
| 🗂 基础数据 | ★★★★☆ | 原料信息已完成打通（详情页/批次/供应商联动/溯源），组分基础信息较完整；2026-09-09 新增关键技术分类/内置计算公式/实验模板三个子页 |
| 📥 需求管理 | ★★★☆☆ | S4 轻量列表页 |
| 📦 产品管理 | ★★★☆☆ | S4 轻量列表页 |
| 🔧 设备资源 | ★★★☆☆ | S4 轻量列表页 + **设备维护台账**（2026-09-15 第二十五轮）：5 Tab（维护台账 / 保养要求 / 保养记录 / 校准记录 / 维保工单）+ 台账状态灯（正常/临期/逾期）+ 异常转维修自动生成工单 + 首页行动区联动 |
| 💡 知识产权 | ★★☆☆☆ | S4 轻量列表页 |
| 🔍 质量/检测管理 | ★★☆☆☆ | S4 轻量列表页；与实验管理有数据关联（是实验"过程测试 / 成品检测"的业务来源） |
| 📂 文档管理 | ★★★★☆ | 公共文档 / 我的文档（两级文件夹：面包屑+卡片+含子文件夹开关+新建/重命名/删除/移动）/ 周报月报归档（类型·提交人·状态·月份四维筛选 + 分页 + 统计卡） |
| 📊 报表管理 | ★☆☆☆☆ | 2026-09-15 第二十四轮新增的一级菜单，`page:'rpt:home'` 直跳**空态占位页**——BI 接入前**不编造报表数据** |
| 出口注册管理 | — | 标记 `plan`（规划中），未实现 |
| 原料库存 | — | 标记 `todo`（暂不做） |

**已完成轮次**（末尾为最新）：S0 地基 → S1 DOE 主线 → S2 SDS 主线 → S3 项目/需求 → S4 轻量列表页铺满 → S5 首页+项目详情改造 → S6 法规库说明折叠 → S7 SDS 深度改造 → M 因子专项 → G2/G3 持久化 → 交付预览改文档流 → G4/G13 → G5/G6/G10 → 第七轮实验列表双树 → 第八轮实验管理三页重构 → 第九轮实验分析与总结 → 第十轮横排对比+整体总结 → **第十一至十四轮（2026-09-07~09）：原料打通 → 基础数据三子页（关键技术分类/内置计算公式/实验模板）→ 菜单三级重构（原料·供应商·组分与合规）→ 文档管理增强（两级文件夹 + 周报月报筛选 + AI 问答）** → **收口：四层架构图 + 核心数据链路图 + 合规/实验两模块流程图** → **第十五轮（2026-09-10，领导评审落地）：菜单顶层重排（文档管理提前、设备/基础数据/产品后移）→ 原料新增替代类型/状态两态/停用时间三字段 + 一物一供 + 批次测试记录（不合格备注必填 + 新增检测结果入口与持久化）** → **第十六轮（2026-09-10 领导评审第二轮，09-11 落地）：首页置顶行动区（来源标签 OA/本系统 + 动作直达）→ 推广并为项目第七阶段（prod:promo 菜单删除）→ 实验设计问问 AI 取消 → 周报月报菜单删除（入口并入我的文档文件夹）→ 合规新增保证函管理占位页** → **第十七轮（2026-09-11）：首页改 Bento Grid 两列栅格（行动区跨行与风险预警同层、KPI 四宫格、周报/最近实验半栏、项目阶段进展宽表通栏）** → **第十八轮（2026-09-11）：实验分析 AI——exp:sum 新增「问问 AI（分析版）」（左栏勾选实验 + 右栏问答，2 条预置问答，无一键采纳）与「智能分析报表」（投料/结果对比表 + 结论段，保存到我的文档 · 分析报表文件夹，周报可引用）** → **第十九轮（2026-09-11）：两个 AI 入口从 exp:sum 列表页迁至报告详情页（分析范围 = 报告引用的实验，只读不勾选）；智能分析报表新增三张 ECharts 图表（投料配比堆叠柱 / 工艺步骤温度曲线 / 结果达标率分组柱 + 100% 标准线）** → **第二十轮（2026-09-11）：理化性质三层模型落地**——基础数据新增「理化性质配置」（指标库 58 项 / 类别模板 9 类）→ 产品详情页 `prod:detail` 新增理化性质分组区（组内小三列表格 + 三通道来源留痕）；指标库与 SDS 第 9 章**不联动** → **第二十一轮（2026-09-11）：12 个轻量列表页补真实详情页**——供应商 / 送检单 / 检测报告 / 设备 / 设备能力 / 维保工单 / 零备件 / 文档 / 知识条目 / 知识产权 / GHS 与受限属性（新分片 `27z4-js-detail-misc.js`，通用 md 骨架 + 11 份定义，全部只读）；并修复回归基础设施缺陷：15 个 smoke 脚本失败时返回 0 导致「假全绿」** → **第二十二轮（2026-09-14）：周报按真实 Word 模板重建，区分两套角色模板**——研发人员版（表头 → 一、研究工作总结及计划 → 二、其他工作含文献资料学习表 → 三、工作量统计 → 四、周计划调整说明）与研究室经理版（置顶「部门管理工作」按项目负责人归集，无工作量统计与文献学习表）；统计周期默认**上一个完整自然周**，弹窗内可切角色/切周期；内容由实验数据自动汇总（项目 → 实验目的 → 1.x.1~1.x.4 四子小节）** → **第二十二轮补充（2026-09-14）：周报填充口径改为「自动 / 人工」二分**——编辑态（查看本周完整周报）**只展示系统能取到的值**，人工维护项（文献学习、其他工作、工作量工时、经理评价与下周计划、计划调整说明）**一律留空**；footer 提供「**一键填充示例数据** / 清除示例数据」切换；**只读态（历史周报 / 文档归档）字段全满**，由 `wkDemoDraft()` 临时换入完整示例草稿、渲染完还原** → **第二十三轮（2026-09-14）：产品目录树（方案 A，挂产品基础信息列表页左侧做筛选）→ 理化性质改版（指标值单值 + 两列卡片 + 来源留痕改灰字）→ 页面清理守卫 `check_pages.py` → 推广阶段口径拍板 W1** → **第二十四轮（2026-09-15）：① 侧栏新增「收起全部菜单 / 展开全部菜单」工具条按钮（品牌区下方固定条，状态写 `plm3_menu_open`，与单组折叠 / `highlightSidebar` 自动展开联动；新分片 `28z1-js-navtools.js` + `06z-css-navtools.css`）② 新增一级菜单「报表管理」（挂「设备资源」之后、`基础数据` 之前，用 `page:'rpt:home'` 直跳空态占位页 `28z2-js-report.js`；BI 接入前不编造报表数据）③ `check_pages.py` 补 `page:'xxx'` 识别（原先只认 `children:[`，会把直跳型一级菜单误报为「找不到注册页」）**
→ **第二十五轮（2026-09-15）：设备维护台账落地**——① 设备列表新增「类型（实验仪器/分析仪器）」「所属部门（研发部/质管部）」两列 + 两个筛选，字段值按设备所在位置推断（检测中心 → 分析仪器/质管部，合成实验室与老化室 → 实验仪器/研发部）② 「保养维护」页改为 5 Tab：维护台账（默认，按到期日升序）/ 保养要求 / 保养记录 / 校准记录 / 维保工单（原 10 条原样保留）③ 三张新表 `EQ_PLANS` / `EQ_LOGS` / `EQ_CALS` + 台账状态灯（绿正常 / 黄临期 / 红逾期）+ 异常转维修自动生成工单 + 首页行动区联动
→ **第二十八轮（2026-09-18）：CLP 法规库统一页面**——`law:clp` 由「附录 VI 清单维护页」升级为统一页面（新分片 `23z6-js-clp.js` 接管注册，23y 旧注册已删）：① 顶部法规主信息（名称/编号/市场/状态/证据灯/主版本/审核与更新时间/数据截止日期/维护责任人 质管-杨工/影响功能 + CLP-REACH 分工说明 + C&L Inventory 与 SVHC/XIV/XVII 独立菜单声明）② 5 Tab：Annex VI 物质统一分类（原 5 条数据，补生效版本与条款位置列）/ Annex I 分类规则（人工审核规则，5 条含 ATE 加和静态示例，抽屉标「研发实现用」不做实际计算）/ Annex III-IV-V 标签字典（12 条 H/EUH/P 码，官方口径标注）/ Annex VIII PCN·UFI 静态占位 / 版本变更与影响（配方与 SDS 数量显式标「示例」）③ 每个 Tab 顶部模块信息条（模块版本/生效日期/审核状态+证据灯/数据截止/责任人/来源类型）④ Tab 间跳转链：Annex VI 条目 → 相关分类规则（自动过滤）→ 对应标签字典（自动过滤）⑤ 右侧详情抽屉复用 `.exp-ai-mask/.exp-ai-panel` 样式（新 CSS `06z3-css-clp.css` 已在 build.sh 登记）⑥「导入新版本」5 步静态向导（登记来源文件→创建新版本→上传结构化数据→查看变更→审核发布），发布后变更入 Tab5；**页面无「自动解析」类表述**。新增 `smoke_clp.py` 82 项断言；`smoke_law_split.py` 移除 CLP 旧断言（改边界检查）；菜单名「CLP 附录 VI」→「CLP 法规库」
→ **第二十七轮（2026-09-17）：首页行动区分页**——`homeActionArea()` 改为 **5 条/页**：取数抽成 `homeActList()`（静态 TODOS + 设备保养 + 临时任务，紧急置顶、按截止日升序），页码存会话变量 `_homeActPage`，翻页 `homeActPage(n)` 只重刷卡片本体（`#homeActBox`）不动整页；分页条（‹ 上一页 / 页码 / 下一页 › + 第 x/y 页）仅在 >1 页时渲染，清单变短自动收页码。同步改 `smoke_eqmaint`（每页 5 条 + `homeActList().length ≥ 6`）与 `smoke_round26`（临时任务不在第 1 页时先翻页再断言标签）。**理化性质改分组表格（方案 A，Cayla 拍板）**——新增 `src/27z7-js-phys-table.js`：包装 `PAGES['prod:detail'].render`（链条 27z3 原渲染 → 27z5 范围值装饰 → 27z7 把每组 `.ph-grid` 替换为表格）。表头五列：指标 / 标准值 / 实测值 / 来源 / 操作；**超范围整行浅红底（`.ph-row-over`）+ 实测值标红 + 方向提示**；**待填整行置灰（`.ph-row-wait`）**；统计条「不做强制拦截」口径与 27z5 装饰共存。⚠️ 类名兼容设计：表格行复用 `.ph-item / .ph-item-nm / .ph-item-v / .ph-item-std / .ph-src / .ph-over-tag`（卡片样式在 06z2 按 `tr` 场景重置），`smoke_phys` / `smoke_catalog` 断言同步为 `.ph-table` 选择器，`smoke_round26` 理化段零改动通过。
→ **第二十六轮（2026-09-17）：① 新增「临时任务」模块**（`proj:task`，挂项目管理下；列表 7 列 + 新建表单 + 执行人反馈文本/附件 + 首页行动区带「临时任务」标签 + 3 条 mock）**② 实验分析改「两步选择 + 可选统计工具」**（左栏 ① 勾选实验 ② 勾选分析参数；右栏可切「交给 AI 分析 / 手动选择统计工具」；图表柱/折线/散点已实现 + 箱线/雷达占位；统计项 7 项；AI 结论只围绕所选参数）**③ 新建实验「实验目的」≥5 字校验**（前端演示级拦截 + 必填标记）**④ 产品理化指标改范围值展示**（上下限 → `6.0～8.0`，单限 → `≥55%` / `≤0.1%`，实测超范围标红并注明方向）
→ **第二十六轮补充（2026-09-17）：过时自检脚本随之更新**——`smoke_exp_ai.py` 旧断言基于 24z4 一步到底的流程（`#aiReportBox` /「丢弃」按钮 / 只读范围条目），改为两步选择 + `anaGen()` + `#aiAnswerBox` +「保存到文档 / 关闭」；`smoke_exp_restructure.py` 建实验补填实验目的；`smoke_round26.py` 容器 id 修正；`smoke_eqmaint.py` 把硬编码的「临期 6 台」改为按规则断言（状态灯台数随运行日浮动）。**全量 36 个 smoke 全绿**
→ **第二十九轮（2026-09-18）：合规管理菜单重构 + REACH 法规库统一页面**——① **菜单**：法规库维护改为 6 入口（CLP → REACH → 职业接触限值 → 运输 → 国内危化品 → ZDHC MRSL）；「REACH / RoHS」更名「REACH 法规库」、「国内危化品分类」更名「国内危化品法规库」；新增「外部参考数据」分组（C&amp;L Inventory 移入，id `ext`）；「受限物质管理」由叶子改为分组（受限物质清单 + RoHS 限用物质）② **新分片 `23z7-js-reach.js`**：`law:reach` 升级为统一页面（顶部法规主信息，与 CLP 页同字段结构 + 6 Tab：SDS 编制要求 16 章 / SVHC 候选清单 / Annex XIV 授权清单 / Annex XVII 限制清单 / Annex XIII PBT·vPvB 规则卡片 / 版本变更与影响；每 Tab 独立数据版本信息条；详情抽屉 + 五步导入向导）③ **新分片 `23z8-js-law-misc.js`**：RoHS 限用物质页（10 条全列、非空占位）+ 职业接触限值 / 运输两个占位页 ④ **接管方式**：`23y` 删 `law:reach` 旧注册、`23-js-sds.js` 删 `law:rohs` 旧隐藏页注册（均改注释，不留重复注册）⑤ 法规统一查询新增「法规类别」（lawKey）筛选，来源筛选中「REACH / RoHS」改「REACH」⑥ 新增 `smoke_reach.py`（**152 断言**），同步 `smoke_law_split` / `smoke_sds` / `smoke_note` 的过时断言；**全量 38 个 smoke 全绿**
→ **第三十轮（2026-09-18）：职业接触限值（OEL）法规库维护页**——新分片 `23z9-js-oel.js` 接管 `law:oel`（`23z8` 的占位注册已删）：以「官方来源 → 数据集版本 → 限值明细 → 版本对比 → 审核发布」五步为单位维护（**OEL 不是一部法规，是各市场发布的限值清单**）+ 7 步导入向导 + 7 类校验；专用样式 `06z5-css-oel.css`；新增 `smoke_oel.py`。**第三十轮补充**：来源收敛为**欧盟（EU）+ 中国**两套，德/英/法/西全部删除（来源 6→2、版本 11→6、明细 26→16）；新增**限值类型语义槽位对齐**（长期/短期/上限，**只对齐语义不换算数值**）
→ **第三十一轮（2026-09-18）：CLP「导入新版本」路线切换为「专员整理稿 + 结构化规则表」**——否掉 Codex 第一版的 AI 智能解析 / 三栏工作台 / 7·9 步 / 导入任务列表页，采纳第二版「法规专员在系统外整理 → 导入结构化规则表」：① `23z6` 数据层扩容（ATP 22 现行 / ATP 23 目标、`CLP_METHODS` 计算方法字典、`CLP_PICTO` 九图式、`CLP_LABELS` 15 行含官方组合码、`CLP_CHANGES` 带来源模块）② Tab1 15 列 / Tab2 14 列 / Tab3 12 列，Tab3 顶部加 **Annex V 只读图式块** ③ 旧向导移出，**新分片 `23z6a-js-clp-import.js`** 独占五步向导（三模块分支，第 4 步三子 Tab 含**仅 Annex I 才有**的规则测试，测试未通过不进发布清单，按规则编号 upsert 落库）+ **新分片 `06z3a-css-clp-import.css`** ④ 顺手补上 `23z9` 一直缺的 `.oel-old/.oel-new` 样式 ⑤ 重写 `smoke_clp.py`（82 → **182** 断言）；**全量 39 个 smoke 全绿**
→ **第三十二轮（2026-09-20）：修复 REACH 导入向导「二级弹窗顶掉向导」**——全站只有 `#modal` 一个模态宿主，向导内 `rchImpImpact()` 再 `openModal()` 会覆写向导、`closeModal()` 直接把向导关掉：① `23z7` 新增 `rchImpRestore()`（按 `_rchImp.step` 重建当前步，第 3 步补 `rchImpShowFile()`）② `rchImport()` 改 `_rchImp={step:1}` + restore，`rchImpNext(n)` 补存步号 ③ 影响范围 footer「关闭」→「返回向导」 ④ `smoke_reach` 补 4 条**真实点击**断言（151 → **155**）⑤ **更正第三十一轮对 OEL 的误判**（OEL 无此问题：`23z9` 仅 `oelImport` / `oelSrcNew` 两处 `openModal`，字典走独立遮罩 `#oelDwMask`）⑥ 同类风险全项目排查完毕（`23-js-sds.js` 的 `lawUpload` / `dbImport` 均不弹二级模态）。**全量 39 个 smoke 全绿**
→ **第三十三轮（2026-09-21）：CLP 导入校验改为可执行的修正闭环**——① 第 4 步阻断 / 告警 / 提示由纵向卡片改为六列二维表（级别 / 检查项 / 数量 / 问题明细 / 处理方式 / 当前状态），六列在 900px 弹窗内完整显示 ② 阻断项取消「标记已处理」，未修正时禁止提交审核 ③ 新增「下载问题明细」CSV 与「上传修正版并重新校验」入口，复检后阻断清零、修正版文件名与状态留痕 ④ 告警保留为发布前确认，提示不阻断 ⑤ Annex I 新增 CLP-R-0008「需研发实现」演示明细，在数据校验与版本比较中均可见且自动排除发布 ⑥ 修复数据校验 / 规则测试 / 版本比较切换后选中态仍停留在数据校验的问题 ⑦ CLP 顶部两条与 Annex I 一条提示文本框合并收进标题右侧“页面说明”，默认折叠、点击展开 ⑧ 原型中的维护人 / 审核人姓名统一为“杨工” ⑨ `smoke_clp` **204/0**，构建 46:46 配平 / 23971 行。
→ **第三十四轮（2026-09-21）：Annex V 象形图素材维护入口**——① Tab3 顶部将 Annex V 从“只读”改为“9 个固定编号 · 素材独立维护”，新增“管理象形图素材”按钮 ② 弹窗用二维表展示 GHS01–GHS09 的当前文件、状态、更新时间、维护人 ③ 每行支持选择 SVG / PNG 进行上传 / 替换演示，编号不可新增 / 删除 ④ 弹窗明确图片素材与 Annex III（H / EUH）及 Annex IV（P 码）结构化字典导入分离 ⑤ `smoke_clp` **209/0**，构建 46:46 配平 / 23988 行。
→ **第三十五轮（2026-09-21）：修复 ZDHC 法规库重复注册**——① 定位到旧 `23-js-sds.js` 的 `regPage('law:zdhc', …)` 与 `23y-js-law-query.js` 的 `regLawMaintenance('law:zdhc', …)` 两处注册 ② 删除旧 SDS 页面注册，保留法规统一维护页的单一路由 ③ `smoke_law_split` 增加源码级单注册断言；菜单、统一查询、ZDHC 分库维护与说明文本回归均通过（**64/0**），构建 46:46 配平 / 23981 行。
→ **第三十六轮（2026-09-21）：SDS 生成向导第 3 / 4 步「结论诚实性」改造**——原则是「不深究规则引擎真伪，只保证演示叙事通顺」。① **第 3 步改「异常优先」**：5 个概览 KPI（组分数 / 已自动获取 / 待补充 / 待确认低可信 / 是否满足分类计算条件）+ 四个异常区块（缺失 / 仅 PubChem 辅助来源 / 多来源冲突 / 会导致危害类别算不动）+ 「查看全部汇集数据」默认收起；新增 `EVID_META` / `wzEvidence()` / `wzBlockers()` / `CONFLICT_MOCK`（冲突与阻止项放在**数据层**，不在渲染函数里写结论）② **第 4 步三态口径**：`ed` `pmt` → **待人工判断（need:judge）**（引擎未实现 + `COMP_CLP` 根本无 ED/PMT 字段，原先那句「甲醛 0.35% 未列入 ED 清单」属**无源断言，已删除**）；`stot` `carc` → **系统建议·待人工确认（need:confirm）**；5 项引擎真算的保持「系统自动判定」③ 新增 **「查看计算依据」折叠**（`evToggle()`）：规则包编号 / CLP-R 规则号 / CLP-M 方法号 / 输入参数 / 计算过程默认收起④ 新增 **`adoptAllSug()` 一键采纳系统建议**——只采纳有建议值的 judge 型不在此列；状态流转为 `confirmed`（区别于 auto / manual，审计时可还原「人是否真看过」）⑤ `wzCheck(4)` 阻断提示改为区分 **待人工判断 vs 待人工确认**。产物 **24154 行 / script 46:46**；`smoke_deliver` **100/100（+3）**、`smoke_sds` 0 错、`smoke_clp` 209/0、`smoke_mfactor` 63/63、`smoke_reach` 160/0

→ **第三十七轮（2026-09-22）：引入 `DEMO_TODAY` 演示时间锚点**——修 `smoke_weekly` 遗留失败（mock 实验日期写死 `2026-09-09/10/11`，随真实日期滑出统计周期 → 周报整页空掉）。① `20-js-core.js` 新增锚点工具（`demoNow` / `demoYmd` / `dateAdd` / `daysBetween` / `demoWeekRange` / `lastWeekDate` / `demoDate` / `expAnchorDates`），`nowStr` / `todayStr` / `daysFromNow` 统一改走它；② 平移量 δ = **本周一 − DATA_EPOCH(`2026-09-14`)**（必为 7 的倍数，保星期对齐）——用「今天 − 基准日」会在周中错位，实测排除；③ `21-js-data.js` 在 `seedExperiments()` / `seedExpSummaries()` 出口对实验、DOE 方案、`expSummaries[].items[].basic.date` 整体平移（`expDate()` 的第一优先级取自后者，首版漏了它导致实验集合错位）；④ 另 7 个分片的本地「取今天」函数统一走 `demoNow()`；⑤ `smoke_weekly` 4 条写死周期断言改为 Python 侧按同一规则动态算，新增 3 条锚点断言。**`smoke_weekly` 91/0、全量 39/39 全绿**，并用 +7/+28/+77/+112 天四个未来日期验证实验集合恒定。另修自身两个坑：`expAnchorDates` 加幂等标记（`seedExperiments` 会被调两次）、补丁脚本重复插入 4 份（已回退重来）
→ **第三十八轮（2026-09-22）：SDS 第 3 步「一键补录演示数据」+ 修 aqua 矛盾**——用户问「演示系统没引擎没清单，第 4 步计算部分会不会全空白」，实测结论是**不会空白**（5 项引擎真算 + 2 项系统建议 + 4 项待人工作业），但实测抓出**两处自相矛盾**：① 第 3 步 `wzBlockers()` 承诺「对应危害类别在第 4 步会落到待人工判断」，第 4 步 `clpEvaluateMixture()` 却照算不误标 `auto` ② EUH 候选区仍写「EUH380 / EUH450 由 ED、PMT 判定自动带出」，而 ED / PMT 已改为待人工判断。本轮：① 新增 `DEMO_FILL`（演示补录数据源，值写在数据层）；② 第 3 步①区块加「一键填充演示数据」、④区块加「一键补充演示数据」（`wzFillDemo(kind)`），补录后 `wz.classItems=null` 强制重算；③ `23z6` 新增**加和法输入可靠性门禁** `aqBad`——存在 `aqState!=='known'` 组分时 aqua 降级 `pending/need:'judge'`，补录后恢复 `auto`；④ 修正阻止项文案（**M 因子按 Annex I 表 4.1.3 只适用于 Acute 1 / Chronic 1**，甲醛 Chronic 2、乙二醇单丁醚 Chronic 3 本就不适用，原表述站不住）；⑤ EUH380/381/450 文案同步三态口径。`smoke_sds` 新增 5 条真实点击断言并**加严**（aqua 由单点断言改为「补录前待判断 / 补录后恢复自动」两段式），**全量 39/39 全绿**
> 📌 **2026-09-09 状态：原型迭代阶段已收口，主线转向 PRD 输出。** 新会话默认不再新增原型页面，除非用户明确要求。

---

## 三、核心结论（⚠️ 不可推翻，新会话必须遵守）

这些是用户明确拍板或反复确认的设计决策。**新会话不要重新讨论、不要"优化"掉**。

### 3.1 实验管理：三层数据模型

```
doeSchemes[]     设计层   1 条 = 1 套 DOE 方案（草稿/待下发/执行中/已完成）
    ↓ 确认下发
experiments[]    执行层   source='普通' 或 'DOE'
    ↑                      DOE 运行记录带 schemeId + runSeq，id 形如 DOE-2026-0312-R03
expSummaries[]   总结层   1 份对比总结报告，引用多条 experiments
```

- **普通实验** → 1 条记录 → 录入数据 → 实验分析
- **DOE 方案** → 生成试验矩阵 → 确认下发 → N 条实验记录 → 各组录入 → 回到方案统一分析
- 单向聚合（`schemePlanWithRes`），**不做双向同步**

### 3.2 「实验分析」的对象是一组可比实验，不是单条记录

> 用户原话：*"实验分析的对象不是某一条实验记录，而是一组具有可比性的实验记录。"*

### 3.3 「同个实验工艺」= 相同工艺路线，参数可有受控差异 ⭐ 本轮最关键结论

> 用户原话：*"实验采用相同的工艺路线或工艺模板，但具体工艺参数可以存在受控差异。"*
> *"最好不要定义成'所有工艺参数必须完全一样'，否则只能做重复性验证，没办法比较参数影响。"*

因此字段拆成四个，**不能合并回**：

| 字段 | 语义 | 示例 |
|---|---|---|
| `craft` | 工艺**路线/模板**，只写路线与工序顺序，**不含参数取值** | `三辊涂布 → 热风烘干 → 室温熟化 → 压花` |
| `diffNote` | **受控差异说明**：刻意变化了什么、什么保持一致 | `受控差异：树脂来源（国产/进口/1:1 混拼）。涂布量 18 g/m²、烘干 120℃×3min 三组一致。` |
| `items[].craftParams` | 该组**实际参数取值** | `涂布量 18 g/m² · 120 ℃ × 3 min · 熟化 24 h` |
| `items[].diff` | 该组在**受控变量上的取值** | `树脂：进口 WPU-320（基准样）` |

**校验**：新建/编辑总结时勾选实验数 **< 2 直接拒绝保存**。

### 3.4 详情页布局 = 横向并列对比 + 整体总结

- 每行结构：`1 个字段名（左侧固定 200px） + N 个实验值（每列 min-width 280px）`
- 5 个区块横排：基本信息 / 本组工艺参数 / 原料添加 / 过程测试 / 成品检测
- N > 3 时容器 `overflow-x:auto` 横向滚动，`data-cols="N"` 切换最小宽度
- 底部独立「**整体总结**」卡片贯穿整页（`summaryOverall` 字段，针对全部 N 组的结论与建议）
- 每组自己的总结保留为对比表最末一行

### 3.5 演示级合成分析

`generateAnalysis(e, respName)` 基于 `makeRng(hashStr(e.id + '|' + respName))` **种子随机合成** ANOVA / 残差 / Pareto，**不依赖真实录入值**。因此普通实验与 DOE 方案可复用同一报告页。这是刻意为之，不要"修正"成真实计算。

### 3.6 原料模块口径（2026-09-10 领导评审拍板，不可推翻）

- **菜单顶层顺序**：需求 → 项目 → 实验 → 合规 → 质量/检测 → 文档 → 设备资源 → 基础数据 → 产品 → 知识产权。
- **物料状态值域只有两态**：`正常 / 停用`（原 启用/审核中 已并入正常，不要再加回）。
- **替代字段挂被替代的旧料**（用户确认中，暂按此实现）：`substType`（等值替代/降级替代）+ `substTo`（替代料编码，可跳转）+ `stopDate`（停用时间）。若用户最终拍板"挂新料"，只需把这三字段换宿主。
- **一物一供**：一个物料只有一条供应商数据（`MAT_SUP` 每键一条），详情页卡片显示「一物一供 · 唯一供应商」。
- **批次测试记录**：一批次一次检测，字段含 `testDate/items/status(合格|不合格|待检)/note`；**不合格时备注必填**（`matTestSave` 强校验）；「新增检测结果」modal 在 `22z3-js-rawmat-test.js`，用户新增记录持久化到 localStorage（`plm3_matbatch_v1`，按批次号去重合并），**已挂进 wzReset 重置链**。

### 3.7 一键重置必须覆盖所有新增数据

`wzResetConfirm()` 需同时重建：`doeSchemes` / `experiments` / `expSummaries` + 重置 `sumListState`。**每新增一个数据层，都要挂进重置链**。

### 3.8 首页 Bento Grid 栅格规范（2026-09-11 拍板，不可推翻）

- 首页内容区统一包在 `.bento` 两列栅格，**不再是全宽卡片纵向堆叠**。
- 占位类：`.b-c1` / `.b-c2` 指定列，`.b-span2` 通栏，`.b-row2` 跨两行（当前排布未启用跨行）。
- **固定排布**（改前先想清楚对齐关系，1440 视口实测）：
  - 行 1 左 `接下来要做` / 右 `风险预警` —— **两卡等高、底边严格对齐**（栅格 stretch 天然保证）
  - 行 2 `关键指标` —— **通栏矮条，横排四格**（`kpi-bar`，高度 <140px），不占左右列
  - 行 3 左 `本周周报` / 右 `最近实验`（半栏时省略「类型」列）
  - 行 4 `项目阶段进展` 通栏 —— **7 段进度条必须占满宽度，放半栏会把行高撑到 1300+**
- 视觉口径（用户明确要求，勿回退）：
  - 紧急任务：**整行浅红底 + 左侧红条 + 置顶排序**，状态**不再另挂「紧急」标签**（避免与来源标签堆砌）
  - 每条行动项**只允许 1 个标签**＝来源标签（OA 项目计划蓝 / 本系统任务绿）
  - 预警卡片走 `.alerts-slim`：**白底 + 3px 左侧色条 + 圆形小图标**，禁止大面积底色
    （作用域限定在首页，不污染全局 `.notice` 组件）
  - 问候语走 `greetWord()`（凌晨/早上/中午/下午/晚上），**副标题不再重复紧急与预警计数**（徽章只在行动区）
- 独立函数返回 `.card` 的（如 `weeklyCardHTML()`）必须用 `<div class="b-cell">` 包裹才能撑满格子。
- 新分片 `src/02z-css-home-bento.css`：**新增 CSS 分片要同步在 build.sh 的 CSS 段登记**。

### 3.9 实验分析 AI 口径（2026-09-11 院长意见 + 第二轮迭代，不可推翻）

- **AI 只做分析、不做设计**：设计环节的「问问 AI」已永久移除（`24z1`/`24-js-doe` 两处按钮与
  `openExperimentAI` 等函数已清理，勿恢复）；分析环节的 AI 是**补位**不是回退。
- **入口在总结报告详情页**（`exp:sum-detail`）操作区，**不在列表页**；
  实现全在 `src/24z4-js-exp-ai-analysis.js` + `src/03z-css-exp-ai.css`：
  - `openSumAI(sumId)` 问问 AI（分析版）：左栏 = **报告引用的实验（只读清单，无 checkbox）**，
    右栏 = 问答区；2 条预置问答由 `aiPresetQA(sum)` **按当前报告动态生成**（问题与回答都引用
    本报告真实编号，换报告自动适配，勿改回硬编码）；
    **回答区没有「一键采纳」**（分析场景无表单可填）。
  - `openSumReport(sumId)` 智能分析报表：**3 张图 + 3 张表 + 结论段**。
    底部「保存到文档 / 丢弃」→ 我的文档 → **分析报表文件夹（F-M4）**，报表名含报告号。
- **分析范围 = `aiSumExpIds(sum)`**（`expIds` 与 `items` 双取并集），单次上限 8 组，不足 2 组给 toast。
- 图表口径（ECharts 走 `20-js-core.js` 的 `chart(domId, option)`）：
  | 图 | 表意 | 关键口径 |
  |---|---|---|
  | 图一 投料对比 | 配比构成 | 堆叠柱，原料为系列，tooltip 附占比 |
  | 图二 工艺步骤 | 温度 — 累计时间 | 阶梯折线，每组一条；**工序数据取 `normalDetail.processes`，DOE 运行记录不含工序时沿报告骨架推演并标记 `approx`**，图下自动给「温度极差」一致性结论 |
  | 图三 结果对比 | 达标率 | **统一换算为 达标率 %（实测/标准×100）**，否则评分(4.2)与次数(620)量纲不同无法同图；标准线固定 100% |
- ⚠️ 图表生命周期：容器 id 固定为 `aiChartMat / aiChartStep / aiChartRes`，统一由
  `aiRenderCharts()` 在 `innerHTML` 之后 `setTimeout(30)` 渲染；**关闭弹窗必须走
  `aiCloseModal()`**（会 dispose 三个实例），直接用 `closeModal()` 会残留实例。
- 周报联动：`weeklyDraft.refReport`，首页周报卡片与完整周报均可下拉引用已保存报表。
- ⚠️ 两个易踩的坑（已修，勿回退）：
  1. `DOCS` 定义在 **27-js-pages-b.js**，晚于 24z4 加载 —— 本地缓存恢复必须放在
     `DOMContentLoaded` 里，顶层立即执行会因 `DOCS` 未定义而静默丢失。
  2. 恢复写入 DOCS 的记录**不能打 `_extra` 标记**，`seedDocFolders()` 会清除所有 `_extra`，
     刷新后报表从「我的文档」消失。
- 报表持久化沿用独立键 `plm3_aireport_v1`，已在 `wzReset` 链挂 `_aiReset()`。

### 3.12 实验分析 / 临时任务 / 理化范围值口径（2026-09-17 拍板，不可推翻）

**A. 实验分析：两步选择 + 可选统计工具（`24z5`，覆盖 24z4 的 `openSumAI` / `openSumReport`）**
- 两个入口（详情页「问问 AI」与「生成智能分析报表」）**共用同一套弹窗**，只是 `_anaEntry` 不同：
  - `'ai'`：锁定「交给 AI 分析」，右栏是预置问题 + 自由提问，**不出现统计工具切换**；
  - `'report'`：右栏多一个「分析方式」切换（交给 AI 分析 / 手动选择统计工具）。
- **左栏固定两步**：① 勾选参与分析的实验（2~8 组，少于 2 组拒绝）② 勾选分析参数
  （参数从实验记录里实际存在的数据提取：工序参数温度/时间 · 配比原料用量 · 过程检测 · 成品检测，
  至少 2 组有值才进列表；各组取值相同会标「各组一致」）。
- **AI 结论只围绕所选参数**——`anaAiText()` 逐参数给有值数 / 均值 / 标准差 / 极差 / 最优或取值最高组，
  末尾附 `anaAiRankText()` 综合排序；文案固定带「未做全量数据分析；仅供参考」。
- **统计工具**：图表类型 5 项中 `bar / line / scatter` 已实现，`box / radar` 标「（规划中）」只出占位提示
  且**不产出图表容器**；统计项 7 项（均值 / 中位数 / 方差 / 标准差 / 极差 / 线性拟合含 R² / 相关系数）
  渲染在 `#anResult`。
- ⚠️ **容器 id 沿用 24z4 的 `#aiAnswerBox`**（不是 `#anAnswerBox`）——24z5 注释里写明了是为了
  复用既有问答样式与自检选择器。自检脚本踩过这个坑。
- 底部按钮为「保存到文档 / 关闭」（原 24z4 的「丢弃」已去掉）；`anaSaveDoc()` → `aiSaveReport()`。
- 选择按 `sumId` 缓存在会话内（`_anaCache`），重开弹窗保留上次勾选。

**B. 临时任务（`27z6`，菜单 `proj:task` 挂项目管理下）**
- 列表 7 列：任务标题 / 发布人 / 执行人 / 发布时间 / 要求完成时间 / 状态 / 操作；
  状态仅三态：待接收 → 进行中 → 已反馈。
- 编号自动生成 `TMP-2026-NNN`；发布人 = 当前登录用户（`TMP_ME`），初始状态「待接收」。
- **执行人视角**：`taskTodos()` 只返回指派给 `TMP_ME` 且未反馈的任务，并入首页行动区，
  来源标签单独走「临时任务」；行动区按钮通过 `homeActGo(t)` 的 `t.fn` 分支直接开反馈弹窗。
- 反馈弹窗：文本必填 + 附件；提交后写 `fbAt` 并把状态置「已反馈」，发布人可在列表查看。
- 持久化键 `plm3_tmptask_v1`，重置钩子 `_tmpReset()` 已挂 `23z1` 的 `wzReset()`。

**C. 新建实验「实验目的」必填（`24z6`，包装 24z1）**
- ≥5 字（`EXP_PURPOSE_MIN`）才放行；不足时 toast「实验目的不得少于 5 个字（当前 N 字）」并中断提交。
- 打开表单时给 `#nePurpose` 加 `req` 必填标记 + 占位提示「至少 5 个字…」。
- ⚠️ **只覆盖「新建」表单**，24-js-doe 的 `editNormalExp`（`#nedPurpose`）本次未纳入。
- ⚠️ 24z1 `saveNewNormalExp` 原本对空实验目的有兜底默认值，**校验必须在外层拦截**，别去改 24z1。

**D. 产品理化指标范围值（`27z5` + `06z2-css-phys-range.css`）**
- 产品详情页理化性质区每项增加一行标准值：上下限齐全 → `6.0～8.0`；只有上限 → `≤0.1%`；
  只有下限 → `≥55%`；指标库未定义 → `—`。
- 实测值对照上下限判断：低于下限 / 高于上限即标红，并挂角标注明方向（如「高于上限 8」）。
- **产品实例自带 `std` 时以实例为准**（例：酶制剂固含量 `≥92%`），不被指标库上下限误判——
  回归脚本 `smoke_round26.py` 专门盯这条。
- 保留「不做强制拦截」口径，统计条文案不改。

### 3.13 合规管理菜单与法规库口径（2026-09-18 第二十九轮拍板，不可推翻）

**A. 菜单结构**（`src/22-js-router.js` 的 `comp`）

```
合规管理
├─ 流程引导
├─ SDS 管理             sds（SDS 文档列表 / SDS 生成向导）
├─ 法规统一查询          law:query
├─ 法规库维护            law
│  ├─ CLP 法规库         law:clp
│  ├─ REACH 法规库       law:reach
│  ├─ 职业接触限值（OEL）  law:oel
│  ├─ 运输法规库         law:trans    （占位）
│  ├─ 国内危化品法规库    law:cn
│  └─ ZDHC MRSL          law:zdhc
├─ 外部参考数据          ext
│  └─ C&L Inventory      law:cl
├─ 受限物质管理          subst
│  ├─ 受限物质清单        subst:list
│  └─ RoHS 限用物质       law:rohs
├─ 出口注册管理          export:reg（plan）
└─ 保证函管理            comp:guarantee（plan）
```

- **C&L Inventory 不属于法规**，移到「外部参考数据」独立分组，不再与 CLP 混放。
- **RoHS 不再与 REACH 合并**：拆到「受限物质管理 · RoHS 限用物质」；**不做空占位页**，直接列全 10 条更直观。
- **ZDHC MRSL 本期维持现状**（仍在法规库维护下），后续随「行业与客户清单」统一调整；文案中不宜再把它描述成"法规"。
- ⚠️ 「受限物质管理」由叶子改分组后，其下**必须**保留「受限物质清单」子项，否则 `subst:list` 会变成孤儿页（`check_pages.py` 会报）。

**B. 路由接管方式**（沿用 CLP 既有做法）
- 新页面一律在新分片 `regPage` 注册，**旧注册行删掉改成注释**，不留同一路由的两次注册（后加载静默覆盖是项目已知坑）。
- 已接管：`law:clp` → `23z6`、`law:reach` → `23z7`、`law:rohs` → `23z8`（原 `23-js-sds.js` 里的隐藏旧页）、`law:oel` → `23z9`（原 `23z8` 占位页）。
- `regLawMaintenance()` 函数**保留**——`law:cl` / `law:cn` / `law:zdhc` 仍在使用。

**C. REACH 法规库页面（`23z7`）口径**
- **三条法规依据必须分清**（勿混填到「法规出处」一列）：REACH **Article 31** 决定「何时必须提供 SDS」；REACH **Annex II**（经 (EU) 2020/878 修订）决定 **16 章结构与内容要求**；**CLP** 提供分类与标签要素。
- **系统不判断产品是否需要编制 SDS**——用户创建 SDS 即代表业务已判断需要；Tab 1 只维护 16 章的结构、字段与填写要求。
- Annex XVII **不能做成 CAS 黑名单**：必须含 适用对象 / 限制类型 / 限制用途 / **浓度阈值（可为空，如 Entry 28–30 只禁用途不设阈值）** / 生效日期 / 豁免条件；查询结论只能表述为「**可能命中限制**」。
- Tab 2 SVHC 的「当前版本」与「列入日期」不可混为一谈（示例数据须显式标注）。
- Tab 5 PBT/vPvB 用**判定规则卡片**（判定对象 / 所需输入 / 判断条件 / 输出结果 / 法规出处 / 规则版本 / 审核状态），抽屉标「研发实现用」，**不做真实判定计算**。
- Tab 6 一律「**规划能力**」口径：监控来源列 EUR-Lex / Cellar，**页面不得出现「已实现自动监控 / 自动解析」类表述**；影响配方 / SDS 数量必须标「示例」。
- 每个 Tab 顶部显示**自己的** 数据版本 / 生效日期 / 来源文件 / 最近更新时间 / 审核状态（SVHC、Annex XIV、Annex XVII 与 Annex II 的更新时间并不同步）。
- 各 Tab → 法规统一查询走 `showPage('law:query',{lawKey:'svhc'|'xiv'|'xvii'})`，查询页据此预置「法规类别」筛选。

**D. RoHS 限用物质（`23z8`）**
- 字段：物质名称 / 中文名 / CAS / EC / 限值（**镉 0.01%，其余 0.1%，口径 = 均质材料中的重量百分比**）/ 适用对象（电子电气设备）/ 备注 / 数据版本 / 来源（Directive 2011/65/EU Annex II，经 (EU) 2015/863 修订）。
- PBBs / PBDEs 是**物质组、无单一 CAS / EC**，如实标注，不硬塞 CAS。
- 数据来源方式：**RoHS 没有官方结构化数据下载** → 人工维护一份 10 行的结构化数据表（**区别于 CLP 附录 VI 的批量导入模式**），版本随指令修订人工更新。
- 页面须注明「**豁免项见指令附录 III / IV，本页仅列附录 II 受限物质**」并预留「查看豁免清单」入口（占位）。
- **中国 RoHS 为独立体系**（工信部《电器电子产品有害物质限制使用管理办法》），是否纳入**待业务确认，本页不做**。

**E. 职业接触限值（OEL）页面（`23z9`，第三十轮实现）**
- **核心口径**：OEL **不是一部单独法规**，而是各市场官方发布的限值清单。用户**不逐条维护散落条文**，而是以「**官方来源 → 数据集版本 → 限值明细 → 版本对比 → 审核发布**」五步为单位维护（页内有五步流程条）。这是本页与 CLP / REACH 页最本质的区别：**CLP / REACH 维护「法规条款」，OEL 维护「限值数据集版本」**。
- ⚠️ **来源范围（2026-09-18 二次拍板，不可推翻）**：**公司只做欧盟与中国两个市场**，故来源**只维护两套** —— 「欧盟（EU）IOELV」与「中国 GBZ 2.1」；德国 TRGS 900 / 英国 EH40 / 法国 INRS ED 984 / 西班牙 LEP 等**一律不建**（首版曾按 6 套来源演示，已收敛）。后续要扩展市场时再走「新建数据来源」。
- ⚠️ **限值类型语义槽位对齐（2026-09-18 二次拍板）**：中国为 MAC / PC-TWA / PC-STEL，欧盟为 8h TWA / STEL，**类型名不同但语义只有三类**。系统用「**限值类型字典**」把原文类型名映射到统一**语义槽位**：
  - **长期**（8 h 时间加权平均）← 中国 PC-TWA / 欧盟 8h TWA
  - **短期**（15 min 时间加权平均）← 中国 PC-STEL / 欧盟 STEL
  - **上限**（任何时点不得超过）← 中国 MAC（欧盟 IOELV 清单未设上限型，字典里如实标「—」）
  槽位**只用于按槽位筛选与对齐**：`Tab3` 有「语义槽位」列 + 槽位筛选器 + 「限值类型字典」抽屉，`Tab4` 变更行标注涉及槽位，导入向导第 4 / 6 步也带槽位。
  **不做数值换算归一**（ppm→mg/m³ 需温度 / 压力 / 分子量假设，跨体系折算属口径搬运）——页面与字典都显式写明「**只对齐语义、不换算数值**」。
- **顶部**：已覆盖国家/地区（按已建数据集的 `rg` 去重计）· 已发布数据集数量 · 待审核版本数量 · 最近更新时间 + 两个主入口「新建数据来源」「导入限值数据集」。
- **四个 Tab**：① 数据来源（Tab1，9 字段，演示 2 条）② 数据集版本（Tab2，12 字段 + 6 操作，演示 6 个版本）③ 限值明细（Tab3，19 字段，演示 16 行）④ 版本对比与审核（Tab4）。
- **数据集版本状态机**：草稿 → 待审核 → 已发布 / 已失效。演示数据 6 个版本 = 已发布 2（EU V2026.1 / CN 2019 版）· 待审核 2（EU V2027.1 / CN 2026 送审稿）· 草稿 1（CN 2027 版）· 已失效 1（EU V2025.1）。**原始 PDF 或网页存档与结构化 Excel 分开保存**（两个独立附件字段，不是二选一）。
- **限值明细**：**同一 CAS 在不同来源、不同版本中的记录必须并存，不得相互覆盖**——演示数据里甲苯 `108-88-3` 跨 3 个版本（EU V2026.1 / EU V2025.1 / CN 2019）并存，点开抽屉可看到「同 CAS 其他来源 / 版本」对照表（含语义槽位列）。
- **版本对比与审核**：两条会话 —— A「欧盟 IOELV V2026.1 → V2027.1」（6 条变更）· B「中国 GBZ 2.1-2019 → 2026 送审稿」（4 条变更），合计覆盖 **6 类变更**（新增物质 / 删除物质 / 数值变化 / 单位变化 / 标记变化 / 备注或适用范围变化）；动作为 审核通过 / 退回修改 / 发布。
- **导入向导 7 步**：选择数据来源 → 填写版本信息 → 上传附件（结构化文件为硬门槛）→ 解析预览 → 数据校验 → 与上一版本比较 → 提交审核。
- **数据校验 7 类**：CAS 格式错误 / 物质名称缺失 / 数值存在但单位缺失 / 同一数据集内重复记录 / 生效日期冲突 / **无法识别的物质标记**（原「国家标记」已改名）/ 与上一版本相比发生重大变化；分级 **阻断 / 告警 / 提示**，**存在阻断项时禁止进入下一步**，逐条「标记已处理」后才能放行。
- **明确边界（页面须体现）**：不从 PDF 自动识别并直接发布数据；不自动判断哪套清单限值更严格；不把两套市场限值合并成一个值；不根据投放市场自动选择 OEL；不实现真实 AI 解析与法规网站监控；「投放市场 → 有效法规版本 → SDS 第 8 节」的关联**后续统一实现**。
- **本页不改动**：CLP 法规库、REACH 法规库、SDS 生成向导、法规统一查询、投放市场关联逻辑。
- ⚠️ 数据为**演示用节选**；中国 GBZ 2.1-2019 的限值与表 4 生物监测指标取自标准原文，欧盟 IOELV 数值按 20 ℃ / 101.3 kPa 由原文 ppm 换算，其余为示例整理。

**F. 运输占位页（`23z8`）**
- `law:trans`：数据源 **UN TDG（橙皮书）+ GB 6944 / GB 12268 + IMDG（海运）+ IATA（空运）**；**运输属独立法规体系（CLP 不管运输）**，页面必须体现「多运输方式」；用于 **SDS 第 14 章**。
- 注明「数据源待确认、本期仅占位」；**不进入法规统一查询的来源筛选**。

**G. 法规统一查询（`23y`）新增项**
- 工具栏加「法规类别」`#lqLaw`（7 类：CLP 附录 VI / SVHC 候选清单 / Annex XIV 授权清单 / Annex XVII 限制清单 / C&L Inventory / 国内危化品 / ZDHC MRSL），支持 `showPage('law:query',{lawKey})` 预置。
- 数据类型下拉补「授权」（供 Annex XIV 命中记录）。
- 示例命中记录 15 条 → **18 条**（补 DEHP / 铬酸铅的 Annex XIV、六价铬的 Annex XVII）。

**H. CLP 法规库页面与「导入新版本」口径（`23z6` + `23z6a`，第三十一轮实现）**

⚠️ **最重要的一条：CLP 法规库页面全程不出现「AI 解析 / 自动解析 / 系统解析」**。这个产品的定位是「**面向业务人员演示的 PLM 业务原型**」，不是法规爬虫。导入对象统一定义为「**已由法规专员对照法规原文逐条确认的结构化规则表 / 整理稿**」，系统的活儿只有六件：**结构校验 → 版本比较 → 规则引擎支持状态检查 → 测试结果展示 → 审核发布 → 来源与历史版本追溯**。

**H-1. 三个 Annex 模块各自的「可获取性」不同（决定了导入路径不同）**

| 模块 | 数据形态 | 导入路径 |
|---|---|---|
| **Annex VI** 物质统一分类 | **有结构化数据**（ECHA CHEM 导出 / 开放数据门户 CSV；官方无正式 Excel，有非正式整理稿） | 直接导入整理稿，**法律效力以 OJ 公布文本为准**（页面须显式写明） |
| **Annex I** 分类规则 | **无结构化数据**，只有法规条文（推断性规则） | 法规专员**在系统外**用统一模板整理 + 逐条对照原文确认后导入 |
| **Annex III/IV/V** 标签字典 | **无结构化 Excel**，只有排版文本 + 图片文件 | H / EUH / P 码走结构化字典导入；**Annex V 象形图作为独立图片资产，在“管理象形图素材”入口上传 / 替换，不随字典导入** |
| Annex VIII PCN/UFI | — | **本轮不进导入入口**（Tab 保留展示，模块下拉只有 3 项） |

**H-2. 五步向导（`23z6a`，三模块共用同一骨架）**

`登记来源 → 创建版本 → 上传结构化数据 → 数据校验与版本比较 → 审核发布`

- **第 1 步 7 字段**：导入模块 / 官方来源名称 / 法规或 ATP 编号 / 来源发布日期 / 官方链接 / 备注 / **原始法规附件（PDF，只作追溯依据、不参与校验，与结构化文件分开保存）**。
- **第 3 步**：`模块下拉改变 → 后续每一步的示例文件名 / 模板列 / 数据条数 / 校验项 / 变更预览 / 发布落点必须同步变化`，**不得复用同一组假数据**。模板弹窗即「字段契约」（模板列与系统字段一一对应）。
- **第 4 步三子 Tab**：`数据校验`（8/8/10 类）· `规则测试`（**仅 Annex I 才有**，4 例）· `版本比较`（6/9/6 类变更）。
- **门禁**：第 3 步未上传文件 → 第 4 步禁用；第 4 步以二维表格展示校验问题，**存在阻断项 → 第 5 步禁用**（须在线下修正结构化表并上传修正版，系统复检通过后放行，不允许手工「标记已处理」）；第 5 步三项人工确认未全勾 → 发布被拦。
- **发布落库**：更新 `CLP_MODULES[key]` 的 ver / eff / cutoff / status → 数据落入对应 Tab → 在「版本变更与影响」`unshift` 一条带 `mod`（来源模块）的变更记录。

**H-3. 「规则引擎支持状态」挂在「计算方法」上，不挂在规则上**

口径：**一条规则可调用多个方法**，若逐规则存支持状态会出现「同一方法在两条规则里状态自相矛盾」，故以**方法为唯一口径**：`CLP_METHODS`（7 条：M-ATE-SUM / M-GCL-SUM / M-SCL / M-MFACTOR / M-LAYER / M-ED-PBT / M-BRIDGE），规则通过 `method` 引用后继承状态。三态取值：**已支持 / 需要配置参数 / 需要研发实现**。

**H-4. 「规则测试」与发布清单的硬关系**

- 测试结果三态：**通过 / 未通过 / 未执行**（筛选用）。
- **测试未通过的规则不进入本次发布清单**：`CLP_IMP_MODS.rules.excluded = ['CR-006']`，发布新增数 = 文件级新增数 − 排除数（演示：2 − 1 = 1）。校验文案与第 5 步「新增数量」都必须体现扣除。
- 规则落库按「**规则编号 upsert**」——库中已有同编号草稿时**更新状态与版本**，不存在时才新增，**避免同一编号出现两行**。

**H-5. Tab3 必须显式标注的三条口径（防误读）**

1. **字段适用性矩阵**：代码 / 标准文本 / 语言 —— **H、EUH、P 三类都适用**；GHS 图标代码 / 信号词 —— **仅 H 行适用**；标签组合关系 —— **H 行与 P 行适用，EUH 行不适用**。
2. **官方组合码数量口径**：**H 12 条 / P 31 条**，组合码本身即**单个标签要素**（`P301+P310` 整体算一条，不是把两条拼起来）；**EUH 没有官方组合码**。
3. ⚠️ **CLP 精确性说明**：严格按 CLP，**象形图与信号词是「危害类别 + 分类」的属性，不是 H 码本身的属性**——H 码看着像「自带」它们，只是因为 H 码与危害类别高度一一对应。本页按 H 行维护是**可接受的简化**，但必须在页面上如实标注，否则会被专业用户读错。

**H-6. ATP 版本口径（2026-09-18 核实，之前写错过）**

| ATP | 生效日期 |
|---|---|
| ATP 20 | 2025-02-01 |
| ATP 21 | 2025-09-01 |
| **ATP 22** | **2026-05-01（现行）** |
| ATP 23 | 待发布 —— **本原型的导入目标版本** |

首版原型曾写「ATP 21 = 2026-09-01」并当作现行版本，**已更正为 ATP 22 现行 / ATP 23 目标**。Tab1 的「生效版本」下拉改为**数据驱动**（`clpLViVers()` 从行数据里去重生成），发布新版本后自动出现、旧版本自动消失，不再硬编码。

**H-7. `23z6` / `23z6a` 的符号边界（零重复注册）**

- `23z6` 保留：数据层（`CLP_TOP` / `CLP_MODULES` / `CLP_VI_ROWS` / `CLP_RULES` / `CLP_METHODS` / `CLP_PICTO` / `CLP_LABELS` / `CLP_CHANGES`）、5 个 Tab 渲染与抽屉、`clpLGoTab` / `clpLFill`。
- `23z6a` 独占：**全部 `clpImp*` / `clpLImp*` 符号**（含 `clpLImport()` 入口与 `CLP_IMP_MODS` 三模块画像）。
- 页面头部「导入新版本」按钮仍 `onclick="clpLImport()"`，函数由紧随其后的 `23z6a` 提供，**同名符号零重复**（`check_names.py` 可验证）。
- ⚠️ 全站**只有一个模态宿主 `#modal`**，向导内打开「导入模板 / 影响范围」会覆写向导本体 → 两个二级弹窗的关闭按钮统一走 `clpImpRestore()` 重建向导当前步，**否则会出现「点一次模板说明，向导就没了」的演示级事故**。


### 3.11 设备维护台账口径（2026-09-15 拍板，不可推翻）

- **菜单不动**：设备资源下仍是 设备列表 / 设备能力 / 保养维护 / 零备件管理；
  「保养维护」页面本体改为 5 Tab（页面标题「设备维护台账」，菜单名与面包屑不变）。
- **Tab 顺序**：维护台账（默认）→ 保养要求 → 保养记录 → 校准记录 → 维保工单。
  ⚠️ 默认 Tab 的行点击 = 「登记保养」（停留本页），**只有「维保工单」Tab 的行才跳 `eq:maint-detail`**
  —— `smoke_detail_misc.py` 已按此改（先 `eqmTab='wo'` 再点行）。
- **设备两字段按位置推断**：检测中心 → 分析仪器 / 质管部；合成实验室与老化室 → 实验仪器 / 研发部。
  例外：EQ-2026-009 激光粒度仪在合成实验室，仍是分析仪器，但部门归研发部。
  字段由 `27z8` 的 `EQ_TYPE_DEPT` 映射补进 `EQUIPMENTS`（不在 `27-js-pages-b.js` 里写死）。
- **三表关系**：`EQ_PLANS`（保养要求，每台设备一份）→ `EQ_LOGS`（保养记录，编号 `BY-2026-NNN`）；
  `EQ_CALS`（校准记录，只挂分析仪器）。台账行 = 保养要求 + 最近一次保养记录 + 到期状态。
- **到期日 = 最近一次保养日期 + 周期**（周期按 天/周/月，月按 30 天折算）；
  无保养记录时起算 `plan.base`。状态灯：`left<0` 红逾期 / `left<=lead` 黄临期 / 其余绿正常。
- **首页联动**：`eqMaintTodos()` 返回「left ≤ lead」的行，`26z1` 里 `TODOS.concat(dyn)` 并入行动区；
  超期行 `urgent:true`（整行浅红 + 置顶）。行动区 7 天窗口只按 `due` 过滤，故 lead 过大的行
  （如 GC-MS lead=30）标黄但不会出现在首页——这是预期行为，不是 bug。
- **异常 → 转维修**：`EQ_LOGS` 结果选「异常」时异常说明必填；勾选转维修后
  `EQMAINTS.unshift()` 一条「故障维修 / 待执行」工单（编号 `MT-2026-NNN`）。
- 持久化键 `plm3_eqmaint_v1`，重置钩子 `_eqmReset()` 已挂 `23z1` 的 `wzReset()`。

### 3.10 领导评审第二轮口径（2026-09-10 拍板，2026-09-11 落地，不可推翻）

- **首页行动区**：欢迎语下方 KPI 上方置顶「📌 接下来要做」，浅蓝 accent 面板，近 7 天清单按截止日升序，每条 = 标题 + 来源标签 + 截止日 + 动作按钮直达；**原「我的待办」栏已删除并入行动区**，预警栏保留。渲染函数在新分片 `26z1-js-home-actions.js`（`homeActionArea()`），TODOS 数据在 `26-js-pages-a.js`，字段：`source / act / go / goId`。
- **任务来源两分**：`OA 项目计划`（蓝色标签，只读接入的里程碑节点）/ `本系统任务`（绿色标签，实验/检测/合规等系统内闭环），文案全中文。
- **推广 = 项目第七阶段**：阶段序列 `['评审','预研','小试','中试','试生产','交付','推广']`；`prod:promo` 菜单已删（PROMOS 数据保留，供项目详情取数）；推广阶段表单复用 `PROJ_GATE_GROUPS` promote 子结构 + 目标客户/渠道/推进阶段字段；**PRJ-2026-005 已进入推广阶段**（交付 done、SDS V2.1 已归档的叙事）。
- **实验设计「问问 AI」已取消**：`openExperimentAI/adopt/close/copy` 四函数已删，`aiSparkIcon()` 保留供后续 AI 功能复用，不留隐藏入口。
- **周报月报菜单已删**：查看入口 = 我的文档 →「我的周报月报」文件夹（F-M1）；`doc:weekly` 页保留为重定向兼容旧链接；归档筛选保留 类型/状态/月份（weekly 行有 `month` 字段）。
- **保证函管理**：合规管理下 P4 占位页（`23z5-js-guarantee.js`，`comp:guarantee`），数据来源口径 = 外检记录 + 法规库命中，3 条示意 mock。
- 专项自检 `smoke_leader_review.py`（40 断言）覆盖以上全部。

---

## 四、代码组织速查

```
项目根目录/
├── PLM3.0全系统演示原型.html   ← 交付产物（构建生成，勿手改）
├── build.sh                    ← 拼接 src/ → 单文件
├── run_smoke.sh                ← 自检并行聚合（xargs -P4，按退出码判定）
├── 续接指南.md                  ← 本文
├── 进度交接.md                  ← 详细技术档案（1000+ 行）
├── SDS缺失项清单.md             ← SDS 对照真实文档的差距分析（做 SDS/合规 PRD 时读）
├── src/                        ← 源码分片（**45 js + 16 css + 2 html = 63**），按文件名字典序拼接
│   ├── 00-head.html / 10-shell.html
│   ├── 01~04-css-*.css          ← 样式（04 是 SDS 专用）
│   ├── 20-js-core.js            ← $ / $$ / esc / toast / modal / confirmBox / tabs / renderListPage
│   ├── 23z6-js-clp.js          ← CLP 法规库统一页面（主信息 + 5 Tab + 抽屉 + Annex V 图式块 / 素材管理；接管 law:clp）
│   ├── 23z6a-js-clp-import.js  ← CLP「导入新版本」五步向导（三模块分支；**独占全部 clpLImp\* 符号**，紧随 23z6 加载）
│   ├── 23z7-js-reach.js        ← REACH 法规库统一页面（主信息 + 6 Tab + 抽屉 + 五步向导；接管 law:reach）
│   ├── 23z8-js-law-misc.js     ← RoHS 限用物质页（law:rohs，10 条全列）+ 运输占位页（law:trans）；OEL 占位已删除并移交 23z9
│   ├── 23z9-js-oel.js          ← 职业接触限值（OEL）法规库（来源只含欧盟 + 中国；四 Tab：数据来源 / 数据集版本 / 限值明细 / 版本对比与审核 + 限值类型语义槽位对齐 + 7 步导入向导 + 7 类校验；接管 law:oel）
│   ├── 23z5-js-guarantee.js     ← 保证函管理占位页（P4）
│   ├── 26z1-js-home-actions.js  ← 首页置顶行动区（接下来要做）
│   ├── 21-js-data.js            ← 全部数据层 ⭐ 新增数据放这里
│   ├── 22-js-router.js          ← MENU 菜单树（支持三级）/ HL_MAP 高亮锁定 / PAGES 注册表
│   ├── 22z-js-flow-guide.js     ← 流程引导页（proj:guide / exp:guide / comp:guide）⭐ 画流程图的一手来源
│   ├── 22z1-js-rawmat.js        ← 原料打通（批次/供应商/溯源）
│   ├── 22z2-js-bd-extend.js     ← 基础数据三子页（techcat / formula / exptpl）
│   ├── 22z3-js-rawmat-test.js   ← 批次测试记录录入（modal/校验/持久化/重置钩子）
│   ├── 23*-js-sds*.js           ← SDS 模块（最大，含 23z1 持久化 / 23z3-4 章节 / 23y 法规统一查询）
│   ├── 24-js-doe.js             ← DOE 主线（4 步向导）
│   ├── 24z1-js-exp-restructure.js  ← 实验管理三页重构
│   ├── 24z2-js-exp-summary.js      ← 实验分析与总结
│   ├── 24z3-js-weekly-ai.js        ← 周报月报 + 实验 AI 问答
│   ├── 24z-js-exp-tree.js       ← 实验列表双树
│   ├── 25-js-charts.js / 26,27-js-pages-*.js
│   ├── 27z-js-doc-folder.js     ← 文档管理两级文件夹 + 周报归档（覆盖 24z3 的同名页）
│   ├── 28-js-boot.js            ← 启动序列（**绝不可再注册 home**）
│   ├── 27z5-js-phys-range.js    ← 产品理化指标范围值展示 + 超范围标红
│   ├── 27z6-js-tmp-task.js      ← 临时任务（列表 / 新建 / 接收 / 反馈 / 首页联动）
│   ├── 27z7-js-phys-table.js    ← 理化性质分组表格（方案 A：包装 prod:detail，卡片→表格）
│   ├── 27z7-js-report.js        ← 报表管理占位页（rpt:home，BI 接入前空态）
│   ├── 27z8-js-eq-maint.js      ← 设备维护台账（三表 + 5 Tab + 状态灯 + 首页联动）
│   ├── 27z9-js-navtools.js      ← 侧栏「收起/展开全部菜单」工具条（包装 renderSidebar/highlightSidebar，零侵入）
│   ├── 24z5-js-exp-analysis2.js ← 实验分析两步选择 + 统计工具（覆盖 24z4 的两个入口，属已知有意）
│   ├── 24z6-js-exp-form-guard.js← 新建实验「实验目的」≥5 字校验（包装 24z1）
│   └── 06z3-css-clp.css       ← CLP 信息条 .clp-strip + 抽屉补充 + .clp-page .notice b 句内加粗
│   └── 06z3a-css-clp-import.css ← CLP 导入向导专用（.clpimp-seg 分段控件 / .cip-check-table 校验二维表 / .cip-filemeta /
│                                  .cip-row-bad / .mini-steps.mini-5 / .picto-grid 图式格 / .oel-old·.oel-new 新旧值）
│   └── 06z4-css-reach.css       ← 法规页（REACH / RoHS / 运输）信息条 .law-strip + 规则卡片 + 运输方式卡片
│   └── 06z5-css-oel.css         ← OEL 专用（五步流程条 / 变更类型计数条 / 校验卡片 / 双栏上传 / 抽屉）
└── smoke_*.py                   ← **39 个**自检脚本
```

### ⚠️ 分片命名与 locale 排序（本轮新增的一手经验）

`build.sh` 的 `for f in "$SRC"/2*.js` 在 **C.UTF-8 字典序**下展开，规则是**逐字符比较**，不是按数字大小：

| 名字 | 实际排序结果 |
|---|---|
| `23z10-*.js` | ❌ 排在 `23z1` 与 `23z2` **之间**（`1` < `0`？不——比的是第 5 位 `z` 之后的 `1` vs `0`，`0` < `1`，所以 `23z10` 反而在 `23z1` **前面**） |
| `23z6a-*.js` | ✅ 排在 `23z6-js-clp.js` 之后、`23z7-js-reach.js` 之前 |

**结论**：要在 `23zN` 与 `23zN+1` 之间插分片，用 `23zN` + **小写字母后缀**（`a`/`b`…），**不要用 `23zN0` 这类数字后缀**——数字后缀会跑到 `23zN` 前面去。本轮已实测验证（先建空文件排次序，再写内容）。

### 铁律

1. **新代码进新分片，旧文件只加最小钩子**——避免大改已有文件引发回归
2. **分片命名注意 locale 排序**：`build.sh` 按文件名字典序拼接，而排序会忽略连字符。历史教训：`23b-` 被排到 `23-` 前面，被迫改名 `23z-`。**新增分片用 `NNz` 后缀且放在最后**
3. **改完必须 `bash build.sh`**，产物是拼接出来的，手改 HTML 会被覆盖
4. **改 JS 分片后必跑 `node --check <分片>`**：`build.sh` 只数 `<script>` 标签配平，**不检查 JS 语法**。语法错要到浏览器 `pageerror` 才炸（第三十一轮 `CLP_RULES` 数组被提前闭合、`CR-006/007` 变游离语句，就是这么暴露的）
5. **模态里禁止直接 `openModal()` 二级弹窗**：全站只有一个 `#modal` 宿主，二级弹窗打开是**覆写**而非叠加。二级弹窗的关闭按钮必须走**重建函数**（CLP `clpImpRestore()` / REACH `rchImpRestore()`），**不能 `closeModal()`**——否则「点一次影响范围，向导就没了」

### 自检体系

**39 个** Playwright Python 脚本（实际断言数远超 900，含 `smoke_rawmat` / `smoke_bdext` / `smoke_docfolder`）。跑法——用并行聚合脚本 `run_smoke.sh`：

```bash
bash run_smoke.sh                # 全量 39 个，并行 -P4
bash run_smoke.sh rawmat         # 只跑 smoke_rawmat.py
bash run_smoke.sh sds doe        # 按关键字匹配多个
```

> ⚠️ **必须前台运行**：后台 Bash 会被沙箱拦截 Chrome 写临时文件，结果空白。
> 浏览器用本机 Google Chrome for Testing（chromium-1223 / Playwright 1.60 对应），各脚本已注入 `_CHROME_PATH`；screenshot 落 `/tmp`。

| 脚本 | 覆盖 |
|---|---|
| `smoke_all.py` | 全菜单遍历 |
| `smoke_pages.py` | 全注册表渲染 + 侧栏高亮锁定 |
| `smoke_exp_summary.py` | 实验分析与总结 **82/82** |
| `smoke_exp_restructure.py` | 实验管理三页重构 |
| `smoke_exp_tree.py` | 实验列表双树联动 |
| `smoke_navtools.py` | 侧栏一键收起/展开全部菜单 **21/21**（含持久化、刷新保持、单组折叠联动） |
| `smoke_clp.py` | CLP 法规库统一页面 **209/0**（主信息 / 折叠页面说明 / 5 Tab / 跳转链 / 抽屉 / Annex V 象形图素材管理 / 三模块导入向导 / 第 4 步子页签选中态 / 校验二维表 / 需研发实现示例 / 问题明细 CSV / 修正版复检门禁 / 规则测试与发布 / 无「自动解析」表述） |
| `smoke_reach.py` | REACH 法规库统一页面 + 合规管理菜单调整 **160/0**（菜单 6 入口 / 外部参考数据 / 受限物质管理分组、顶部主信息、6 Tab、各 Tab 数据版本信息条、**信息栏宽字段网格不错位**、抽屉、lawKey 跳转、五步向导、RoHS 10 条、OEL 入口（已非占位）、**向导二级弹窗「返回向导」不顶掉向导**、表述红线） |
| `smoke_oel.py` | 职业接触限值（OEL）法规库 **210/0**（顶部 4 KPI / 五步流程条 / 边界表述、来源只含欧盟与中国两套 + 新建来源、Tab2 六个版本状态与提交发布 KPI 联动、Tab3 20 列含「语义槽位」+ 槽位筛选 + 类型字典抽屉 + 甲苯跨 3 版本并存 + 同 CAS 抽屉、Tab4 六类变更 + 会话切换 + 退回/通过、7 步向导（含上传门槛、7 类校验阻断门槛、标记已处理后放行）、提交后数据集 6→7、表述红线（不得再出现德/英/法/西）） |
| `smoke_eqmaint.py` | 设备维护台账（设备列表两列两筛 / 5 Tab / 台账排序与状态灯 / 三表列 / 异常转维修 / 持久化 / 首页联动 / 重置）。⚠️ 状态灯台数随运行日浮动，断言只校验「与规则一致」，不写死台数 |
| `smoke_round26.py` | 第二十六轮四项：临时任务全流程 / 两步分析 + 统计工具 / 实验目的校验 / 理化范围值 |
| `smoke_exp_ai.py` | 实验分析 AI（24z5 两步选择 + `anaGen()` + 三段图表表格 + 保存 / 周报联动 / 持久化）**55/55** |
| 其余 | DOE / 项目 / SDS / 持久化 / G 系列 / 交付 / 导出 / 说明折叠 |

**断言用 `textContent` 而非 `innerText`**——acc 手风琴折叠时 `innerText` 为空。

---

## 五、协作偏好（用户明确要求 ⚠️）

1. **不要为了展示而截图**——改完 build + 自检通过，直接交产物给用户体验。截图、效果对比图是浪费时间。
2. **执行要节约时间**——自检脚本跑一次就够，**不要重复跑、不要重复验证产物内容**。
3. **交付只传产物本身**——`present_files` 只传 HTML，不要附带一堆截图文件。
4. **工作目录保持干净**——临时文件用完即删，不要留在 `/workspace`。
5. **聚焦核心功能点**——不铺摊子，一个模块做深再做下一个。
6. **先讨论清楚再动手**——用户提出新需求时倾向于先对齐定义再实现（本轮实践效果很好）。

---

## 六、最容易重犯的坑

| 坑 | 说明 |
|---|---|
| 三元表达式与字符串拼接优先级冲突 | `src/24-js-doe.js` 曾 `Unexpected token ')'`。抽出独立函数返回字符串解决 |
| 硬编码数量断言 | 实验种子一变就崩。**用动态断言**（`=== analysisTargets().length`） |
| `exp:best` 无 params 时渲染空页 | 方案 id 不在 `experiments`。改为优先取 `window._curReportObj`，逐级回落 |
| 表格 `white-space:nowrap` 导致逐字断行 | 「实验工艺」列曾被压成每字一行。需显式 `white-space:normal` + 给列最小宽度 |
| `wzReset()` 依赖 SDS 向导 DOM | 非 SDS 页调用报 null。必须用 `try{...}catch(e){}` 包裹 |
| Node 版 Playwright 缺依赖 | 一律用 Python 版，与其余脚本一致 |
| **行点击抢走行内链接** | `renderListPage` 的行点击会与行内 `<a>`/`.btn-link` 叠加，后触发者胜出（供应商页「供应物料」链曾被跳去供应商详情）。已在 `20-js-core.js` 行点击里判断 `ev.target.closest('a,button')` 并放行 |
| **smoke 失败却返回 0（假全绿）** | 15 个脚本只打印失败数不设退出码，`run_smoke.sh` 依据 rc 判定 → 失败被吞。每个脚本结尾必须 `raise SystemExit(1)` / `sys.exit(1 if FAIL else 0)`；同时暴露了 smoke_proj 四条陈旧断言（首页「我的待办」已并入「接下来要做」、项目阶段 6→7 含推广） |
| **周报「自动 vs 人工」必须分清** | 2026-09-14 定为口径：编辑态只渲染系统有数据源的栏目，人工项留空 + 「一键填充示例数据」按钮；只读态（历史周报）才全量填充。**不要把编的系数（如工时 n×3）或硬编码示例直接写进编辑态**，演示时会被当成"系统算出来的" |
| **`wkReportHTML_RO` 曾是与编辑态并列的简化实现** | 只读渲染原本是另一份精简代码（其他工作写死、无文献表、无完整章节），两边改一处漏一处。已改为 `wkReportHTML_RO_Inner` 委托 `wkReportHTMLInner(true, per)`，**只读与编辑共用同一模板**，不要再各写一份 |
| **`ensureNormalDetail()` 改写 experimentDate** | 它有副作用：`expDate()` 在无 `analyzedAt` 时回退 `dueDate`，把日期甩出周报周期（研发版查到 4 组、切经理版只剩 2 组）。**新增实验一律显式写 `experimentDate`** —— `normalDate()` 对已有值直接返回，不再推导 |
| **Edit 工具静默失败** | 同一批多锚点替换里，部分会「报成功但不落盘」。批量接线后必须 `rg -n "<新符号>"` 复核命中行数；不稳定时改用 Python 精确替换并 `assert count==1` |
| **模态内再 `openModal()` 会覆写模态本体** | 全站只有一个 `#modal` 宿主，二级弹窗打开就是「覆写」而非「叠加」。向导/弹窗里的二级弹窗，其「关闭」按钮必须走**重建函数**（CLP 用 `clpImpRestore()`、REACH 用 `rchImpRestore()`），**不能 `closeModal()`**，否则「点一次影响范围，向导就没了」。2026-09-20 已把全项目此类问题清干净（复核方式见 §十四） |
| **核查字段只看顶层 key 会误报「缺字段」** | 2026-09-21 核合同时犯过：用 `Object.keys(CLP_RULES[0])` 只有 17 个 key 就判「规则表缺 8 个字段」，实际这些字段**全在嵌套的 `det` 对象里**（`det.inputs/cond/formula/except/prio/output/label/src`）。**判「字段缺失」前必须同时查三处：数据对象（含嵌套）／渲染列／导入模板 `fields`**，单点采样会得出假结论 |
| **「页面承诺」必须和「实际执行」成对验证** | 第三十八轮抓到：第 3 步 `wzBlockers()` 写着「第 4 步会落到待人工判断」，第 4 步 `clpEvaluateMixture()` 却照算不误标 `auto` —— 同一条数据两处说法相反。**凡是某处 UI 向用户承诺了后果，就必须有一处代码真正执行它**，两者不在同一文件也要 grep 配对。修法是加「输入可靠性门禁」：`var aqBad=aqRows.filter(function(r){return r.p.aqState!=='known';});`，非空则不产出结论 |
| **辅助函数可能是局部的** | `casName(cas)` 看着像全局，实际定义在 `renderStep3()` 内部（缩进 2 空格），跨函数调用直接 `ReferenceError`。**调用陌生函数前先 `grep "function 名字"` 看缩进层级**；拿不准就用内联表达式从 `wz.formula` / `COMP_CLP` 取 |
| **领域术语不能望文生义** | 「缺 M 因子」这个说法第三十八轮前一直在用，但按 CLP Annex I **表 4.1.3**，M 因子只适用于 Acute 1 / Chronic 1 —— 甲醛 Chronic 2、乙二醇单丁醚 Chronic 3 按定义就不适用。写判定 / 告警文案前先确认规则适用范围，否则内行一看就穿帮 |
| **`clpCalcInput()` 的 label 回调只传元素、不传下标** | 第三十九轮踩到：`clpCalcInput(rows, function(r,i){...})` 里的 `i` 恒为 `undefined`（内部是 `rows.map(function(r){... label(r)})`），按 `rows[i]` 取值直接 `Cannot read properties of undefined`，且报错点落在 `buildClassItems()` 里，看栈很难定位到引擎。**复用老工具函数前先看它的回调签名**，需要下标就把值预置到行对象上（如 `tag` 字段） |
| **重构前先存一份「旧行为快照」，改完再逐项 diff** | 第三十九轮的做法：`git show HEAD:<产物> > /tmp/old.html`，用 Playwright 在两个产物上跑同一段 JS，比对 `items` 的 id/result/code/status/method/input/formula/opts/src + `labels` + 顺序。**「看起来没变」不算证据，逐字段 diff 才算**；这个方法也顺带证明了本次重构零行为变化 |

| **⚠️ `CLP_RULES` 已是「活动版本投影」，不是事实源** | 第四十轮起：改 `CLP_RULES` 不会生效（下一次 `clpSyncActiveRulesProjection()` 会把它整个覆盖掉）。**要改规则就改活动版本的 `rules` 再同步**：`var v=clpRuleVersionResolve(); v.rules[…].… = …; clpSyncActiveRulesProjection();`。`smoke_compliance_engine.py` 的两条断言曾因此失败（改投影后 `clpActivePack()` 读的仍是版本库） |
| **测试夹具要保存「未污染的初始规则集」** | 生命周期测试会发布版本、停用规则，污染 `CLP_RULES`；后续分组再 `reset()` 时基线是从被污染的 `CLP_RULES` 重建的，导致候选停用识别不出来。夹具里先 `window.PRISTINE_RULES=深拷贝(CLP_RULES)`，reset 时一并还原 |
| **版本级校验会拦「生效日期 < 数据截止日期」** | 造「当天生效」的测试版本时若沿用默认的 cutoff（2026-09-15），eff 必须 ≥ cutoff，否则 `clpRuleVersionValidate` 直接报错、发布失败且原因不显眼 |
| **连发两个草稿版本会出现「无可发布变化」** | `clpImpDemoDraftRows()` 基于**基线**（不是活动版本）生成候选行，所以第一个版本发布后再发第二个，所有变化都已落地 → publishable 0 → 系统拒发。这是**正确行为**；写测试时要给第二版额外施加一处新变化 |
| **批量改动别「边遍历边 splice」** | `clpRuleVersionPublish()` 原按 `idx[id]` 逐个 `splice`，删掉一条后后面元素下标全部前移、`idx` 却不重建 → 一次停用 2 条以上必然出错：实测停 `[R-0004,R-0005]` 时 **R-0005 漏删**；停 `[R-0002,R-0004]` 时 **误删了无关的 R-0005**。改为「先收集待删 id 集合 → 一次 `filter`」。凡是「按下标批量删」都要警惕这一条 |
| **「系统日期」和「业务上下文日期」不能共用一个取值函数** | 第四十一轮前 `clpRuleAsOfDate()` 优先返回 `wz.project.date`（SDS 的**计划投放日期**），却同时被用于「发布时判定已生效 / 待生效」。把 SDS 投放日填成未来（页面默认 `2026-10-01`，本就比系统日 `2026-09-23` 晚 8 天），再回法规库发布 `effectiveFrom=2026-10-01` 的版本，就会被判**已生效并顶掉当前活动版本**。现已拆成 `clpSystemToday()`（只走 `demoYmd()`，专供系统行为），SDS 要按投放日匹配规则版本时**由调用方显式传参**。**见到「取今天」的函数，先问它代表谁的今天** |
| **夹具把要测的东西清掉了 → 假全绿** | `smoke_clp_lifecycle.py` 的 `LF.reset()` 原来只 `seedBaseline()`、不 `seedCandidate()`，于是「导入草稿覆盖候选草稿」这个 bug 在 41/41 全绿里藏了一整轮。补测试前**先让夹具还原真实加载态**（新增 `LF.resetReal()` = 基线 + 候选草稿共存），否则新断言照样测不出来 |

---

## 七、当前主线：PRD 输出（2026-09-09 起）

**原型迭代已收口，工作重心转为按模块产出 PRD。** 新会话默认不再新增原型页面——除非用户明确要求改原型。

### 7.1 PRD 的一手素材在哪（不要凭印象写，先读这些）

| 素材 | 位置 | 提供什么 |
|---|---|---|
| 模块真实流程 | `src/22z-js-flow-guide.js` | exp / proj / comp 三个流程引导页，是流程章节最可靠的骨架 |
| 页面与菜单 | `src/22-js-router.js` | MENU 树（**69 页 / 50 叶子项 / 11 个一级菜单**，2026-09-21 核），PRD 的页面清单来源 |
| 数据模型 | `src/21-js-data.js` | DB_CFG / doeSchemes / experiments / expSummaries / EXP_RECIPE 等 |
| 合规细节 | `src/23-js-sds.js` | SDS 六步 `WZ_STEPS`、取数优先级（677 行）、证据灯四档 `SDS_EV_TXT` |
| 差距分析 | `SDS缺失项清单.md` | SDS 对照真实 CIRS 欧盟 CLP 文档的 P0/P1/P2 分级 |
| 实现细节与踩坑 | `进度交接.md` | 每轮决策与教训，按需查阅 |

### 7.2 需求与排期基线（写 PRD 前先对齐，口径有出入）

| 优先级 | 人天 | 内容 |
|---|---|---|
| P0 | 51 | 菜单结构重构、工作台、基础数据增强、文档中心版本管理与关联关系 |
| P1 | 118 | SDS 选料查询/法规库/模板生成、AI 问答与图表、DOE 设计向导/计划/ANOVA/最优推荐、BOM 配方转 BOM 与版本对比 |
| P2 | 64 | 质量校验与留痕、变更管理 ECR/ECO、知识库、合规库扩展、模板管理、全文检索 |

- 需求清单合计 **25 项 / 233 人天**。
- 里程碑：M1 设计文档（收尾）→ M2 原型试用与排期评审 → M3 P0 上线（8/15–9/19）→ M4 P1 上线（9/19–10/31）→ M5 P2（11 月）。
- ⚠️ **未解决**：需求清单 233d 与里程碑 238d 对不上，P0/P1 工时口径不一致。排期暂以里程碑为准，**写 PRD 时须向用户确认差额**。

### 7.3 历史遗留待办（暂缓，用户挑）
- **原料详情页供应商信息去重（2026-09-15 提出，暂缓）**：基本信息「主供应商」与「供应商与货号」卡片同源于 `MAT_SUP[code][0]`，一物一供口径下 100% 冗余（且卡片副标题「一物一供 · 唯一供应商」与表格「主供」标签自相矛盾）。拟定方案：删基本信息那行 + 供应商卡片上移到基本信息之后 + 去掉「主供」标签；列表页 / SDS 选料的「主供应商」列标题是否一并改「供应商」待定。**等 Cayla 与业务对齐原料信息表字段后一并推进**
- ~~给 S4 的 12 个轻量列表页补详情页~~ → **已完成（2026-09-11 第二十一轮）**。`prod:promo`（产品推广计划）随「推广并为项目第七阶段」退出菜单，**页面本体已于 2026-09-14 第二十三轮删除**；`PROMOS` 数据保留，仍作为项目详情推广表单「目标客户/推广渠道/推进阶段」的回退源
- **产品目录树（2026-09-14 第二十三轮，已落地）**：数据来自 `产品目录树.xlsx`（9月10日沟通稿）
  - `PROD_CATALOG`（`src/27z5-js-prod-catalog.js`）：3 大类 / 28 一级 / **144 个真实二级**
    （xlsx 二级列共 145 行，其中「复鞣剂」下一格是末级占位「/」，不计）
  - 选的是**方案 A**：目录树挂在产品基础信息列表页左侧做筛选，**不单独开菜单**；目前为只读字典（无增删改）
  - 产品挂 `catPath`（`大类/一级[/二级]`），原则：**只对得准的才挂二级**，其余挂一级
  - ⏳ **待 Cayla 开会确认**：蒙囿剂 MK-2（树中无对应，现进「未归类」）、WPU-320 / SB-11（涂饰树脂只见二级暂挂一级）、
    NS-10（合成革树脂挂到「聚氨酯树脂/合成革聚氨酯」，待确认）
- **理化性质区改版（同轮，已落地）**：取消「标准值 / 实测值」二分 → 每个指标只有一个**指标值**；
  明细区由三列表格改为**两列卡片网格**；来源留痕去掉彩色 chip 改为灰色小字；详情「基本信息」新增「所属目录」
- **页面清理守卫（2026-09-14 第二十三轮）**：新增 `check_pages.py`（菜单/页面一致性体检，已挂 `run_smoke.sh` 前置，与 `check_names.py` 并列）。**当前结论（2026-09-21 第三十五轮复核）**：
  - 菜单指向空页：**无**
  - 孤儿页：仅 `doc:weekly`（**有意保留**的重定向，代码里另有字面量引用）
  - 不在菜单但有入口（详情/向导类，正常）：**12 个**
  - 同名重复注册 **4 处**（较第三十二轮的 5 处少 1）：旧文档中心 `doc` / `doc:public` / `doc:mine`（27 → 被 27z 新版覆盖）、`eq:maint`（27 → 被 **27z8 设备维护台账**覆盖，原列表保留为「维保工单」Tab）——均为新版替代旧版，属已知有意
  - ℹ️ `law:zdhc` 的重复注册**已于第三十五轮消除**：删除 `23-js-sds.js` 的 `regPage('law:zdhc')`，只保留 `23y` 的 `regLawMaintenance('law:zdhc')`（这也是已注册页面 70 → **69** 的原因）
  - ℹ️ 上一版这里写的「孤儿页 `law:rohs`」**已过时**：第二十九轮菜单重构后 `law:rohs` 成为「受限物质管理 · RoHS 限用物质」的正式菜单项，不再是孤儿页
- ~~**推广阶段口径待拍板（W1/W2/W3）**~~ → **已拍板 W1（2026-09-14 第二十三轮）**：不新增客户×产品台账，只对齐推广阶段字段
  - 真实 8 字段：**产品验证是否完成**｜**产品性能评审是否完成**｜**技术与成本评审是否完成**｜技术及成本评审附件｜产品试推广计划完成时间｜产品试推广实际完成时间｜**放大生产是否合格**｜**产品正式推广是否发起**
  - 其中 **1/2/3/7/8 共 5 项取值为「是/否」**，4 为附件，5/6 为时间
  - 落地：`PROMO_FIELDS`（8 项定义）驱动项目详情推广阶段表单；`PROJ_GATE_GROUPS` 推广阶段列名对齐 6 个子阶段（产品验证｜产品性能评审｜技术与成本评审｜产品试推广｜放大生产｜产品正式推广）；矩阵单元格与 `promoGates` 同步改「是/否」口径
  - 演示数据 PRJ-2026-005：验证=是 / 性能评审=是 / 技术与成本评审=是 / 试推广计划=2026-09-16（实际空）/ 放大生产=否 / 正式推广=否
- 4 个 toast 占位按钮改成真实改数据（项目详情：返回/更换负责人/修改名称/调整部门）
- 产品目录若要开放维护（增删改/启停/编码），需从方案 A 升级到方案 C（独立维护页 + 列表页只读树）
- 详情页的「原料添加/过程检测/成品检测」目前只读，可加行内编辑
- DOE 方案详情页加「基于此方案生成对比总结」快捷入口
- **A9**（待改版 diff 对比）——用户明确「先不做」
- **SDS 剩余 G 项**（G11 / G14 / G15 / G16 / G17）——用户明确「其余 G 类任务不影响演示功能，先放置」。明细见 `SDS缺失项清单.md` 第五节

### 7.4 PRD 产出进度（每完成一篇，更新此表）

| # | 模块 | PRD 状态 | 代码素材完备度 | 备注 |
|---|---|---|---|---|
| 1 | 合规管理（SDS） | ⬜ 待写 | ★★★★☆ | 六步管道、取数优先级、证据灯四档均已核实；2026-09-09 已产出流程图 |
| 2 | 实验管理 | ⬜ 待写 | ★★★★★ | 三条并行路径、DOE 下发边界已核实并画图 |
| 3 | 文档管理 | ⬜ 待写 | ★★★★☆ | 两级文件夹 + 周报四维筛选；属 P0 |
| 4 | 基础数据 | ⬜ 待写 | ★★★★☆ | 三级菜单 + 三子页（技术分类/公式/实验模板）；属 P0 |
| 5 | 项目管理 | ⬜ 待写 | ★★★★☆ | 6 Tab + 阶段导航 + 多对多关联 |
| 6 | 产品管理 | ⬜ 待写 | ★★☆☆☆ | 仅列表页，需补业务调研 |
| 7 | 需求管理 | ⬜ 待写 | ★★☆☆☆ | 仅列表页，需补业务调研 |
| 8 | 质量/检测管理 | ⬜ 待写 | ★★☆☆☆ | 仅列表页；是实验「过程测试/成品检测」的业务来源 |
| 9 | 设备资源 | ⬜ 待写 | ★★☆☆☆ | 仅列表页 |
| 10 | 知识产权 | ⬜ 待写 | ★★☆☆☆ | 仅列表页 |

**建议顺序**：合规 → 实验 → 文档 → 基础数据。前两个代码最完备且已画好流程图，能快速定下 PRD 模板；后两个在 P0 范围内，排期上优先。

---

## 八、新会话启动指令（PRD 阶段）

新窗口直接复制这段话：

> 读 `项目根目录/续接指南.md`（详细细节按需查 `进度交接.md`），**当前主线是产出 PRD，不是继续改原型**。
> 先确认本次要写哪个模块的 PRD，再按 EARS 原则输出：背景、目标、用户故事、功能清单、流程说明、交互说明、数据指标、验收标准。
> 写之前读对应代码（`src/22z-js-flow-guide.js` 取流程骨架，`src/21-js-data.js` 取数据模型），不要凭印象。

读完会自动追平进度，然后**等用户明确本次写哪个模块再动手**。

> 📌 若本次仍要改原型：产物全量回归 **39/39** 通过，改完必须 `bash build.sh` 并跑对应 `smoke_*.py`。

---

## 九、第二十八轮收口 · 外部复核修订（2026-09-18）

外部复核提出 2 样式缺陷 + 3 内容问题 + 2 顺带项，已全部落地：

| # | 事项 | 修法 | 落点 |
|---|---|---|---|
| 1 | 弹窗步骤条竖排（CLP 向导 + SDS 批量导入 + 法规库上传） | 根因是 5 处 `openModal` 未带 `sds-scope`；在调用点补 `cls:'sds-scope'`（CLP 三处再加 `clp-page`），未动 openModal 本体 | 23-js-sds.js dbImport/lawUpload；23z6-js-clp.js 三处 |
| 2 | 「含」字残片 | 实为 `.drop .ic` 的 ⇪ 图标失去样式后裸排成 13px 小图形，看着像残字，与 #1 同源同修 | 同上 |
| 3 | 通知块 `<b>` 被强制换行 | 新增 `.clp-page .notice b{display:inline}`（06z3 在 04 之后加载，同特异性后到者胜）；CLP 页根与抽屉加 `clp-page` | 06z3-css-clp.css / 23z6 |
| 4 | 规则来源页码未核对 | 全部改「条款号 + 章节名」，删 PDF 页码；**水生加和法 4.1.3.1.5 → 4.1.3.5（慢性加和公式见表 4.1.4）**，已经官方译文核实；抽屉字段改「来源条款与章节」 | 23z6 CR-001..005 |
| 5 | Tab5 标注矛盾 | `clpLIsNum()`：数字才挂「官方清单 / 示例」；「待评估」「—」不挂；「全部（标签模板）」改「—」 | 23z6 |
| 6 | 注释署名 | clp.js 第 4 行 Cayla → 王舒；24z3-js-weekly-ai.js:375 的「Cayla 定」指用户本人拍板、语义正确，保留不改 | 23z6 |
| 7 | 顺带：Tab1 Index No. 折行 | 列宽 110 → 130 | 23z6 |
| 8 | 顺带：面包屑 `<b>` 原文 | 渲染器本就给末级自动加粗+转义，批量清掉 52 处 crumb 里的手写 `<b>`（13 个文件） | src/*.js |

**回归**：37 个 smoke 全绿（smoke_clp 82/82）；构建 42:42 配平。
**遗留提示**：`23-js-sds.js:901` fmImportExcel（Excel 批量导入配方弹窗）仍是裸 openModal，内部 notice 走全局样式暂无肉眼问题，属同类隐患供排期。

## 十、并行写盘排查结论（2026-09-18 14:00）
- **并行写 src 元凶 = Codex（ChatGPT.app）**：用户把 PLM 项目也交给了 Codex 并行「接手重构」（`.codex-global-state.json` 含 PLM 路径 + 指令「wb写了一半的，你看看你能不能接上？」）。Codex 经 `codex-code-mode-host` / `unified-computer-use` / Telegram 桥 `telegram_codex_bot.py` 拥有文件写权限——上一轮 `23z6-js-clp.js` 两次被回退即源于此。
- **定时 auto-commit 钩子**：仓库存在 `auto: 2026-09-18 12:00 / 14:00` 类提交（无 git hook、无 launchd、无 cron），来源为 Codex agent 模式自带按节奏自动提交。它不写 src，但会扫工作树提交，会抢先 WorkBuddy 的显式 commit（本次已 amend 为描述性消息 `6425788` 固化）。
- **协作硬规则（务必遵守）**：同一 PLM 项目不要同时让 WorkBuddy 与 Codex 改同一批文件。① 同一文件同一时段只由一个 agent 改；② 或让 Codex 走独立 branch/clone，再有意 merge；③ WorkBuddy 大改前先确认 Codex 当前没有 PLM 任务在跑。

## 十一、第二十九轮收口 · 合规管理菜单重构 + REACH 法规库（2026-09-18）

**范围（用户明确限定）**：只调整①合规管理菜单结构、②REACH 法规库页面。**不动已完成的 CLP 法规库页、不动 SDS 生成向导；不实现真实后端 / 文件解析 / AI / 法规联网监控。**

**交付**

| # | 事项 | 落点 |
|---|---|---|
| 1 | 菜单重构：法规库维护 6 入口、新增「外部参考数据」分组、受限物质管理改分组、两处更名 | `22-js-router.js` |
| 2 | REACH 法规库统一页面（顶部主信息 + 6 Tab + 详情抽屉 + 五步导入向导） | **新分片** `23z7-js-reach.js` |
| 3 | RoHS 限用物质页（10 条全列）+ OEL / 运输两个占位页 | **新分片** `23z8-js-law-misc.js` |
| 4 | 法规页样式（`.law-strip` 信息条 / PBT 规则卡片 / 运输方式卡片） | **新分片** `06z4-css-reach.css`（已在 build.sh 登记） |
| 5 | 删 `law:reach` 旧注册（23y）、删 `law:rohs` 旧隐藏页注册（23-js-sds.js），均改注释 | `23y` / `23-js-sds.js` |
| 6 | 法规统一查询：来源筛选「REACH / RoHS」→「REACH」、新增「法规类别」lawKey 筛选、补 3 条示例命中（15 → 18） | `23y-js-law-query.js` |
| 7 | 专项自检 + 过时断言同步 | 新增 `smoke_reach.py`（152 断言）；同步 `smoke_law_split` / `smoke_sds` / `smoke_note` |

**回归**：**38 个 smoke 全绿**（smoke_reach 152/152、smoke_law_split 62/62、smoke_clp 82/82）；构建 44:44 配平 / **21931 行**。

> 后续变更（第三十轮）：`smoke_reach.py` 里的 OEL 占位断言已替换为 OEL 入口断言，断言数 152 → **151**；OEL 已由「占位页」升级为独立页面（见第十二节）。

**待确认（下次业务沟通时一并带上）**

- **A1「受限物质管理」子项数量**：现按 **两个** 子项实现（受限物质清单 `subst:list` + RoHS 限用物质 `law:rohs`）。用户给的菜单树只画了 RoHS 一项，但若只留 RoHS，`subst:list` 会失去菜单入口变成孤儿页。**默认按两项**，若要严格照树改可随时调整。
- **A2 中国 RoHS**：属独立体系（工信部《电器电子产品有害物质限制使用管理办法》），本次按「待业务确认，不做」处理。
- **A3 OEL / 运输数据源**：OEL = GBZ 2.1 + 欧盟 IOELV 指令；运输 = UN TDG + GB 6944 / 12268 + IMDG + IATA。**OEL 已于第三十轮落地为独立页面（见第十二节）；运输仍为待确认占位。**

---

## 十二、第三十轮收口 · 职业接触限值（OEL）法规库维护页（2026-09-18）

**范围（用户明确限定）**：只建设「合规管理 → 法规库维护 → 职业接触限值（OEL）」一个页面。**不动 CLP 法规库、REACH 法规库、SDS 生成向导、法规统一查询、投放市场与法规库的关联逻辑；不实现真实 PDF 解析 / 后台接口 / 自动抓取 / SDS 联动。**

**设计原则（本轮最关键的认知）**：用户说的原话是「**OEL 不是一部单独法规，而是欧盟及成员国发布的多套职业接触限值清单**」。所以本页**不以「法规」为单位，而以「国家/地区的官方限值数据集版本」为单位**维护，主线是 `官方来源 → 数据集版本 → 限值明细 → 版本对比 → 审核发布`。这条口径写进了页面头部主信息条的 `note`，**后续再动 OEL 页必须先读它**。

**交付**

| # | 事项 | 落点 |
|---|---|---|
| 1 | OEL 法规库维护页（顶部 4 指标 + 五步流程条 + 4 Tab + 详情抽屉 + 7 步导入向导 + 7 类数据校验） | **新分片** `23z9-js-oel.js`（约 1030 行） |
| 2 | OEL 专用样式（`.oel-flow` 流程条 / `.oel-diffbar` 变更计数 / `.oel-old` `.oel-new` 新旧值 / `.oel-check` 校验卡片 / `.oel-split` 双栏上传 / `#oelDw` 抽屉） | **新分片** `06z5-css-oel.css`（已在 `build.sh` 登记） |
| 3 | 删 `23z8` 里的 `law:oel` 占位注册（改注释，不留同路由重复注册） | `23z8-js-law-misc.js`（190 → 169 行） |
| 4 | 菜单项更名：`职业接触限值法规库（占位）` → `职业接触限值（OEL）`，`prio` 由 `plan` 改 `P1` | `22-js-router.js` |
| 5 | REACH 页 Tab1「数据来源」第 8 行文案同步：OEL 法规库「本期占位」→「按国家 / 数据集版本维护」 | `23z7-js-reach.js` |
| 6 | 专项自检 | **新增** `smoke_oel.py`（183 断言）；`smoke_reach.py` 占位断言改入口断言（152 → 151） |

**四个 Tab 落地要点**

- Tab1 数据来源（9 字段，6 条演示数据：欧盟（EU）/ 德国 / 英国 / 法国 / 西班牙 / 中国）；「新建数据来源」只增来源，**已覆盖国家/地区按已建数据集去重计**，故新建来源后该 KPI 不变（`smoke_oel` 有专门断言）。
- Tab2 数据集版本（12 字段 + 6 操作，11 个版本）：**原始 PDF / 网页存档与结构化 Excel 分开保存**；状态机 = 草稿 → 待审核 → 已发布 / 已失效；「待审核版本数量」KPI 随提交/发布实时联动。
- Tab3 限值明细（18 字段，26 行）：**同一 CAS 跨国家、跨版本并存不覆盖**（甲苯 `108-88-3` 6 条）；行抽屉内含「同 CAS 其他国别 / 版本」对照表。
- Tab4 版本对比与审核（6 类变更 + 11 条差异）：变更详情抽屉 + 审核通过 / 退回修改 / 发布；会话 A（德国）/ B（中国）可切换。

**踩过的坑（后续别重犯）**

1. Tab4 的过滤器状态 `_oelF.cmp` 是**对象** `{cmp:'A'}` 不是字符串，曾写成 `r.cmp !== f4` 导致永远只出 1 行 → 正确写法 `_oelF.cmp.cmp`。
2. 向导第 2 → 3 步时 `$('oiSrc')` 不存在（该 DOM 只在第 1 步渲染），直接取值会抛 TypeError 把后续渲染一起中断；且 `srcIdx` 用 `if(!_oelImp.srcIdx)` 判断会把合法的 `0` 当假值 → **改用 `==null||isNaN` 判定**。
3. 用 `Edit` 改文案时 `old_string` 同时匹配到文件头注释块，导致 `**加粗**` 这类 Markdown 语法插进 HTML —— **改 HTML 文案要把上下文带够**。
4. smoke 断言前务必在**切完 Tab 之后**再取 `textContent` 快照，否则断言的是上一个 Tab 的文本。

**回归**：**39 个 smoke 全绿**（smoke_oel 210/0、smoke_reach 151/0、smoke_clp 82/82）；构建 **45:45 配平 / 23001 行 / 2531525 bytes**；`check_names.py` 1334 个全局符号无新增重名；`check_pages.py` 70 个注册页、无菜单孤儿页、`law:oel` 已不在「重复注册」名单。

> ⚠️ 本轮改动被 Codex 的**自动提交钩子**（`auto: 2026-09-18 15:30`）抢先扫进仓库，已 `--amend` 改写为描述性提交信息固化。**再遇同样情况照此处理**，别留 `auto:` 裸信息。

### 第三十轮补充 · 来源收敛 + 限值类型语义槽位（2026-09-18 第二次拍板）

用户对 B1–B4 的答复：**① 公司只做欧盟和中国市场，其他国家的来源先不考虑；② 限值类型要做归一；③ 不涉及后端、是演示原型，不用做那么多；④ 联动等后面做。**

| 项 | 决定 | 落地 |
|---|---|---|
| B1 来源范围 | **只留欧盟（EU）+ 中国** | `23z9` 数据层收敛：来源 6 → **2**、数据集版本 11 → **6**、限值明细 26 → **16** 行；德 / 英 / 法 / 西 全部删除（含 Tab1 提示文案、来源说明、向导示例文件名）；`smoke_oel` 增加「页面不得再出现德 / 英 / 法 / 西」红线断言 |
| B2 限值类型归一 | **做，但只做轻量语义映射** | 新增 `OEL_KINDS` / `OEL_KIND_DICT` + `oelKinds()` `oelKindTag()` `oelKindChips()` `oelKindText()` `oelKindAlign()` `oelKindDrawer()`；Tab3 加「语义槽位」列（19 列）+ 槽位筛选器 + 「限值类型字典」抽屉；Tab4 变更表加「语义槽位」列、变更详情加槽位行；向导第 4 / 6 步带槽位；校验通过项补「限值类型可映射到槽位」 |
| B3 来源软删除 | **不做** | 保持「只新增 / 编辑」，不做删除 |
| B4 SDS 第 8 节联动 | **后面做** | 页面边界文案维持「后续统一实现」 |

**语义槽位口径**（写进 `OEL_TOP.boundary` 与字典抽屉，后续再动必须先读）：

| 语义槽位 | 中国 GBZ 2.1 | 欧盟 IOELV |
|---|---|---|
| 长期（8 h TWA） | PC-TWA | 8h TWA |
| 短期（15 min） | PC-STEL | STEL |
| 上限（任何时点） | MAC | 清单未设（字典标「—」） |

**明确不做数值换算归一**：ppm → mg/m³ 需要温度 / 压力 / 分子量假设，把中国 MAC 与欧盟 STEL 折算成同一个数更是两套法律体系之间的口径搬运。页面与字典抽屉都显式写明「**只对齐语义、不换算数值**」，并把「为什么不做数值归一」写进抽屉。

**本轮数据口径（演示）**

- 数据集版本 6 个：已发布 2（EU V2026.1 · CN 2019 版）· 待审核 2（EU V2027.1 · CN 2026 送审稿）· 草稿 1（CN 2027 版）· 已失效 1（EU V2025.1）。
- 限值明细 16 行：EU V2026.1 五条（甲苯 / 二甲苯 / 甲醛 / 丙酮 / 甲醇）· EU V2025.1 三条（**保留已失效版本记录，用于演示跨版本并存**）· CN 2019 版七条 · CN 送审稿一条（苯）。
- 版本对比两条会话：A「EU IOELV V2026.1 → V2027.1」6 条；B「CN GBZ 2.1-2019 → 2026 送审稿」4 条；合计覆盖 6 类变更。
- ⚠️ 甲苯 `108-88-3` 现在跨 **3 个版本**（EU V2026.1 / EU V2025.1 / CN 2019）并存——这是「同一 CAS 不覆盖」这条口径的演示锚点，改动数据时不要破坏。

**待确认（下次业务沟通时带上）**

- **B1（更新）** 来源扩展：本期只建欧盟 + 中国。若将来做其他市场，走「新建数据来源」即可，数据结构已支持。
- **B2（更新）** 语义槽位已落地为「只对齐不换算」。**是否还需要「同一槽位并排对比视图」**（把中欧同槽位数值并排显示）待确认——注意并排显示易被误读为「可直接比较」，若要做得加显著提示。
- **B3** 数据来源是否可删：现只做新增与编辑，**未做删除**（避免误删已发布数据集的上游来源），是否需要软删除待确认。
- **B4** 与 SDS 第 8 节的联动：本轮按用户要求**不做**，「投放市场 → 有效法规版本 → SDS 第 8 节」的关联后续统一实现。

---

## 十三、第三十一轮收口 · CLP 导入新版本（AI 解析 → 专员整理稿）路线切换（2026-09-18）

**起因**：Codex 先后给了两版「CLP 导入功能调整 prompt」。**第一版**（AI 智能解析 + 三栏工作台 + 7/9 步 + 「导入任务」列表页）被否；**第二版**（不做 AI 解析，改为「法规专员在系统外整理 → 导入结构化规则表」）被采纳。用户的最终指令是：

> 「不用出 prompt 了，你直接改就行。需要我排版的内容：ATP 版本号 改吧，Annex 只读展示 9 个图式 + 适用危害类别，3. 按你的推荐做，4. 导入任务是什么？这个「任务」指的什么」

**对「导入任务」的回答（结论：不做）**：「导入任务」是第一版 prompt 里的**列表页/工单概念**（把每次导入当成一张可排队、可指派、可重试的任务单）。第二版方案里已经没有这个概念，本轮也**不建**——理由：这是演示原型、不涉及后端与异步任务队列；每次导入本身就是一次同步的五步向导，工单化只会多出一层没有后端支撑的空壳页面。

### 交付

| # | 事项 | 落点 |
|---|---|---|
| 1 | CLP 数据层扩容：`CLP_MODULES.vi` 改 ATP 22；`CLP_VI_ROWS` 补 `euh`/`picto`/`signal`；`CLP_RULES` 补 `method`/`engine`/`test`/`checker`/`checkDate` 并新增 CR-006、CR-007；新增 `CLP_METHODS`（7 条）与 `CLP_PICTO`（GHS01–GHS09）；`CLP_LABELS` 12 → **15 行**（补 `isCombo`/`lang`/`appliesTo`/`st`，加 3 条官方组合码）；`CLP_CHANGES` 补 `mod` | `23z6-js-clp.js`（538 行） |
| 2 | Tab1 12 → **15 列**、Tab2 10 → **14 列**、Tab3 9 → **12 列**、Tab5 加**来源模块**列；Tab3 顶部加 **Annex V 只读图式块**；三个抽屉同步补字段 | `23z6-js-clp.js` |
| 3 | 新增 `clpLViVers` `clpLMethod` `clpLEngineTag` `clpLTestTag` `clpLStTag` `clpLRuStTag` `clpLModName` `clpLPictoBlock` | `23z6-js-clp.js` |
| 4 | **删掉 23z6 里的旧导入向导**（改为指向 23z6a 的注释，保留 `onclick="clpLImport()"`） | `23z6-js-clp.js` |
| 5 | **新分片**：五步向导 + 三模块分支（`CLP_IMP_MODS` / `CLP_IMP_KEYS` / `CLP_IMP_LV` / 第 1–5 步渲染 / 第 4 步三子 Tab / 发布落库 / 影响范围 / 轻量 Markdown） | **`23z6a-js-clp-import.js`**（约 600 行） |
| 6 | **新分片**：向导专用样式 | **`06z3a-css-clp-import.css`**（已登记 `build.sh`，紧跟 `06z3-css-clp.css`） |
| 7 | 重写专项自检 | `smoke_clp.py`（82 → **182** 断言） |

### 关键决定（Cayla 拍板）

| 项 | 决定 | 落地 |
|---|---|---|
| ATP 版本号 | **改** | 现行 = ATP 22（2026-05-01）；导入目标 = ATP 23（2027-02-01） |
| Annex V 象形图 | **只读展示 9 个图式 + 适用危害类别** | `CLP_PICTO` + `clpLPictoBlock()`，红框菱形几何占位（原型阶段无真实图片素材），并写明「图片素材建库时一次性导入，不随字典导入上传」 |
| 测试未通过的规则 | **不进发布清单**（按推荐） | `CLP_IMP_MODS.rules.excluded=['CR-006']`，发布新增数扣除排除项；变更记录正文带「排除 1 条」 |
| 「导入任务」 | **不做** | 见上文 |

### 本轮修掉的两个真 Bug

1. ⚠️ **`CLP_RULES` 数组被提前闭合**：上一轮用脚本插入 CR-006 / CR-007 时，在 CR-005 之后误留了一个 `];`，导致 CR-006 / CR-007 变成游离语句（`{id:...}` 在语句位置被解析成块 + 标签，紧跟 `,name:` 直接 **SyntaxError**）。**`node --check` 才暴露出来**——所以改完 JS 分片一定要 `node --check`，`build.sh` 只检查 `<script>` 标签配平，**不检查 JS 语法**。
2. **向导被二级弹窗顶掉**：`clpLImpTmpl()`（导入模板）与 `clpLImpImpact()`（影响范围）都走 `openModal()`，而全站只有一个 `#modal` 宿主 → 打开即覆写向导，关掉就「向导没了」。已加 `clpImpRestore()`，两个二级弹窗的关闭按钮改为「返回向导」并重建当前步。

   ⚠️ **2026-09-20 更正**：当时写的「REACH（`23z7`）与 OEL（`23z9`）的向导有同样问题」**对 OEL 是错的**。复核结论：**只有 REACH 真有此问题，已在第三十二轮修掉**；`23z9` 全分片只有两处 `openModal`（`oelImport()` 向导本体、`oelSrcNew()` 数据来源弹窗），向导 7 步内无二级弹窗，其「限值类型字典」走的是**独立遮罩** `#oelDwMask`（不共用 `#modal`）、校验项「标记为已处理」是**内联改状态**（`oelCheckFix`），均不受影响。详见第十四节。

### 顺手修掉的一个可读性问题

规则表「审核状态」原用通用 `TAG_CLS()`，而它把「已审核」和「待审核」**都映射为蓝色**，演示时看不出差别 → 新增本页专用 `clpLRuStTag()`：**已发布 / 已审核 = 绿 · 待审核 = 橙 · 审核中 = 蓝**。同时把「已发布」加进 Tab2 的状态筛选器。

### 一个跨页面的补漏

`.oel-old` / `.oel-new` 这两个类名**在 `23z9-js-oel.js` 里用了但全项目从未定义过样式**（OEL 的新旧值单元格一直是裸文本）。本轮在 `06z3a-css-clp-import.css` 里**全局定义一次**（旧 → 灰、新 → 深绿加粗），**CLP 版本比较表与 OEL 新旧值同时受益**，避免两处各写一份。OEL 页视觉有变化属预期，`smoke_oel.py 210/0` 全绿。

### 回归

**39 个 smoke 全绿**（`smoke_clp` **182/0**、`smoke_oel` 210/0、`smoke_reach` 151/0、`smoke_law_split` 62/0，其余照旧）。
构建 **46:46 配平 / 23701 行 / 2591322 bytes**（较第 30 轮 +147 行 / +约 0.6 万字节）。
`check_names.py` **1372** 个全局符号、无新增跨分片重名（已知有意覆盖 7 处）；`check_pages.py` 70 个注册页、无菜单孤儿页、无新增重复注册。

### 本轮新增的教训（写进铁律）

- **改 JS 分片后必跑 `node --check <分片>`**：`build.sh` 只数 `<script>` 标签，不管 JS 语法，语法错要等浏览器 `pageerror` 才炸。
- **`23zN` 与 `23zN+1` 之间插分片用字母后缀（`23z6a`），不要用 `23zN0`**——数字后缀在 C.UTF-8 字典序里会排到 `23zN` 前面。
- **`00-` 系列的命名坑**：`06z3-css-clp.css` 与 `06z3a-css-clp-import.css` 同属 CLP，后者必须紧跟前者；`build.sh` 的 CSS 段是**逐行手写 `cat`**，新增 CSS 分片**必须手动登记**（JS 是 `for` 循环自动扫，CSS 不是）。

---

## 十四、第三十二轮收口 · REACH 导入向导「二级弹窗顶掉向导」修复（2026-09-20）

**范围（用户明确限定）**：只修 REACH 导入向导的这一个 Bug。**不动 CLP / OEL / SDS 生成向导，不动 REACH 页其余内容。**

### Bug 是什么（演示级事故）

全站只有一个模态宿主 `#modal`（`openModal()` 写它；`closeModal()` 只把 `#mask.className` 清空）。REACH 向导本体开在 `#modal` 里，而第 4 步 footer 的「查看影响范围」→ `rchImpImpact()` **又调一次 `openModal()`**，把向导整块覆写成影响范围面板；再点「关闭」→ `closeModal()` 直接把模态关掉 → **向导消失**。此时重点「导入新版本」会走 `rchImport()` 里的 `_rchImp={}`，**已填的 4 步全清空，从第 1 步重来**。

### 修法（照抄 CLP 第三十一轮的 `clpImpRestore()` 模式）

| # | 改动 | 落点 |
|---|---|---|
| 1 | 新增 `rchImpRestore()`：按 `_rchImp.step` 重建向导当前步（第 3 步且已有文件时补 `rchImpShowFile()`，恢复文件名行与「下一步」可用态） | `23z7-js-reach.js` |
| 2 | `rchImport()` 改为 `_rchImp={step:1}` + `rchImpRestore()`；`rchImpNext(n)` 补 `_rchImp.step=n`（步号**首次**被持久化） | 同上 |
| 3 | `rchImpImpact()` footer：「关闭」→ **「返回向导」**，`onclick="closeModal()"` → `onclick="rchImpRestore()"` | 同上 |
| 4 | `smoke_reach.py` 补 4 条断言（151 → **155**） | `smoke_reach.py` |

**没改的**：`rchChgImpact()`（REACH 页 Tab6 的「查看影响范围」）是**页面级**弹窗、不在向导内，`closeModal()` 语义正确，保持原样。

### ⚠️ 更正上一轮的一处误判

第三十一轮 §十三 写的「REACH 与 OEL 的向导有同样问题」**对 OEL 不成立**，已在该处就地更正。复核依据：`23z9-js-oel.js` 全分片仅两处 `openModal` —— `oelImport()`（向导本体）与 `oelSrcNew()`（Tab1「新建数据来源」，不在向导内）；向导 7 步内**没有**任何二级 `openModal`，其「限值类型字典」用**独立遮罩** `#oelDwMask`（`oelDrawer()` / `oelDrawerClose()`），校验项「标记为已处理」是 `oelCheckFix()` **内联改状态**。

### 顺手排掉的同类风险（结论：无）

按「分片内既写 `#mBody` 又有 `openModal`」筛出 4 个分片逐个复核：`23z6a`（CLP，第 31 轮已修）、`23z7`（本轮修）、`23z9`（无此问题）、`23-js-sds.js`（`lawUpload()` 三步向导与 `dbImport()` 批量导入的步进都只用内联改写 + `toast`，**不弹二级模态**）。**全项目此类 Bug 已清干净。**

### 回归

**39 个 smoke 全绿**（`smoke_reach` **155/0**，`smoke_clp` 182/0、`smoke_oel` 210/0、`smoke_law_split` 62/0，其余照旧）。
构建 **46:46 配平 / 23711 行 / 2591830 bytes**（较第 31 轮 +10 行）。

### 本轮教训

- **`smoke` 会替 Bug 打掩护**：旧断言是 `rchImpImpact()` → `closeModal()` → 直接 `page.evaluate("rchImpNext(5)")`，绕过 UI 强制推步，所以状态层过了、真实交互是坏的。**凡是"用户会点的路径"，断言要走真实点击（`locator(...).click()`），不要用 `evaluate` 直接调内部函数跳步。**
- **`closeModal()` 清的是 `#mask` 的 className**：判断模态是否打开要看 `$('mask').className === 'on'`，不要看 `#mBody` 是否存在（它一直存在，只是被遮住）。
- **把信息从「常驻」改成「折叠可见」，旧断言必挂**（2026-09-21 第三十六轮）：`innerText` 拿不到 `display:none` 的内容。此时断言要改成**两段式**——① 默认状态不可见 ② 触发展开后可见，**不要直接删断言**。照此办理的：`smoke_sds` / `smoke_deliver` 里关于规则包编号、CLP-R / CLP-M 方法号的断言。
- **Edit 改一行到一半会留下孤儿括号**：把 `+row([…])` 整体替换成 `+'<tr>…'` 时，原语句的**闭括号在新串里不存在**，容易多/少一个 `)`。Python 补丁脚本用 `assert count==1` 能挡住锚点错位，但挡不住括号失衡——**替换完必须 `node --check`**（本轮就是这样抓到一个 `SyntaxError`）。
- **mock 数据里的「相对日期」会随时间过期**（2026-09-21 发现、2026-09-22 第三十七轮用 `DEMO_TODAY` 锚点根治）：演示数据**禁止写死**具体年月日——要么用 `lastWeekDate(dow)` 相对生成，要么在装配处用 `expAnchorDates()` 按锚点平移。详见 §十七。
- **改时间锚点类代码时，先查清取值链再动手**：`experimentDate` 的实际来源不止数据字段——`expDate()` 的优先级是 `expSummaries[].items[].basic.date` > `analyzedAt` > `dueDate`，只平移 `createTime/experimentDate` 会漏掉第一优先级，导致「看起来改对了、实验集合却变了」。
- **补丁脚本的幂等判断必须先判「新文本是否已存在」**：若 `new` 里包含 `old` 锚点（例如在「`/* ---- Toast ---- */`」前插一整块，`old` 就是那个标记），只判 `old` 命中数就会**重复插入**（本次实际插进去 4 份）。正确写法：`if new in s: skip`，再判 `old` 命中数。

---

## 十五、法规库与 SDS 调用合同（设计基准 · 2026-09-21 定稿）

> **文档不在项目根目录**，在 Obsidian Vault：
> `~/Desktop/work/03 Documents/Obsidian Vault/PLM/需求讨论/法规库与SDS调用合同（设计基准）.md`
> 同目录另有 `法规库速查卡（设计视角）.md`（09-21）、`SDS法规库数据清单.md`（09-17）、`欧盟SDS必须法规待办.md`（09-18）等配套资料。
>
> **地位**：后续所有法规相关 prompt / 研发接口的**基准**。改这份文件 = 改合同，需 kaka 确认。**新会话做法规相关改动前必读。**

### 15.1 合同要点（压缩版，细节看原文档）

| 条 | 内容 |
|---|---|
| **目的** | 用户选组分录浓度 → 系统按法规库自动填「是否受限 / H 码 / P 码」→ 混合物加和计算 → 填 SDS 模板出文档；另有法规统一查询页（CAS / 物料名 → 各法规列入情况） |
| **三方分工** | 法规专员管**参数实例**（阈值/条款号/生效日期）+ 审核发布；研发管**计算方法库**（代码）；系统管版本/审核/证据灯/调用接口 |
| **库分四类** | 计算引擎类（CLP、中国 GHS GB 30000）／名单匹配类（REACH XVII·XIV·SVHC、国内危化品目录、ZDHC、RoHS、禁止进出口目录、有毒化学品名录）／限值查询类（OEL）／运输规则类 |
| **合同 A** | 专员维护字段：规则表 22 字段 / 清单类 11 字段 / 字典类 9 字段；**引擎支持状态由系统生成，非人工填写**；法规修订走 diff 增量比对 |
| **合同 B** | 计算方法库**原型期固定 5 个**：`M-SCL` / `M-GCL` / `M-ATE` / `M-MFACTOR` / `M-LIST`；每个方法须带**输入参数结构 / 输出结构 / 代表性测试用例 / 实现状态**；**测试用例未通过的规则不得发布给 SDS 调用** |
| **合同 C** | SDS 向导输入（组分 CAS+浓度 / 产品类型 / 目标市场）→ 必须返回 4 项：① 各库列入情况 ② 分类结论（类别+H/P 码+象形图+信号词）③ **计算过程证据**（命中规则+方法代码+输入+中间值+结果）④ **版本三件套快照**（数据版本+规则版本+模板版本），法规升级不静默改写历史文档 |
| **不变量** | 把用户当八十岁老奶奶设计（下载→喂 AI→对数字→上传→看结果点发布，五步）；系统只吃人确认过的结构化文件，AI 是人的工具不是系统功能；法规原文只存档+全文检索不做解析；同一计算方法只开发一次多市场复用；演示口径克制 |

### 15.2 已符合项（2026-09-21 逐项核过）

| 合同条 | 原型落点 | 结论 |
|---|---|---|
| **合同 A · 规则表全字段** | 字段不是扁平 key，而是**嵌套在每条规则的 `det` 对象里**：`det.inputs`（所需输入）/ `det.cond`（前置条件）/ `det.formula`（法规公式·判定说明）/ `det.except`（例外条件）/ `det.prio`（规则优先级）/ `det.output`（输出分类）/ `det.label`（标签要素）/ `det.src`（来源 Part + 章节条款号）。7 条规则 × 8 个字段**全部有值，无一为空** | ✅ 符合 |
| **合同 A · 规则表 ↔ 导入模板** | `23z6a` 的 Annex I 模板 `fields` 已含 20 个业务填写列，**含「来源 Part」独立列**；「引擎支持状态」由系统生成、不在模板里（与合同一致） | ✅ 符合 |
| **合同 A · 清单类 11 字段** | `REACH_XVII`（8 条）字段为 `entry/name/ids/obj/type/use/thr/eff/exempt/sum/ver`，与合同逐项对齐 | ✅ 符合 |
| **合同 A · 字典类 9 字段** | `CLP_LABELS`（22 条）字段为 `tp/code/text/lang/picto/signal/combo/src/eff`，全覆盖 | ✅ 符合 |
| **硬约束：测试未通过的规则不得发布** | 第 31 轮起实现（`CLP-R-0006` 测试未执行 → 不进发布清单）；第 33 轮加了「上传修正版并重新校验」闭环 | ✅ 符合 |
| **合同 C-4 版本快照** | `clpActivePack()` 生成规则包只读快照（含 VI/rules/labels 三模块版本），SDS 向导用 `classPack` 冻结，「法规升级不静默改写历史文档」已在页面文案写明 | ✅ 符合 |
| **不变量 · AI 是人的工具不是系统功能** | 全项目 grep「智能解析 / 自动解析 / AI 解析」**0 命中**；系统只收「已确认的结构化规则表」 | ✅ 符合 |
| **不变量 · 老奶奶五步** | 第 4 步已有「下载 CSV 模板 → 下载问题明细 → 上传修正版 → 看结果 → 点发布」，报错文案是「第 N 行缺 X」而非参数结构错误 | ✅ 符合 |
| **不变量 · 原文只存档不解析** | 页面明确写「系统不解析法规原文」 | ✅ 符合 |
| **系统框架：版本管理 / 审核发布 / 证据灯** | 各法规库 Tab 顶部模块信息条 + 证据灯齐备 | ✅ 符合 |

### 15.3 有意偏离（**Cayla 拍板，不是缺陷，不要改回去**）

| # | 合同原文 | 原型现状 | 拍板时间 |
|---|---|---|---|
| **B1** | 方法代码为 `M-SCL` / `M-GCL` / `M-ATE` / `M-MFACTOR` / `M-LIST` | 改为带域前缀的 **`CLP-M-*`** 体系：`CLP-M-SCL` / `CLP-M-GCL-SUM` / `CLP-M-ATE-SUM` / `CLP-M-MFACTOR` … | 2026-09-21 Cayla 要求 |
| **B2** | 原型期**固定 5 个**方法 | **7 个**（多出 `CLP-M-LAYER` 分层裁决、`CLP-M-ED-PBT` ED/PBT 判定；另有 `CLP-M-BRIDGE`） | 同上 |

> ⚠️ 这两条是**用户主动要求偏离合同**。改这份合同需 kaka 确认，但原型实现以 Cayla 口径为准；若合同要同步更新，是去改那份 md，不是改代码。

### 15.4 下一步要做的内容（合同已覆盖、原型尚未开工）

| # | 合同要求 | 现状 | 类型 |
|---|---|---|---|
| **N1** | 计算引擎类含**中国 GHS（GB 30000）** | 国内危化品仅有 `23y` 通用维护页，无引擎、无导入向导；合同注明的「国内比欧盟多一个『是否危化品 / 需登记』输出字段」未体现 | 新建 + 改造 |
| **N2** | 名单匹配类含 **ZDHC MRSL** | 重复注册已消除（第 35 轮），但无导入向导 | 补导入入口 |
| **N3** | 名单匹配类含**禁止进出口目录** | 全项目 grep 0 命中，**库不存在** | 新建 |
| **N4** | 名单匹配类含**有毒化学品名录** | 同上，**库不存在** | 新建 |
| **N5** | 名单匹配类含**RoHS** | 页面已有 10 条全列（`23z8`），无导入向导 | 补导入入口 |
| **N6** | 运输规则类（UN 编号与运输分类） | `23z8` 占位页，数据源待确认 | 待定数据源 |
| **N7** | **C&L Inventory**（外部参考数据） | 菜单已建，通用维护页 | 补导入入口 |
| **N8** | 计算方法库含 **名单命中判定方法**（合同的 `M-LIST`，名单匹配型代表） | 7 个方法全是 CLP 计算类；且 SDS 向导 grep `SVHC` 0 命中，**名单库尚未被 SDS 调用链引用** | 新建方法 + 接通调用 |

> **N8 说明**：`M-LIST` 是「名单匹配类」法规库（REACH XVII/XIV/SVHC、危化品目录、ZDHC、RoHS、禁止进出口、有毒化学品名录）在计算方法库里的**统一执行入口**——合同要求这 7 个名单库共用同一个「阈值 / 用途 / 豁免命中判定」方法，而不是每个库各写一套。所以 N8 做完，N3/N4 两个新库才是「能被 SDS 调用」而不是摆着看的数据表。

### 15.5 ⚠️ 自查更正记录

- **2026-09-21 误报 A1**：初版核查把「规则表缺 8 个字段」列为不符项，依据是 `Object.keys(CLP_RULES[0])` 只有 17 个 key。**这是错的**——合同 A 的这些字段全部存在，只是**嵌套在每条规则的 `det` 对象内**（`det.inputs` / `det.cond` / `det.except` / `det.prio` / `det.output` / `det.label`），顶层 key 看不到而已。逐条验证：7 条规则 × 8 个字段**全有值**；且 `23z6a` 的 Annex I 导入模板已有 20 列、含「来源 Part」独立列。**A1 实际符合，无需改动。**
- 教训写进 §六：**核查数据结构时不能只看顶层 key，要看嵌套对象与「导入模板 fields」这类字段清单源头**；报「缺字段」前必须同时查三处——数据对象（含嵌套）、渲染列、导入模板。

---

## 十六、第三十六轮收口 · SDS 向导第 3 / 4 步「结论诚实性」改造（2026-09-21）

> 背景：Codex 给了一份实施方案（详见会话记录），核心诉求是**不得把写死的分类结论伪装成规则引擎自动计算结果**。
> 用户当轮明确降口径：**「演示原型可以先不考虑背后的规则计算引擎，只需要达到演示功能的目的」**——所以本轮只校准「演示叙事是否通顺 / 结论是否诚实」，**不重构引擎、不引入新依赖**。

### 16.1 改造前的真实家底（先别信文档，代码为准）

`buildClassItems()` 的 11 个条目 = **5 条引擎真算**（`clpEvaluateMixture()` 产出：acuteOral / skin / sens / eye / aqua）+ **6 条手写**。手写里原先 **4 条状态标错**：

| id | 项目 | 改前状态 | 真实依据 | 改后 |
|---|---|---|---|---|
| `ed` | 内分泌干扰 | `auto` | ❌ 引擎 `CLP-M-ED-PBT` 状态是「需要研发实现 / 未执行测试」；`clpActivePack()` 只取 `CLP-R-0001~0004`，0006 不在生效包；**且页面原有一句「甲醛 0.35% 未列入 ED 清单」属无源断言**（`COMP_CLP` 根本没有 ED 字段） | **待人工判断**（原断言已删，改为「缺少判定输入 → 不计算」） |
| `pmt` | PMT / vPvM | `auto` | 同上 | **待人工判断** |
| `stot` | STOT 一次接触 | `auto` | Σ=11.35% ＜ 20% 限值，加和方法未接入引擎 | **系统建议·待人工确认** |
| `carc` | 致癌性 | `auto` | 甲醛 0.35% ≥ 0.1% 有依据，但致癌加和方法未接入引擎 | **系统建议·待人工确认** |
| `resp` `repr` | 呼吸道致敏 / 生殖毒性 | `pending` | 已正确 | 保持 judge 型（**未显式写 `need` 字段**；`wzCheck(4)` 用 `need!=='confirm'` 判定，故无 `need` 即归 judge 组。⚠️ 与 `ed`/`pmt` 的写法不一致，需统一时一并补齐） |

### 16.2 本轮落地的五件事

| # | 内容 | 落点 |
|---|---|---|
| 1 | **第 3 步改「异常优先」**：5 概览 KPI + 四异常区块 + 全部数据默认收起 | `renderStep3()` + 新增 `EVID_META` / `wzEvidence()` / `wzBlockers()` / `CONFLICT_MOCK` / `wzToggleAll()` |
| 2 | **第 4 步三态口径**：`need:'judge'`（不给结论，必须人判）vs `need:'confirm'`（给建议值+H 短语，待采纳） | `buildClassItems()`，`adjSave()` 保持写 `manual` |
| 3 | **「查看计算依据」折叠**（`evToggle()`）：规则包 / CLP-R / CLP-M / 输入 / 公式默认收起 | `23-js-sds.js` |
| 4 | **`adoptAllSug()` 一键采纳**：只采纳有建议值的 `confirm` 项，流转 **`confirmed`** 态 | 同上 |
| 5 | `wzCheck(4)` 阻断提示区分「待人工判断 / 待人工确认」 | 同上 |

**状态机（四类，别再混用）**：`auto`（引擎真算） / `confirmed`（已采纳系统建议） / `manual`（人工改判） / `pending`（待处理，含 judge 与 confirm 两种 `need`）。
消费侧 `hCodesMix()` 与 `classOf()` **只排除 `pending`** —— 所以未确认的项**不会**漏进标签要素，这也是 `smoke_deliver` 里 G1 那段必须先推 `carc` 到 `confirmed` 才能验 H350 的原因。

### 16.3 新增/修订的 smoke 断言（不放水，加严）

| 脚本 | 变化 |
|---|---|
| `smoke_deliver` | **97 → 100**。新增 3 条：① 致癌未确认前 H350 **不得**进标签要素（反向断言）② 待判断项不预置结论 / 待确认项必须带建议值+H 短语 ③ 标题旁标签与实际口径一致。**修订 3 条**：原「规则编号必须出现在正文」改为「默认收起 + 展开后可见」两段式；步骤 3 说明文案同步 |
| `smoke_sds` | 0 错（原 1 错，同上修订为两段式）。新增：三态口径断言、`adoptAllSug()` 流转断言、仍有 judge 项时第 5 步必须阻断 |

### 16.4 本轮教训（写进铁律）

- **演示原型也要「诚实」**：judge 型条目原先不仅状态标错，还**编了一句不存在的依据**（甲醛未列入 ED 清单）。判「数据是否支持结论」要顺着依赖链查到**数据字段层**——`COMP_CLP` 有没有这个字段，比规则/方法状态更根本。
- **状态=enough 但语义不够**：把 `judge` / `confirm` 都塞进 `pending` 会让阻断策略没得选；新增 `need` 字段区分，UI、阻断、smoke 三处口径才统一。
- **改 UI 折叠会打断旧断言**：把信息从「常驻」改成「折叠可见」时，相关断言必须改成**两段式**（默认不可见 + 展开后可见），否则会被迫删断言。已同步写进 §六 坑表。

### 16.5 ⚠️ 遗留：`smoke_weekly` 1 条失败（存量 · 时间相关）

断言「人工项置空后，自动带出的 4 组实验仍在」，实际 0。根因：`wkPeriodExps()` 按 `experimentDate` 筛本周实验，而 mock 那 4 组的日期写死在 `2026-09-09/10/11`（`src/21-js-data.js`），随真实日期推进已滑出本周 → 周报显示「本周期暂无实验记录」。

**已核：这条在 HEAD（本轮改动之前）上同样失败，非本轮引入。**

> ✅ **2026-09-22 第三十七轮已修复**——采用方案 ② `DEMO_TODAY` 锚点，详见 §十七。修复后 `smoke_weekly` **91/0 全绿**，且已用 5 个未来日期（+7 / +28 / +77 / +112 天）验证实验集合恒定。

---

## 十七、第三十七轮收口 · DEMO_TODAY 演示时间锚点（2026-09-22）

> 起因：第三十六轮遗留的 `smoke_weekly` 失败——周报按 `experimentDate` 筛「上一个完整自然周」的实验，而 mock 里那 4 组写死在 `2026-09-09/10/11`，真实日期一推进就滑出周期 → 周报整页空掉。
> 用户在两种修法中**选定 `DEMO_TODAY` 锚点**（而非「改动态日期」），理由是改一次、全站「今天」统一，以后不再过期。

### 17.1 锚点设计（`src/20-js-core.js`）

| 函数 | 作用 |
|---|---|
| `demoTodayStr()` / `demoNow()` | 全站「今天」唯一来源。默认跟随真实日期；可用 `window.DEMO_TODAY='YYYY-MM-DD'` 或 `localStorage['DEMO_TODAY']` 把演示**冻结**在固定某日 |
| `demoYmd(d)` / `dateAdd(s,n)` / `daysBetween(a,b)` | 日期格式化与加减 |
| `demoWeekRange(off)` | 完整自然周区间：`off=0` 上一个完整周（周报口径）、`off=-1` 本周 |
| `lastWeekDate(dow,off)` | 上周第 `dow` 天（1=周一…7=周日）——**新增 mock 数据用它生成，不要写死日期** |
| `DATA_EPOCH='2026-09-14'` / `demoShiftDays()` / `demoDate(s)` | 演示数据日期平移：δ = **本周一 − DATA_EPOCH**（必为 7 的倍数 → 星期几不变） |
| `expAnchorDates(e)` | 实验 / DOE 方案的日期字段整体平移，**带 `__anchored` 幂等标记**（`seedExperiments()` 会被调用两次） |

**为什么 δ 取「本周一 − 基准日」而不是「今天 − 基准日」**：周报窗口是 `[本周一-7, 本周一-1]`，只有按「周一」对齐平移，窗口成员关系才恒定。用「今天 − 基准日」会在周中错位（周五时会整体偏出一周），实测验证过。

### 17.2 改了哪些文件

| 文件 | 改动 |
|---|---|
| `src/20-js-core.js` | 新增锚点工具块；`nowStr()` / `todayStr()` / `daysFromNow()` 改走 `demoNow()`（`daysTo()` 因依赖 `todayStr()` 自动跟随） |
| `src/24z3-js-weekly-ai.js` | `wkPeriod()` → `demoWeekRange(off)`；`weeklyDateRange()` → `demoWeekRange(-1)` |
| `src/21-js-data.js` | `seedExperiments()` 出口对 `list` 与 `doeSchemes` 调 `expAnchorDates`；`seedExpSummaries()` 出口平移 `items[].basic.date` |
| `22z2` / `22z3` / `24z4` / `27z3` / `23z6a` / `27z6` / `27z8` | 各自本地的「取今天」函数（`tcNow` / `_mbToday` / `_aiToday` / `physToday` / `clpImpNow` / 年份下拉）统一改走 `demoNow()` |
| `smoke_weekly.py` | 4 条写死周期的断言改为 **Python 侧按同一规则动态计算**（`_wk(off)` 镜像 `demoWeekRange`），新增 3 条锚点断言 |

**没有平移的**：法规生效日、专利授权日、项目节点日、物料停用时间等**业务事实日期**——它们是绝对事实，跟着平移反而错（`smoke_clp` / `smoke_oel` / `smoke_proj` / `smoke_rawmat` 都有对具体日期的断言）。

### 17.3 ⚠️ 本轮踩的两个坑（已写进 §六 坑表）

1. **`experimentDate` 的真实来源不止数据字段**：`expDate()` 的优先级是 `expSummaries[].items[].basic.date` > `analyzedAt` > `dueDate`（`24z1` L181）。首版只平移了 `createTime/experimentDate`，结果 EXP-0418 的有效日期仍是未平移的 `09-08`，漂进了「上上周」→ 断言「上上周无实验」失败。**改时间锚点前必须先把取值链查全。**
2. **补丁脚本幂等判断写错会重复插入**：判断「已应用」时只看 `old` 是否命中是不够的——若 `new` 里包含 `old`（在 `/* ---- Toast ---- */` 前插整块时就是），`old` 会持续命中 → 同一块被插了 **4 份**（`demoNow` 定义了 4 次）。正确写法是**先判 `if new in s: skip`**。事后用 `grep -c` 数定义次数才发现。

### 17.4 验证

- `smoke_weekly` **91/0**（原 73 条断言 + 新增 3 条：平移量为 7 的整数倍、`wkPeriodExps` 上周恰 4 组、上上周 0 组）
- **全量 39 个 smoke 全绿**
- **未来日期压力测试**（`window.DEMO_TODAY` 注入后重载）：+7 / +28 / +77 / +112 天四种情形，上周窗口内实验集合恒为 4 组、上上周恒为 0 组，日期整体随之后移 —— **不会再过期**
- 产物 **24236 行 / 46:46 配平**，`check_names` 1415 符号无新增重名

### 17.5 后续约定

- 新增 mock 数据时：**相对日期一律用 `lastWeekDate(dow)` 或直接写死后交给 `expAnchorDates` 平移**，不要出现新的写死年月日（与时间窗口绑定的那些）。
- 想让整站演示冻结在某一天：页面加载前设 `window.DEMO_TODAY='2026-09-22'` 即可，无需改数据。

---

## 十八、第三十八轮收口 · SDS 一键补录演示数据 + 修 aqua 矛盾（2026-09-22）

> 起因：用户问「演示系统没有计算引擎也没有全部清单，**SDS 第 4 步涉及计算的部分会不会全空白**」。
> 实测回答：**不会空白**（见 18.1），但顺着这一问抓出**两处自相矛盾**（见 18.2），本轮一并修掉。

### 18.1 实测结论：第 3 / 4 步的真实渲染内容

用 Playwright 走到第 3、4 步抓真实文本，不是凭印象：

| 位置 | 实测内容 |
|---|---|
| 第 3 步概览 | 组分数 6 · 汇集 54 项 · 已自动获取 49 · 待补充 5 · 待确认 9（7 PubChem 辅助 + 2 来源冲突）· 阻止项 2 |
| 第 4 步 11 条目 | **5 项引擎真算**（ATE_mix 3922 mg/kg 不分类 / H315 / H317 / H319 / 水生）、**2 项系统建议**（H335 / H350）、4 项「—」 |
| 标签要素（未处理） | 象形图 GHS07 · P 码 12 条 · 信号词「警告」 |
| 点「采纳全部系统建议」后 | GHS07+GHS08 · P 码 20 条 · 信号词「危险」 |

**结论：演示叙事是完整的**，不存在「全是空白」的情况。真正需要注意的反而是另一头 —— **该空白的地方有没有老实留白**（这就是 18.2 的 aqua）。

### 18.2 查出的两处自相矛盾

| # | 矛盾 | 位置 |
|---|---|---|
| **①** | 第 3 步承诺「第 4 步会落到待人工判断」，第 4 步却照算不误标 `auto` | 承诺方 `23-js-sds.js` `wzBlockers()`（L743-745）+ L840 说明；**未兑现方** `23z6-js-clp.js` L223-244 `aqRows` 只筛 `haz.aquaticChronic`、**不看 `aqState`**，且 `m=r.p.mC\|\|1`（M=0 退化为 1）→ 照样算 |
| **②** | EUH 候选区写「EUH380 / EUH450 由 ED、PMT 判定**自动带出**，不可取消」，而 ED / PMT 已在第三十六轮改为待人工判断 | `EUH_OPTS` |

额外纠正一处**术语错误**：原来写「缺少可用 **M 因子** / 急性毒性数据」——按 CLP Annex I **表 4.1.3**，M 因子**只适用于 Acute 1 / Chronic 1**；甲醛是 Chronic 2、乙二醇单丁醚是 Chronic 3，按定义本就不适用。已改为准确表述「未归档急性毒性 / NOEC 实测依据」，并在 `eff` 里注明该组分是 Chronic 几、为什么不适用 M 因子。

### 18.3 本轮落地的四件事

| # | 内容 | 落点 |
|---|---|---|
| 1 | 新增 **`DEMO_FILL`**（演示补录数据源）。值写在**数据层**，渲染层不现场编结论 | `23-js-sds.js` |
| 2 | 第 3 步 ① 区块加「**一键填充演示数据**」、④ 区块加「**一键补充演示数据**」，由 `wzFillDemo(kind)` 执行；补录后 `wz.classItems=null;wz.classPack=null` 强制重算 | 同上 |
| 3 | **`23z6` 新增加和法输入可靠性门禁**：`var aqBad=aqRows.filter(function(r){return r.p.aqState!=='known';});` —— 非空则 aqua 降级为 `pending / need:'judge'`、result `'—'`、并在计算依据里写明「哪些组分缺依据、加和法未执行」；补录后自动恢复 `auto` | `23z6-js-clp.js` |
| 4 | EUH380 / 381 / 450 文案同步三态口径 | `23-js-sds.js` |

**演示链路现在是自洽的**：

```
未补录 → 第 3 步报「2 项会导致加和法输入不可靠」→ 第 4 步 aqua = 待人工判断（兑现承诺）
                    ↓ 点「一键补充演示数据」
补录后 → 第 3 步阻止项归零 → 第 4 步 aqua = 自动计算，恢复 H 码推导
```

### 18.4 M 因子 / 急性毒性数据的取值口径

用户问「缺的这些数据应该从哪里取值」。本轮答案是：**不凭空生成，而是把「更高的来源优先级」补上**。

- ① 缺失数据：按 `DEMO_FILL.collect` 逐项给出值 + 来源 + 依据编号，来源一律取**实测报告 `lab` / 官方限值 `reg` / 供应商 SDS `sup`**（不用 PubChem 辅助值，因为第 3 步明确「辅助来源不得作为合规依据」）
- ④ 水生毒性：给甲醛、乙二醇单丁醚补 `lc50` / `noec` 并把 `aqState` 置 `'known'`（这两个第 3 步的 PubChem 辅助来源里本来就有 LC50 值，补录的含义是「人工去核对实测报告后采纳」）
- **重要边界**：M 因子对 Chronic 2 / 3 本来就不存在，所以本轮并非「补 M 因子」，而是补**慢性分类的来源依据**（实测报告 + NOEC）

### 18.5 smoke 变化（加严，未删旧断言）

| 脚本 | 变化 |
|---|---|
| `smoke_sds` | **加严**：`aqua` 由单点断言改为**两段式**——① 存在 `aqState!=='known'` 组分时必须为 `pending/need:'judge'`/result `'—'` ② 临时置 `'known'` 后必须恢复 `auto` 且计算过程不含「加和法未执行」。另新增 **5 条真实点击断言**：两个按钮存在性、点击后缺失项归零、点击后阻止项归零、补录后第 4 步 aqua 恢复自动、两个按钮补完后消失。**点击后复位 `aqState`，保证后续第 5/6 步条件不变** |
| 其余 | `smoke_clp` 209/0、`smoke_mfactor` 63/63、`smoke_deliver` 100/100 全部保持 |

### 18.6 验证

- **全量 39 个 smoke 全绿**（含 `smoke_weekly` 91/0、`smoke_oel` 210/0、`smoke_reach` 160/0）
- 产物 **24298 行 / 46:46 配平 / 2635498 bytes**，`check_names` 1417 符号无新增重名
- Playwright 实测无 `pageerror`

---

## 十九、第三十九轮收口 · 合规模块阶段 1：建立最小真实计算引擎内核（2026-09-22）

> 定位：**行为保持型重构**。目标不是新增法规能力，而是把已经存在、已经在演示中真实运行的四类计算，
> 从 `clpEvaluateMixture()` 里抽出来，变成一套**最小、真实、受控、可测试、可追溯**的计算内核。
> 本阶段**不修改任何法规阈值、区间与结论**；发现的疑点一律只记录，留给后续专业核验。

### 19.1 重构前后结构对比

```text
【旧】clpEvaluateMixture() 一个人干完所有事
  ├─ 直接算 ATE          （阈值、区间、文案全写在函数体内）
  ├─ 直接算皮肤腐蚀/刺激
  ├─ 直接算眼损伤/刺激
  ├─ 直接判 SCL
  └─ 直接算 M 因子

【新】clpEvaluateMixture() = 规则包调度器（不含任何法规计算细节）
  ├─ 1 获取活动规则包        clpActivePack()
  ├─ 2 标准化配方输入        complianceBuildContext(formula, rule, pack)
  ├─ 3 按 rule.method 查执行器  complianceGetMethod() / complianceMethodImplemented()
  ├─ 4 执行计算方法          complianceExecuteMethod() → 23z6b 的四个执行器
  ├─ 5 汇总统一结果          items / executions / warnings
  └─ 6 转页面兼容结构        complianceAdaptItem() + complianceSortItems()
```

**关键变化：算法分派不再靠规则编号。** 旧代码是 `clpRuleById('CLP-R-0001')` 写死四条；
新代码遍历活动规则、按 `rule.method` 查注册表。规则编号现在只作审计信息。

### 19.2 新增 / 修改的文件

| 文件 | 类型 | 职责 |
|---|---|---|
| `src/23z6b-js-clp-engine.js` | **新增** | 计算方法注册表 + 统一执行上下文 + 四个执行器 + 未实现方法占位 + 兼容适配器 |
| `src/23z6-js-clp.js` | 修改 | `clpActivePack()` 改为「按状态 + 方法登记情况」筛选（不再写死规则编号）；`clpEvaluateMixture()` 改为调度器；删除旧的 `clpCalcRows()`（已由 `complianceBuildContext()` 取代） |
| `src/23-js-sds.js` | 修改 | 第 4 步「计算依据」里补两行：方法版本（`methodVersion`）与非成功执行状态的人类可读提示 |
| `smoke_compliance_engine.py` | **新增** | 方法级自检 **91 条**（不复制公式，全部在浏览器里跑真实引擎） |
| `smoke_adjclass.py` | 修改 | 同步一条**过时断言**（非降标，见 19.6） |

加载顺序：`23-js-sds.js → 23z6 → 23z6a → 23z6b`（build.sh 按文件名排序自然满足）。
`check_names.py`：**1437 个全局符号，无新增重名**。

### 19.3 方法注册表

注册表对象：`COMPLIANCE_METHOD_REGISTRY`；接口：`complianceRegisterMethod / GetMethod / HasMethod / MethodImplemented / ExecuteMethod / complianceMethodList()`。
注册时校验：代码非空、代码不重复、版本非空、实现状态合法（`implemented` / `not_implemented` / `disabled`）、`execute` 必须是函数 —— **任一条不满足直接抛错，不静默覆盖**。

| 方法代码 | 执行器 | 版本 | 当前状态 | 返回分类项 |
|---|---|---|---|---|
| `CLP-M-ATE-SUM` | `clpEngineAte` | 1.0.0-demo | implemented | `acuteOral` |
| `CLP-M-GCL-SUM` | `clpEngineGcl` | 1.0.0-demo | implemented | `skin`、`eye`（一次返回两项） |
| `CLP-M-SCL` | `clpEngineScl` | 1.0.0-demo | implemented | `sens` |
| `CLP-M-MFACTOR` | `clpEngineMfactor` | 1.0.0-demo | implemented | `aqua` |
| `CLP-M-LAYER` | `clpEngineNotImplemented` | 0.0.0-not-implemented | not_implemented | —（不可调用） |
| `CLP-M-ED-PBT` | `clpEngineNotImplemented` | 0.0.0-not-implemented | not_implemented | —（不可调用） |
| `CLP-M-BRIDGE` | `clpEngineNotImplemented` | 0.0.0-not-implemented | not_implemented | —（不可调用） |

后三个只登记状态、不给真实实现，作用有二：① 它们引用的规则进不了活动规则包；② 与 `CLP_METHODS` 展示字典里的「已支持」形成对照（差异见 19.7 待核验项第 ① 条）。

### 19.4 统一执行上下文与返回结构

上下文（`complianceBuildContext()` 产出，执行器只读）：

```text
{ market, asOfDate,
  rule:{id, version, method, parameters: rule.run},
  pack:{id, market, ruleIds, methodVersions},
  components:[{cas, name, concentration,
    parameters:{classifications, ateState, ateOral, ateDermal, ateInhalation,
                mAcute, mChronic, aquaticState}}] }
```

返回结构（每个方法统一）：`{method:{code,version}, rule:{id,version}, status, items, inputs, intermediates, evidence, messages}`。

| 内部状态 | 含义 | → 页面状态 |
|---|---|---|
| `AUTO` | 已自动计算并产生分类 | `auto` |
| `NO_MATCH` | 已完成计算但未达分类阈值 | `auto`（结果「不分类」） |
| `NEED_INPUT` | 缺 ATE / M 因子 / 分类依据等必要输入 | `pending` + `need:'judge'` |
| `UNSUPPORTED_METHOD` | 规则引用的方法尚未登记 | `pending` + `need:'judge'` |
| `BLOCKED` | 方法未实现 / 未启用 | `pending` + `need:'judge'` |
| `ERROR` | 执行时发生非预期错误 | `pending` + `need:'judge'` |

适配器另有一道防线：**任何非成功状态都不得把结果写成「不分类」**，一律改写为 `—`。

### 19.5 活动规则包筛选逻辑（`clpActivePack()`）

入选需同时满足：① 规则状态「已审核」或「已发布」；② 测试状态「通过」；③ 存在运行参数 `run`；④ `rule.method` 已登记；⑤ 方法 `implemented` 且有执行函数。
**结果仍是 `CLP-R-0001~0004` 四条**（与重构前实际执行范围一致），包编号 `CLP-EU-ATP22-R2026.2-L2026.3` 不变，并新增 `methodVersions` 快照（记录本次使用的执行器版本）。

被排除的三条各有各的原因，都不是靠编号排除的：

| 规则 | 排除原因 |
|---|---|
| `CLP-R-0005`（分层裁决） | 方法 `CLP-M-LAYER` 未登记实现 + 无 `run` 参数 |
| `CLP-R-0006`（ED / PBT） | 状态待审核 + 测试未执行 + 方法未实现 |
| `CLP-R-0007`（Bridging） | 状态待审核 + 方法未实现 |

### 19.6 验证（逐项报告）

| 项 | 结果 |
|---|---|
| **行为保持（硬证据）** | `git show HEAD:<产物>` 取旧产物，与新产物跑同一段 JS 逐字段 diff：`items` 的 id / name / result / code / status / need / method / ruleIds / input / formula / opts / src **全等**，顺序 `acuteOral→skin→sens→eye→aqua` 不变，`labels`（H315/H317/H319 · GHS07 · warning · 12 条 P 码）**全等**，差异 **0 项** |
| `smoke_compliance_engine.py` | **91 / 91 通过**（新增） |
| `smoke_clp` | 209 / 0 |
| `smoke_mfactor` | 63 / 63 |
| `smoke_sds` | 0 错 |
| `smoke_deliver` | 100 / 100 |
| `smoke_sds_p1 / p2 / sec / list` | 51 / 69 / 41 / 38，全通过 |
| `smoke_adjclass` | 24 / 24（原 22/23 的失败是**上一轮存量**：`55639d1` 把 `need:'confirm'` 项的 `sug` 从 null 改为保留建议值，而该断言写在基线期、仍要求「所有 pending 项 sug=null」，与第三十六轮「系统建议·待人工确认」的设计冲突。已按当前设计拆成两类断言：**judge 型 sug=null**、**confirm 型 sug=建议值**，未删除断言、未降标） |
| **全量 smoke** | **40 / 40 全绿**（含 `smoke_weekly` 91/0、`smoke_oel` 210/0、`smoke_reach` 160/0） |
| 构建 | **24790 行 / 2657989 bytes / script 47:47 配平** |
| `check_names.py` | 1437 个全局符号，**无新增重名** |
| `check_pages.py` | 69 页注册不变，无新增孤儿页 |
| `node --check` | `23z6` / `23z6a` / `23z6b` 三个文件均通过 |
| 禁用项自查 | 全仓 `src/*.js` **无 `eval(`、无 `new Function`**；无用户可编辑公式 / 表达式 DSL；执行器不读 DOM、不改 `wz` / `COMP_CLP` / `CLP_RULES` / 传入数组（均有断言覆盖） |

### 19.7 ⚠️ 待专业核验项（**本阶段只记录，未改任何法规结论**）

| # | 问题位置 | 当前表现 | 潜在影响 | 为何本阶段未改 | 需谁确认 |
|---|---|---|---|---|---|
| ① | `CLP_METHODS` 展示字典 vs `COMPLIANCE_METHOD_REGISTRY` 真实执行器 | `CLP-M-LAYER` 在展示字典里标「**已支持**」，但引擎侧从未登记实现、也无 `run` 参数与测试 | 页面宣称「可自动计算」，实际不会执行；`CLP-R-0005` 因此无法进入活动规则包 | 属「展示状态与执行状态不一致」，改它等于改展示口径，超出行为保持范围 | 法规专员（杨工）+ 研发确认：LAYER 到底做不做，不做就把展示状态改成「需要研发实现」 |
| ② | `CLP_RULES[0001].run.thresholds` 的类别 4 上限 = 2000，而 `ate` 项 opts 里的文案写「ATE_mix > 2 000 mg/kg → 不分类」 | ATE_mix 恰为 2000 时按 `<=2000` 判为类别 4，>2000 才不分类；与 CLP Annex I 3.1.3.6 的官方区间（Cat.4 为 300–2000，>2000 视为无急性毒性）口径需核对 | 边界值判定可能与官方口径不一致 | 本阶段不做法规解释，只迁移现有逻辑 | 法规专员对照 Annex I 3.1.3.6 原文确认边界含不含等号 |
| ③ | `COMP_CLP['50-00-0'].haz.aquaticChronic = '2'` vs `CLP_VI_ROWS` 甲醛条目的统一分类（Carc.1B / Muta.2 / Acute Tox.3，未列水生慢性） | 组分基础数据里的水生慢性分类，在 Annex VI 统一分类里查不到对应条目 | 第 4 步水生危害分类的依据来源不明，审核时会被追问 | 属「同一物质在 `CLP_VI_ROWS` 与 `COMP_CLP` 中参数不一致」，需核实来源后才能动 | 法规专员核实该慢性分类的来源（实测 / Annex VI / 企业自评） |
| ④ | `CLP-M-GCL-SUM` 用同一套 `weight=10` 同时做皮肤与眼的加权 | 皮肤（Corr.1 vs Irrit.2）与眼（Dam.1 vs Irrit.2）在 Annex I 里的加权系数是否相同 | 若两者系数不同，眼刺激结论会偏严或偏松 | 需要对照 Annex I 3.2.3 / 3.3.3.3 原文，属法规解释 | 法规专员对照原文确认两套系数 |
| ⑤ | `CLP-M-SCL` 的 `skinSensGcl = 0.1%` | 皮肤致敏通用浓度限值 0.1% 是「Sens.1A/1B」还是「Sens.1」的口径，规则参数里未区分 | 阈值取错会导致该判的不判 | 同上，属法规解释且会影响演示结论 | 法规专员对照 Annex I 3.4.3 确认 |
| ⑥ | 引擎整体 | 四个方法的**法规正确性尚未经过专业核验**，当前输出只是「演示原型中已配置的计算基线」 | 不能直接用于真实合规判定 | 本阶段定位就是只迁移、不核验 | 后续法规专业复核阶段统一处理 |

> 上述 ①~⑤ 均**未在本阶段修改任何代码**，只做记录。

### 19.8 本阶段明确未做的范围

- `M-LIST` 名单匹配方法（留给后续阶段，设计基准里的第 5 个方法）
- ED / PBT / vPvB / PMT / vPvM 自动判断（`CLP-M-ED-PBT` 只登记为 not_implemented）
- Bridging 桥接原则（同上）
- 中国 GHS、运输法规、REACH 名单条件判断
- 法规原文结构化治理流程、单一事实源改造（`CLP_VI_ROWS` 与 `COMP_CLP` 未合并，`LAW_QUERY_ROWS` 未合并）
- 新增法规页面 / 重做法规查询页
- 规则变更生命周期、变化类型识别、内部审核与专业复核分流、测试与发布门禁（**下一阶段**）

### 19.9 有意偏离（已记录，非缺陷）

| 偏离 | 说明 |
|---|---|
| 分类项展示顺序由 `COMPLIANCE_ITEM_ORDER` 固定 | 按规则顺序执行时，GCL 一次产出 `skin`+`eye`，自然顺序会变成 `acuteOral/skin/eye/sens/aqua`。为保证「SDS 第 4 步展示」与标签要素顺序与重构前**逐字节一致**（已由 diff 证明），由适配器做一次稳定排序。**这是兼容层行为，不是算法依赖顺序** |
| 未实现方法也占一个版本号 `0.0.0-not-implemented` | 注册校验要求版本非空；用显式版本号避免「看起来像已发布」 |
| 执行器返回结构里保留了 `display.input` / `display.formula` 展示串 | 由执行器拼装（纯字符串操作，不碰 DOM），适配器拍平给页面。这样能保证与旧文案 1:1，代价是执行器里带了一点文案；后续单一事实源阶段可再抽离 |

### 19.10 下一阶段建议（**不实施**）

> 规则变更生命周期 → 变化类型识别 → 内部审核与专业复核分流 → 测试与发布门禁。

具体地说：法规版本升级时，先识别变化类型（阈值变化 / 新增条目 / 废止条目），按风险分流（自动复核 vs 必须专业复核），通过规则测试与发布门禁后才进入活动规则包，并对已发布 SDS 输出影响清单。

---

## 二十、第四十轮收口 · 合规模块阶段 2：CLP 规则版本生命周期（2026-09-22）

### 20.1 本阶段做了什么（一句话）

把 CLP Annex I 分类规则从「单一数组 + 写死的检查结果」升级为
**版本化规则集 + 增量 Diff + 发布门禁 + 单一法规专员审核发布 + 按日期解析活动版本**；
导入向导 Annex I 分支的第 4 / 5 步改为**系统基于内存草稿版本实时计算**，不再展示写死结论。

### 20.2 新增 / 修改的文件

| 文件 | 类型 | 职责 |
|---|---|---|
| `src/23z6c-js-clp-lifecycle.js` | **新增（约 700 行）** | 版本存储 `CLP_RULE_VERSION_STORE`、基线种子、归一化与双指纹、增量 Diff、引擎支持状态生成、规则测试与复用、发布门禁、Release Manifest、单一审核发布、按日期解析、活动版本投影 |
| `src/23z6-js-clp.js` | 修改 | `clpActivePack(asOfDate)` 支持按日期取规则；`clpEvaluateMixture(formula, asOfDate)` 支持传入投放日期；包内新增 `ruleSetVersion` / `asOfDate` 两个**新增**字段（既有字段与计算结果不变） |
| `src/23z6a-js-clp-import.js` | 修改 | 仅改 **Annex I 规则分支**：第 3 步形成真实内存草稿版本；第 4 步三个子 Tab 分别展示系统实时统计 + 引擎支持 / 参数校验、真实规则测试、字段级 Diff + 发布清单 + 暂不发布清单；第 5 步单一审核动作 |
| `src/23-js-sds.js` | 修改 | `buildClassItems()` 调用 `clpEvaluateMixture(wz.formula, wz.project.date)` —— 按 SDS 投放日期解析规则版本 |
| `smoke_clp_lifecycle.py` | **新增** | 生命周期自检 **72/72** |
| `smoke_clp.py` | 修改 | Annex I 分支断言按新设计重写（211→212 断言，未删断言、未降标） |
| `smoke_compliance_engine.py` | 修改 | 活动规则改为「改活动版本 + 同步投影」的写法（`CLP_RULES` 已不是事实源），仍 **91/91** |

### 20.3 版本数据结构与状态

版本对象（`CLP_RULE_VERSION_STORE` 元素）保存：`id / version / status / frozen / source{regulation,annex,sourceFile,sourceVersion,sourceDate} / createdBy,createdAt / reviewedBy,reviewedAt / publishedAt / effectiveFrom,effectiveTo,cutoff / supersedesVersionId / rules[] / diffSummary / gateSummary / releaseManifest / deferredRules / methodVersions`。

| 层 | 状态 | 由谁写 |
|---|---|---|
| 版本 | 草稿 → 校验失败 / 待补充 / 待研发实现 / 测试失败 → **待审核** → 待生效 / 已生效 → 已失效 · 已撤回 | 除「审核发布」外全部由系统计算 |
| 规则 | 未变化 / 可发布 / 待补充 / 待研发实现 / 测试失败 / 待确认停用 | **全部由系统计算**，法规专员不得填写 |
| 引擎支持 | 已支持 / 需要配置参数 / 需要研发实现 | **全部由系统根据阶段 1 方法注册表生成** |

**基线**：`clpRuleVersionSeedBaseline()` 幂等地把当前 `CLP_RULES` 冻结为 `R2026.2`（状态 已生效，生效日期取 `CLP_MODULES.rules.eff`），规则深拷贝；已发布版本 `frozen=true`，再改必须 `clpRuleVersionClone()` 复制为新草稿。

### 20.4 ⚠️ `CLP_RULES` 已降级为「活动版本兼容投影」

> **CLP_RULES 目前是活动版本兼容投影，不是历史版本存储。**

历史版本一律保存在 `CLP_RULE_VERSION_STORE`；发布后由 `clpSyncActiveRulesProjection()` 把活动版本规则深拷贝回 `CLP_RULES`，现有 CLP 页面无需改动即可继续工作。**写测试 / 改数据时不要直接改 `CLP_RULES`**——它会被下一次同步覆盖；要改就改活动版本的 `rules` 再同步（见 `smoke_compliance_engine.py` 的写法）。

### 20.5 Diff 规则

- 唯一标识 = 内部规则编号；候选版本 vs **当前活动版本**比较。
- 归一化：首尾空格、连续空格、空串与缺失等价；`id/ver/status/engine/test/checker/checkDate` 属生命周期字段，变化不算业务变化。
- 两个指纹：`clpRuleExecutionFingerprint()`（方法 / 参数 / 前置条件 / 例外 / 优先级 / 输出 / H 码 / 标签 / 适用对象）与 `clpRuleFullFingerprint()`（归一化全量）。
- 变化类型：新增规则 / 未变化 / 文案或来源变更 / 计算参数变更 / 适用条件变更 / 例外条件变更 / 方法映射变更 / 输出分类变更 / H 码或标签要素变更 / 规则优先级变更 / 生效日期变更 / 候选停用；多项并存时按优先级取主类型。
- **触发重新测试**：执行指纹变化。**可复用历史测试**：执行指纹不变 + 方法代码不变 + 方法版本不变 + 上版测试通过（页面显示「沿用上版测试结果（来源版本 X）」，文本差异仍照常展示）。
- 字段级差异一律用**业务字段名**（如「皮肤腐蚀阈值：5% → 3%」），页面不出现 `run.skinCorr` 这类内部路径。

### 20.6 发布门禁

| 检查项 | 通过条件 | 不通过的结果 |
|---|---|---|
| 版本号唯一 / 来源信息完整 / 生效日期有效且 ≥ 数据截止日期 | 版本级校验全过 | 版本级阻断，返回业务语言提示（如「版本 R2027.1 已存在，请修改版本号。」） |
| 方法代码登记 | `complianceHasMethod()` 为真 | 需要研发实现 → 延后 |
| 方法已实现 | `complianceMethodImplemented()` 为真 | 需要研发实现 → 延后（**上传文件伪造的「已支持」一律忽略**） |
| 参数完整性 | `CLP_METHOD_PARAM_REQUIREMENTS` 全齐 | 需要配置参数 → 延后（提示缺哪个业务字段） |
| 必填字段 / 来源条款 / H 码字典引用 | 均存在 | 待补充 → 延后 |
| 代表性测试用例 | 新增或执行指纹变化的规则至少 1 条 | 待补充 → 延后 |
| 规则测试 | 真实调用引擎执行后全部通过 | 测试失败 → 延后 |
| 候选停用 | 法规专员统一勾选确认 | 未确认 → **不停用**，旧规则继续有效 |

### 20.7 Release Manifest 与部分发布

`clpBuildReleaseManifest()` 输出 `publishableRules / unchangedRules / retainedOldRules / deactivatedRules / pendingDeactivationRules / deferredRules`。合并原则：

```text
当前活动规则集
+ 通过门禁的新增 / 修改规则
− 已明确确认停用的规则
= 新发布规则集
```

- **修改规则未过门禁** → 不用候选版本，保留旧版（标记「旧版继续生效」）；
- **新增规则未过门禁** → 不进活动规则集，存 `deferredRules`（标记待研发实现 / 测试失败 / 待补充）；
- 演示整理稿当前结果：可发布 **3**（R-0001 文案 / R-0003 例外+测试通过 / R-0004 公式描述）、旧版继续生效 **3**（R-0002 测试失败、R-0005/0006 方法未实现）、本次不发布 **5**、候选停用 **1**（R-0007，未确认）。

### 20.8 单一审核流程（**没有多级审核**）

第 5 步只保留一个动作：法规专员勾选 **「我已对照法规原文核对本次发布清单、参数变化与停用项。」** → `审核通过并发布`（有延后规则时按钮文案为 `审核并发布可用规则`，旁显「将发布 X 条，Y 条暂不发布」）。候选停用只有一个统一确认勾选框，未勾选则旧规则继续有效。
发布后：规则快照 + 方法版本快照 + Diff + 测试结果 + 发布清单 + 审核人 / 时间全部入版本，且 `frozen=true` 不可再编辑。
生效日期晚于演示当前日期 → **待生效**（当前活动版本不变、现有 SDS 不受影响）；否则 **已生效**，上一活动版本 → 已失效。

### 20.9 按日期解析与 SDS 调用

`clpRuleVersionResolve(asOfDate)`：状态 已生效 / 待生效 / 已失效 且 `effectiveFrom ≤ asOfDate < effectiveTo`（已失效版本靠 `effectiveTo` 保证「历史日期仍能解析到历史规则」）、未撤回 → 取生效日期最新者。
SDS 侧：`clpEvaluateMixture(wz.formula, wz.project.date)`；`clpActivePack(asOfDate)` 支持传日期，包内 `ruleSetVersion` 记录本次实际使用的规则版本。日期基准一律 `demoYmd()`（DEMO_TODAY），不依赖真实系统日期。

### 20.10 验证（逐项报告）

| 项 | 结果 |
|---|---|
| `smoke_clp_lifecycle.py` | **72 / 0** |
| `smoke_compliance_engine.py` | **91 / 0** |
| `smoke_clp.py` | **212 / 0** |
| `smoke_mfactor.py` / `smoke_sds.py` / `smoke_deliver.py` / `smoke_adjclass.py` | 63/0 · 0 错 · 100/0 · 24/0 |
| **全量 smoke** | **41 / 41 全绿** |
| 构建 | **25850 行 / script 48:48** |
| `check_names` | 1504 全局符号，**无新增重名** |
| `check_pages` | 无新问题 |
| `eval` / `new Function` | **无**（仅注释里出现字样） |
| **默认 SDS 行为保持** | 旧产物（阶段 1 `871e483`）vs 新产物逐字段 diff：**0 项差异**（items 顺序 / result / code / status / input / formula / opts / src + H 码 / 12 条 P 码 / GHS07 / warning + 活动规则 4 条） |

### 20.11 ⚠️ 本阶段新增的待专业核验项（只记录，未改法规结论）

1. 基线 `R2026.2` 中 `CLP-R-0006 / CLP-R-0007` 的 `ver` 字段写的是 **`R2027.1`**（未来版本号），但它们已经在现行规则表里 —— 版本号与规则归属不自洽，疑似历史演示数据遗留。
2. `CLP_METHODS` 展示字典把 `CLP-M-LAYER / CLP-M-BRIDGE` 标为「已支持 / 需要配置参数」，而引擎注册表里它们是 `not_implemented`；展示口径与引擎真实状态不一致（**本阶段以引擎为准**，展示字典留给后续治理）。
3. 演示整理稿把 `CLP-R-0002` 的皮肤腐蚀阈值由 5% 改为 3%（仅存在于**草稿版本** R2027.1，基线未动），该数值本身**未经专业核验**，只为演示「计算参数变更 → 测试失败 → 旧版保留」这条链路。
4. 阶段 1 遗留的 6 条待核验项（ATE 类别 4 边界、甲醛 Annex VI 缺统一分类、GCL 皮肤与眼共用权重、SCL 0.1% 对应类别等）本阶段**未处理**。

### 20.12 有意偏离（已记录，非缺陷）

| 偏离 | 说明 |
|---|---|
| 解析历史日期时把「已失效」版本也算作候选 | 需求 §19.2 要求新版本生效后旧版本置「已失效」，§20 又要求「历史日期仍能解析到历史规则版本」。两者直接冲突，故用 `effectiveTo` 界定有效期：查询日在有效期内仍可解析到它，之后不可。**这是让两条要求同时成立的唯一办法** |
| `待生效` 版本在 `effectiveFrom ≤ 查询日` 时也算活动版本 | 状态语义上它是「已发布有效」，否则按日期解析永远取不到未来版本（需求 §20 要求支持） |
| 演示整理稿里保留了两条「系统字段」（`engine` / `test`） | 这是**故意**留的：用于演示「上传文件不能伪造支持状态」，系统识别后忽略并提示，见 `ignoredUploadFields` |
| 第 4 步仍保留原有的静态校验表（7 类） | 需求要求第 4 步展示系统实时结果，但结构化表的格式校验（Index 重复 / H 码不存在…）在原型里仍是演示数据；实时结果以新增的「系统自动判断」区块与测试 / 差异两个子 Tab 呈现，两者不混为一谈 |

### 20.13 下一阶段建议（**不实施**）

> 统一 CLP 物质分类、SCL、ATE、M 因子的数据源，
> 消除 `CLP_VI_ROWS`、`COMP_CLP` 和法规查询结果之间的不一致。

---

## 二十一、第四十一轮收口 · 外部复核（Codex）4 项修复（2026-09-23）

> 起因：Codex 复核第四十轮（阶段 2）代码后提出 4 个修复点。**逐项实证，4 项全部成立**，非误报。
> 流程：先复现 → 再修 → 再补断言。**未删任何既有断言、未降低标准**。

### 21.1 四个问题与修复前实测结果

| # | 问题 | 实测证据（修复前） |
|---|---|---|
| P1-1 | 规则版本何时生效，被 SDS 的计划投放日期左右 | 同一 `effectiveFrom=2026-10-01`：投放日=系统今天 → 「待生效」✔；投放日=未来 `2027-06-01` → 「**已生效**」，基线被置「已失效」、活动版本变成新版本 ✘ |
| P1-2 | 一次停用多条规则会删错 | 停 `[R-0004,R-0005]` → 剩 `[R1,R2,R3,R5]`（**R5 漏删**）；停 `[R-0002,R-0004]` → 剩 `[R1,R3,R4]`（**R5 误删、R4 漏删**） |
| P2-1 | 规则包上的版本信息跨版本混用 | 未来日期取包：`ruleSetVersion=R2099-N`，但 `modules.rules=R2026.2` / `effectiveFrom=2026-06-01` / `id=CLP-EU-ATP22-R2026.2-L2026.3` |
| P2-2 | 导入草稿覆盖候选草稿 | 候选草稿原 `rules=[R-0006,R-0007]`；走一遍导入向导（版本号默认 `R2027.1`）后，草稿**就是候选草稿对象本身**，其 `rules` 变成导入整理稿、**R-0007 消失** |

### 21.2 修复后是什么效果

| 改动 | 改完的效果 | 落点 |
|---|---|---|
| 日期分离 | 规则版本「什么时候生效」只由**系统日期**说了算。把 SDS 的计划投放日期填成未来 / 过去 / 等于今天，发布同一个版本得到的结果完全一致；SDS 侧仍按投放日期匹配当时生效的规则版本（这条本来就对，保留不动） | `23z6c` 新增 `clpSystemToday()`，原 `clpRuleAsOfDate()` 整体退役；`23z6a:822`、`23z6:193` 同步 |
| 批量停用 | 一次停用几条就只移除这几条，其余规则原样保留（相邻两条 / 不相邻两条 / 三条均已验证） | `23z6c` 的 `clpRuleVersionPublish()` 改为「先收集待删 id → 一次 `filter`」 |
| 包版本信息同源 | 规则包上写的版本号、生效日期、包编号，跟这个包**实际装的那版规则**一致：按历史日期取包就显示历史版本号，取未来版本就显示未来版本号 | `23z6` 的 `clpActivePack()`；Annex VI 与标签字典尚未版本化，仍用模块当前值（注释已写明） |
| 候选草稿保护 | 候选草稿（下一版规则暂存区）与导入草稿互不干扰：导入向导走完后候选草稿原封不动，导入草稿是另一个独立对象；同一版本号重复上传只复用同一个导入草稿，不会越建越多 | `23z6c` 新增 `clpRuleVersionReserved()`，`clpRuleVersionCreateDraft()` 与 `clpRuleVersionValidate()` 的唯一性判定均排除候选草稿与基线快照 |

### 21.3 验证

| 项 | 结果 |
|---|---|
| `smoke_clp_lifecycle.py` | **95 / 95**（原 80，**+15 条**：12.1 日期分离 3 / 12.2 批量停用 3 / 12.3 包版本信息 4 / 12.4 候选草稿保护 4 / 12.5 重复上传 1） |
| `smoke_clp.py` | **216 / 0**（4 处断言由「按版本号取第一个」改为「按版本号取非候选的那个」—— 修复后同版本号会同时存在候选草稿与导入草稿两个对象） |
| `smoke_sds.py` | **0 错误**（「规则包快照」探针由 `CLP_MODULES.rules.ver` 改为 `CLP_MODULES.vi.ver`：规则集版本已改由版本库决定，改模块字段不再影响包编号，故换一个仍然有效的维度守住同一语义） |
| `smoke_compliance_engine.py` | **91 / 91** |
| **全量** | **41 / 41 全绿** |
| 构建 | **25983 行 / script 48:48**；`check_names` 无新增重名；无 `eval` / `new Function` |

### 21.4 夹具升级（写测试前必读）

`smoke_clp_lifecycle.py` 新增 **`LF.resetReal()`** = 基线 + 候选草稿共存，与页面真实加载后的版本库一致。
原 `LF.reset()`（只 seed 基线）保留给历史分组用，但**凡涉及「导入草稿 / 候选草稿」的断言必须用 `resetReal()`** —— 用 `reset()` 等于先把要测的东西清掉，bug 永远测不出来（这正是 P2-2 藏了一整轮的原因）。

### 21.5 有意保留的现状（非缺陷）

- 候选草稿**不参与版本号唯一性判定**，因此理论上可以有两个同为 `R2027.1` 的版本对象（一个候选草稿、一个导入草稿），靠 `isCandidate` 与状态区分。这是刻意的：候选草稿是「下一版规则暂存区」，不代表一个真实发布的版本号。
- Annex VI 与标签字典在阶段 2 尚未做版本化存储；Annex VI 已在阶段 3 完成，见 §22。标签字典仍取模块当前值。

---

> **下一步**：沿用 §15.4 的排队顺序 —— **N8（名单命中方法 M-LIST）→ N3 / N4 → N1 → N2 / N5 / N7 → N6**。

---

## 二十二、第四十二轮收口 · 阶段 3：CLP 物质数据单一事实源（2026-09-23）

### 22.1 文件与职责

| 落点 | 当前职责 |
|---|---|
| `src/23z6d-js-clp-substance-data.js` | 唯一 CLP 物质数据存储：ATP 22 官方演示快照、企业补充种子、按日期解析、统一 profile、provenance、冲突、兼容投影；由 `build.sh` 装入原型 |
| `src/23z6-js-clp.js` / `src/23z6a-js-clp-import.js` | Annex VI 页面按选定快照显示；导入向导从演示结构化草稿算 Diff，并在单一法规专员审核后发布新数据集；不解析真实 XLSX |
| `src/23-js-sds.js` / `src/23z6b-js-clp-engine.js` | 组分库编辑写补充层；SDS 与引擎按投放日期直接读取 `clpSubstanceProfile()`，第 3 步 CLP 项由 `clpCollectItems()` 动态汇集 |
| `src/23y-js-law-query.js` | 非 CLP 查询行保留静态；CLP 行及 `LAW_DETAIL.clp6` 从当前官方快照生成 |
| `smoke_clp_data_source.py` | 覆盖数据分层、来源、冲突、编辑保存、历史/未来版本及跨页面同步 |

详细结构、API 和维护边界见 [CLP物质数据单一事实源.md](CLP物质数据单一事实源.md)。

### 22.2 合并与迁移口径

官方 `officialDatasets` 按版本保存 Index No.、CAS / EC、分类与 H 码、EUH、象形图、信号词、结构化 SCL / 分途径 ATE / 急慢性 M 因子；已发布快照深度冻结，历史版本不被覆盖。企业 `supplementalByCas` 保存原 `COMP_CLP` 八个 CAS 的补充分类、毒性数值与状态、来源修订；二者不混称为官方值。`clpSubstanceProfile(cas, asOfDate)` 的 `effective` 先取官方已有的同类别统一分类及 SCL / ATE / M，再以企业数据补齐官方未给出的类别/字段；`unknown` 保持未知，`na` 为经确认不适用。`provenance` 按字段记来源版本与依据；`conflicts` 记双方值、计算取用值、来源和待核验状态，相关未解决冲突会阻断对应方法自动分类。

甲醛 `50-00-0` 的慢性 M 因子是**待专业核验冲突**：ATP 22 演示官方值 10，阶段 2 旧计算值 0。迁移期 `legacyResolutions` 只针对该已知字段与 ATP 22 明示沿用旧值 0，以保证默认 SDS 行为；用户重新维护 M 因子后例外删除，不自动做法规裁决。

`CLP_VI_ROWS` / `COMP_CLP` 现为只读兼容投影，不得直接写；查询 CLP 行来自 `clpLawQueryProjection()`，`LAW_DETAIL.clp6` 来自 `clpLawDetailProjection()`，`COLLECT_MOCK` 不再持有 CLP 分类事实。组分页官方数据只读，企业补充分类、SCL、ATE、M 因子可结构化编辑；保存后各入口重新读取统一画像。SDS 第 3 步的 CLP 分类、H 码、Annex VI 命中、SCL、ATE、M 均动态生成，演示一键填充只处理非 CLP 数据。

### 22.3 日期、预期变化与边界

规则包与物质数据集均按同一个 `asOfDate`（SDS 投放日期）解析。发布 ATP 23（2027-02-01 生效）后，当前日期仍用 ATP 22；选择 ATP 23 可查看 6 条未来快照，历史 ATP 22 的 5 条保留。当前查询页由 18 条增至 **19 条**，新增甲苯的动态 Annex VI 命中；非 CLP 查询数据不变。默认演示配方的计算项、标签候选与中间量对阶段 2 基线逐字段 **Diff 0**。

本阶段未做真实 XLSX 解析、法规专业裁决、C&L Inventory 合并、`M-LIST`、REACH / OEL / 运输法规取数。下一步沿既定队列推进 `M-LIST`；上述甲醛 M 因子冲突须先由法规专业人员核验，不能靠代码自行抹平。

### 22.4 验证与协作提醒

运行 `bash build.sh`、`python3 smoke_clp_data_source.py`、阶段 1 / 2 与 SDS / 法规查询专项 smoke、`python3 check_names.py`、`python3 check_pages.py`、`bash run_smoke.sh`，以及源码 `eval` / `new Function` / 兼容投影写入检查。验证数值以本阶段最终提交说明为准。根目录 `README.md`、`.gitignore` 与 `integrations/github-mcp-server/` 存在其他协作者未提交修改，本阶段不碰、不纳入提交。

---

## 二十三、阶段 4A · 最小 M-LIST 名单匹配内核（2026-09-23）

### 23.1 文件与方法

`src/23z7a-js-mlist.js` 在 `23z7-js-reach.js` 后加载，提供 `REGULATORY_LIST_DATASETS`、`listRegisterDataset()` / `listGetDataset()` / `listGetDatasets()` / `listResolveDataset()` / `listDatasetAvailable()`、`listExecute()` 及 REACH 适配器。统一方法注册表新增 `M-LIST`，版本 `1.0.0-demo`，领域 `LIST`，状态 `implemented`；不加入 `CLP_RULES` 或 CLP 活动规则包。通用执行入口 `complianceExecuteMethod()` 仅对 M-LIST 透传 `datasetVersions`、`entryResults` 和 `summary`，原四个 CLP 方法及输出不变。

输入为 `{asOfDate,datasetKeys,components,context}`：组分给 CAS / EC、可选浓度、单位和浓度基础；上下文给目标市场、对象类型及用途类别。输出保留方法代码与版本、方法级状态、数据集版本、逐条结果、六种状态计数、输入快照、身份命中中间值、来源证据和消息。方法级 `AUTO` 表示匹配执行完成，`NO_MATCH` 表示可用数据集内无身份命中，`NEED_INPUT` 表示已命中但条件缺信息，`BLOCKED` 表示有数据集不可用；未登记方法仍由统一入口返回既有 `UNSUPPORTED_METHOD`，异常返回 `ERROR`。数据集不可用绝不作为“未列入”。

### 23.2 名单事实与条件判断

当前仅注册 `reach-svhc` 和 `reach-xvii`，版本、来源与条目内容从现有 `REACH_MODULES`、`REACH_SVHC`、`REACH_XVII` 适配生成，不新增第二份法规事实。登记时检查 key、版本、来源、市场和条目 ID，不允许重复注册。按查询日期判断数据集及条目是否生效。身份只按标准化后的 CAS / EC 精确匹配，名称仅展示；没有 Annex VI 记录也可命中名单。Annex XVII 的多 CAS 物质组保留原始说明，但本阶段不拆作单物质命中。

**列入名单 ≠ 当前产品触发条目。** 现有 SVHC 的 0.1% 文案，以及 Annex XVII 的材质、用途、限值、检测和豁免均为自然语言演示资料，尚无完整结构化条件；真实适配条目均返回 `LISTED_ONLY`，不解析文字、不判断禁用或整体合规。简单条件执行能力由仅存在于 `smoke_mlist_core.py` 浏览器会话中的 `engine-test-fixture` 验证，不进入法规页面或业务数据。

| 条目状态 | 含义 |
|---|---|
| `LISTED_ONLY` | 身份列入，暂无可执行的完整结构化条件 |
| `TRIGGERED` | 已配置简单条件全部满足 |
| `NOT_TRIGGERED` | 已完整执行简单条件，当前未满足 |
| `NEED_CONTEXT` | 缺浓度、用途、对象等，或单位 / 浓度基础不一致 |
| `NOT_APPLICABLE` | 市场或对象类型明确不适用 |
| `DATASET_UNAVAILABLE` | 数据集当前无可用版本，不能判断未列入 |

已实现的简单条件字段为目标市场、对象类型、用途类别（`in`）及浓度（`>`、`>=`、`<`、`<=`、`=`）。浓度只在单位和基础完全一致时比较，不做 `% / ppm / mg/kg` 或配方 / 均质材料浓度换算。结果逐条给出匹配组分、命中方式、条件过程、所需输入、原文与来源；不执行自然语言解析、豁免或授权判断，也不做物质组聚合。

### 23.3 行为保持、验证与后续

本阶段未修改 SDS 六步向导、第 3 / 4 / 15 章、法规统一查询、CLP 或 REACH 页面，也未让这些页面调用 M-LIST。改造前后相同默认配方的分类顺序、结果、代码、状态、H / P 码、象形图、信号词与计算中间值逐字段比较 **Diff 0**；法规查询数据和查询 / CLP / REACH 页面 HTML 快照 **Diff 0**。`smoke_mlist_core.py` 对构建产物中的真实 JavaScript 执行 49 条断言，覆盖注册、真实适配、CAS / EC、六种状态、比较边界和无隐式换算。

待法规专业核验：现有 REACH 演示条目的名单事实、版本日期、阈值文案、用途范围、检测口径和豁免说明；核验前不将这些文字升格为自动条件。阶段 4A 不接 Annex XIV、RoHS、ZDHC、国内危化品目录，不做物质组聚合、豁免、授权、SDS 页面、统一查询页或第 15 章接入。

**下一任务仅为阶段 4B：**把其余名单库接入 M-LIST，再统一接入法规查询页和 SDS 第 4 / 15 章。

---

## 二十四、阶段 4A 收尾 · M-LIST 部分覆盖状态（2026-09-23）

`listExecute()` 增加可快照保存的 `coverage:{requested,evaluated,unavailable,complete}`。三个数组按请求顺序去重；输入中的原 `datasetKeys` 保持不变。已注册的数据集可设 `available:false` 及 `unavailableReason`；元数据仍可读取，但解析、执行和“未列入”判断均不可用。每个不可用库保留独立的 `DATASET_UNAVAILABLE` 结果和原因。

方法级状态按覆盖程度判断：未指定名单库为 `NEED_INPUT`；全部不可用才 `BLOCKED`；有可用库且存在 `NEED_CONTEXT` 为 `NEED_INPUT`；有可用库并命中为 `AUTO`。可用库无命中时，只有覆盖完整才为 `NO_MATCH`；部分覆盖为 `AUTO` 并明确提示不能形成全库“未列入”结论。局部不可用不抹掉已获得的名单结果。

`smoke_mlist_core.py` 已扩展至 56 条断言，覆盖可用/不可用组合、覆盖顺序与去重、原请求不变、原因留痕和全部不可用门禁。本节修正 §23.1 中“有数据集不可用即 BLOCKED”的阶段 4A 原始口径。

---

## 二十五、阶段 4B-1 · 名单适配与统一法规查询（2026-09-23）

### 25.1 数据来源与边界

`src/23z8a-js-list-query.js` 在 `23z8-js-law-misc.js` 后加载，负责名单适配和查询投影；M-LIST 核心仍在 `23z7a`。八个业务数据集已登记，其中六个可用、两个仅有不可用元数据：

| 数据集 | 唯一事实来源 | 状态与当前能力 |
|---|---|---|
| `reach-svhc` | `REACH_SVHC` / `REACH_MODULES.svhc` | 可用；仅列入 |
| `reach-xiv` | `REACH_XIV` / `REACH_MODULES.xiv` | 可用；仅列入，不判断授权 |
| `reach-xvii` | `REACH_XVII` / `REACH_MODULES.xvii` | 可用；仅列入，不判断产品限制触发 |
| `eu-rohs-annex-ii` | `ROHS_ITEMS` / `ROHS_VER` / `ROHS_SRC` / `ROHS_LIMIT_NOTE`；生效日取现有 `lawRows.rohs2` | 可用；仅列入，不比较均质材料浓度 |
| `zdhc-mrsl` | `LAW_DETAIL['zdhc-mrsl']` / `lawRows` | 可用；仅列入，不解析限值文字 |
| `cn-danger` | `LAW_DETAIL['cn-danger']` / `lawRows` | 可用；仅列入，不解析含量备注或执行 GB 30000 |
| `cn-prohibited-import-export` | 尚无结构化条目 | `available:false`，原因“本期尚未建立结构化数据集” |
| `cn-toxic-chemicals` | 尚无结构化条目 | 同上 |

明细型适配器按 `LAW_DETAIL.cols` 的列名找字段；缺少可选列时留空。全部真实名单条目均为 `listed-only`，`conditions` 为空。无单一 CAS / EC 的 PBB、PBDE 物质组仍保留在查询页，可按名称检索，但不会通过名称自动匹配配方。C&L Inventory 和 OEL 不属于 M-LIST。

### 25.2 查询投影与页面

`listLawQueryProjection(asOfDate)` 从已登记且当日可用的名单数据集生成查询行，ID 由数据集 key、版本、条目 ID 编码而成，不依赖数组位置。投影保留 CAS / EC、条目编号、名称、原文阈值 / 用途 / 豁免 / 摘要、版本、来源及维护路由；无法执行的名单不生成普通命中行。`src/23y-js-law-query.js` 的 `LAW_QUERY_REFERENCE_ROWS` 只保留三条 C&L Inventory 参考行，移除原 11 条手工名单行；CLP 的五条 Annex VI 查询行仍由原动态投影提供。当前统一查询为名单动态 **39** + CLP 动态 **5** + C&L 参考 **3** = **47 行**（原 19 行）。六个名单库分别为 SVHC 8、Annex XIV 6、Annex XVII 8、RoHS 10、ZDHC 4、国内目录 3 行。

查询页只陈述“物质被名单列入”，不据此判定当前产品触发限制或整体合规。页面提示只出现一次；名单详情展示条目原文，并在底部提醒该结果不是产品最终限制判断。列表和详情的来源库按钮优先使用每行 `maintenanceRoute`：REACH 三库到 `law:reach`、RoHS 到 `law:rohs`、ZDHC 到 `law:zdhc`、国内目录到 `law:cn`。法规类别筛选与投影 `lawKey` 一致。

### 25.3 回归和后续

本阶段未修改 SDS 向导及第 3、4、15 章，也未让 SDS 调用 M-LIST。4B-1 前后相同默认配方的 SDS 计算快照、CLP 数据及页面、REACH 和 RoHS 维护页 HTML 快照均 **Diff 0**；统一查询页发生上述预期变化。`smoke_mlist_query.py` 对构建产物验证注册、来源、无 CAS 物质组、投影稳定性、页面筛选、详情和跳转。现有法规演示事实、版本、阈值、用途、豁免及甲醛 CLP M 因子冲突仍需法规专业人员核验，本阶段未改写这些事实。

下一任务仅为**阶段 4B-2**：将 M-LIST 接入 SDS 数据准备、法规结论和第 15 章，并在配方、市场、日期变化后使名单结果失效与重算；授权、豁免、物质组聚合和完整 evaluation snapshot 仍需另行设计。禁止进出口目录与有毒化学品名录的真实数据尚未建立。

---

## 二十六、阶段 5A · SDS 统一合规评估与草稿快照（2026-09-23）

### 26.1 调用合同和文件职责

阶段 4B-2 已并入本阶段。新增 `src/23z8b-js-compliance-evaluation.js`，由 `build.sh` 按文件名在名单查询适配器之后、OEL 模块之前装入。公开入口为 `complianceEvaluationInputFromWz()`、`complianceEvaluateDraft(input)`、`complianceEvaluationEnsure()`、`complianceEvaluationCurrent()`、`complianceEvaluationInvalidate(reason)`；`complianceListDatasetKeysForMarket(market)` 负责选择名单。`src/23-js-sds.js` 只负责向导状态、主动失效和三个页面接入，`src/23z4-js-sds-sections2.js` 的第 15 章改为读取当前评估结果。

输入合同为 `{asOfDate,product,formula,context,datasetKeys}`：产品含名称、物料编码、纯物质/混合物类型、市场、目标国家/状态和语言；组分含 CAS、EC、名称、浓度、单位、计量基础、保密标记；上下文含对象类型、用途类别、产品类别、材质类型。`wz` 到合同、合同到执行均深拷贝，浓度沿用配方百分比，不修改原配方。空白上下文合法，实际名单条件可返回 `NEED_CONTEXT`。欧盟请求 SVHC、Annex XIV、Annex XVII、RoHS Annex II、ZDHC；中国请求国内危化品、ZDHC、禁止进出口目录、有毒化学品名录。后两库目前不可用，仍进入覆盖信息。

一次 `complianceEvaluateDraft()` 用同一次 `clpEvaluateMixture()` 结果构造向导分类项、CLP 中间执行、标签、规则包和证据，再调用 `M-LIST`。`buildClassItems()` 保留原无参入口，新增可选预计算 run。名单只按 CAS / EC 身份精确匹配，不复用查询页名称搜索；当前真实条目只有名单列入语义，原文阈值、用途和豁免不被解析为自动条件。

### 26.2 草稿 Evaluation Snapshot 与失效

快照 `schemaVersion=compliance-evaluation-v1`、唯一 ID、`status=draft`、评估时间和 `asOfDate`。`inputSnapshot` 保存完整输入副本；`results.classification` 保存分类项、引擎项、规则包、执行过程、标签和警告，`results.lists` 保存方法状态、逐条结果、计数、覆盖、数据集版本与消息；`evidence` 分别保存 CLP 计算和名单匹配证据；`warnings`、`readiness` 汇总待判断、覆盖不全、上下文缺失、数据集不可用和执行错误。对象可 JSON 序列化，运行时递归冻结；从本地草稿恢复后重新冻结。现有 `PERSIST_SCHEMA=v1` 无需升级，旧草稿无快照时可新建。

`versions` 记录：同一投放日期解析的 Annex VI 版本、逐组分企业补充 revision 和有效数据、仅本次请求的名单版本及可用性；CLP 规则包 ID、规则集版本与规则编号/版本；本次包中的 CLP 方法和 `M-LIST` 方法版本；规则包引用的标签版本；从 `REACH_SDS_CH[].det.tpl` 校验并取得的统一模板版本。输入指纹是规范化输入与上述版本信息的序列化值，覆盖产品名称、编码、类型、市场、目标状态、日期、组分身份/顺序/浓度/保密标记及名单上下文。任一输入或版本变化会产生新快照，旧快照不就地修改。

配方新增、编辑、删除、导入、演示填充、冻结/解冻，市场、日期、产品字段和名单上下文变化，以及组分补充分类保存，均主动标脏。`complianceEvaluationEnsure()` 每次还重新计算指纹，兜住绕过 UI 的变化；同一输入与版本复用当前快照。快照只属于草稿，历史发布文档冻结留到阶段 5B。

### 26.3 SDS 页面、状态语义和验证

第 3 步新增“法规名单数据准备”：已检查库数、命中条目数、待补上下文数、暂不可用库数，并在覆盖不完整时明确提示。第 4 步在 CLP 分类之外新增“法规列入情况”，显示名单、版本、条目、命中组分、列入及当前判断、阈值/用途/豁免原文；M-LIST 方法、输入、匹配、原文和覆盖证据默认折叠。第 15 章从当前快照生成名单表，保留“名单列入不等同于当前产品已触发限制”声明。现有 Word/PDF 草案沿原第 15 章渲染路径读取当前内容，导出数据源和发布冻结机制未改。

`LISTED_ONLY` 仅确认身份列入，尚无产品级限制判断；`TRIGGERED` / `NOT_TRIGGERED` 只适用于已配置的简单条件；`NEED_CONTEXT` 表示缺少判断信息；`DATASET_UNAVAILABLE` 表示未建立可执行数据集，不能解读成未列入。覆盖不完整会提示但不阻断原六步向导，执行异常单独标示，不输出产品整体合规结论。保留原 CLP 阈值和结果、统一查询页面语义与当前日期口径；SDS 按计划投放日期评估。

本阶段以 `9e0923f` 为行为基线：CLP 分类/标签/中间值 Diff 0；统一法规查询行及 HTML Diff 0；SDS 第 1～14、16 章 Diff 0。第 15 章预期从手工产品结论转为逐条动态名单结果、版本及覆盖说明。自动化见 `smoke_compliance_evaluation.py`，同时保留阶段 1～4 与 SDS/导出的 smoke。仍未实现 OEL、第 8 章、中国 GHS、运输、Annex XIV 授权、豁免、物质组聚合、单位或均质材料浓度换算、法规原文解析、真实 XLSX、整体合规结论以及发布后历史文档冻结。

**下一任务：阶段 5B**，设计发布前确认、快照冻结与历史文档重建边界。

---

## 二十七、阶段 5A 收尾 + 5B · SDS 发布快照（2026-09-24）

### 27.1 草稿建议、最终决定与发布结果

新增 `src/23z8c-js-sds-release.js`，按构建文件名紧随 `23z8b` 加载。原 `compliance-evaluation-v1` 仍保存系统最初 CLP/M-LIST 结果与证据，不因人工采纳或改判而就地修改。发布时另建不可变的 `sds-release-v1`：`evaluation` 是完整原始评估副本；`review.finalClassItems`、`classificationDecisions`、`classAdjust`、`euhSelections`、`finalLabels` 记录最终分类、H/P 码、象形图、信号词、EUH、系统原结果、人工最终结果、理由和决定时间；`document.bodyHtml` 是当时的封面及 16 章正文。审核人复用第 6 步现有“EHS 负责人”身份口径，没有新增审核表单或审批层级。

发布入口 `sdsReleaseCreate(reviewInfo)` 在原第 6 步“模拟批准发布”确认后调用；提交审核、分类无待判定项、文档版本非空且未发布过、原评估结构及 16 章正文完整是创建前校验。`LISTED_ONLY`、`NEED_CONTEXT`、数据集不可用和覆盖不完整仍为提示，不新增阻断。成功后一次性追加 `wz.releaseSnapshots`、更新 `activeReleaseId`、`published` 和 `publishedAt`；失败不产生发布记录。可通过 `sdsReleaseList()`、`sdsReleaseGet(id)`、`sdsActiveRelease()`、`sdsReleaseBodyHtml()` 程序接口查阅，页面仅有第 6 步简洁信息卡，没有历史版本中心。

同一文档版本不能重复发布；不同版本可追加，旧对象递归冻结且不会被新版本修改。快照保存原评估内的数据、规则、方法、标签与模板版本，另保留来源评估 ID、输入指纹、文档版本、发布与审核时间。`wzSnapshot()` 沿原 `PERSIST_SCHEMA=v1` 序列化整个 `wz`；旧缓存缺少发布字段时补空数组与空活动 ID，恢复后通过 `sdsReleaseFreezeStored()` 重新冻结；重置演示数据清空发布历史。

### 27.2 正式预览与 Word 的唯一正文来源

`src/23z2-js-sds-export.js` 将原动态生成拆成 `sdsDraftBodyHtml()` 和 `sdsEffectiveBodyHtml()`：未发布读当前草稿；已发布且有活动快照时直接返回 `release.document.bodyHtml`，`sdsDocBodyHtml()` 保留兼容入口。创建新发布版本时明确调用草稿函数，并只将第 16 章的状态标记设为“已发布”，避免复制上一版发布正文或在正式正文中保留“草案”字样。第 5 步交付预览在已发布状态不再预先生成隐藏的草稿手风琴或重新扫描动态章节；正式 Word 正文和文件名分别读取活动发布快照的冻结正文与文档版本。第 6 步原 Word 按钮接入既有真实 Word 下载函数；PDF 仍沿原演示路径，没有新增渲染引擎。

发布后即使当前配方、人工分类、EUH、草稿章节，或 Annex VI、CLP 规则/方法、M-LIST、名单、标签、模板与组分补充数据发生变化，正式预览和 Word 不回读这些实时来源。当前草稿评估仍可独立重算。第 6 步信息卡只展示版本、发布时间、审核人、快照 ID、CLP 数据/规则及模板版本，并说明后续升级不会改写本次发布内容。

### 27.3 验证与后续边界

新增 `smoke_sds_release_snapshot.py`，在真实单文件原型里验证人工决定、发布前校验、冻结、正式预览、Word、重复版本、多版本历史、持久化和旧草稿，专项 **38/38**。以 `891080a` 为基线，未发布状态的 CLP、M-LIST、统一查询行与 HTML、16 章草稿文本及完整草稿正文均 **Diff 0**。已发布状态冻结测试覆盖法规/规则/方法/标签/模板及实时草稿变化；全量 **46/46** 脚本通过，构建 **27008 行 / script 53:53**，`check_names.py` 无新增重名。

本阶段未建设文档版本中心、快照 Diff/回滚、电子签名、多级或外部审批、服务端文档库，也未接入 OEL 第 8 章、中国 GHS、运输规则、Annex XIV 授权、豁免、物质组聚合或真实 PDF。

**后续状态：**阶段 6 已于 2026-09-24 完成，现行边界以 §二十八为准。

---

## 二十八、阶段 6 · 合规管理原型封板（2026-09-24）

### 28.1 OEL 第 8 章

新增 `src/23z9a-js-oel-sds.js`，以 `OEL_SOURCES`、`OEL_SETS`、`OEL_LIMITS` 为唯一事实源。`oelSdsResolveSet(market,asOfDate)` 仅选目标市场中已发布、日期有效且未过期的数据集，取生效日期最新版本；默认欧盟 2026-10-01 为 `DS-EU-2601 / V2026.1`，中国为 `DS-CN-1901 / 2019 版`。`oelSdsEvaluate()` 按配方 CAS 返回原始限值类型、长期/短期/上限值、单位、皮肤/致敏、生物监测、来源记录与缺失组分。只做类型语义对齐，不换算 ppm/mg/m³，不比较严格性，不合并两市场。缺记录标“当前有效 OEL 数据集未维护该组分限值”，数据集不可用另有明确状态；两者只警告，不阻断发布。正式版需维护官方全量数据。

`src/23z-js-sds-sec.js` 已删除独立 `OEL_DATA`，第 8 章读取 Evaluation Snapshot 中的 OEL 结果，列出当前有效版本与生效日期；第 3 步 OEL 项也改从该数据集读取，不提供 SDS 内手工覆盖。旧 `COLLECT_MOCK` 的 OEL 示例值及补录建议已移除。`compliance-evaluation-v2` 增加 `versions.data.oel`、`results.oel`、`evidence.oel` 和 readiness 统计；旧 v1 草稿评估在 `complianceEvaluationEnsure()` 自动重算，`PERSIST_SCHEMA` 不变，旧 `sds-release-v1` 正文不迁移。发布时第 8 章正文连同完整评估结果冻结。

### 28.2 双市场分类边界

`complianceClassificationFramework()` 分发：EU 为 `EU_CLP` 自动路径，继续使用 Annex VI、现有规则包、四种计算方法和 M-LIST；CN 为 `CN_GHS` 的 `manual-required / rule-pack-unavailable` 路径，不调用 `clpEvaluateMixture()`，不保存 CLP 规则包或欧盟系统建议。中国分类项目仅用 ID、名称与顺序目录建立空白人工待判定基线，法规人员填写最终分类、理由和时间；没有 pending 项后可进入草案。没有经专员核验发布的 GB 30000 规则包，故不复制 CLP 阈值冒充中国自动计算。中国第 3 章不展示欧盟 CLP 分类参数，第 2 章不按 CLP Annex IV 自动推导标签；国内危化品目录仍由 M-LIST 返回，两个未建立的国内名单保持 `DATASET_UNAVAILABLE`，中国 OEL 选 GBZ 2.1。第 4 步集中展示一次演示范围声明，技术证据默认折叠。

### 28.3 运输第 14 章与发布快照

新增 `src/23z8d-js-transport-assessment.js`，在第 5 步第 14 章原位置维护人工结论，状态为 `NOT_ASSESSED`、`NOT_REGULATED`、`REGULATED`、`STALE`。非危险货物需依据和确认人；危险货物另需 UN 编号、正确运输名称、危险类别，其他字段可选。指纹覆盖市场、产品、类型、日期、CAS、浓度和配方顺序；相关输入变化标失效，保留旧内容供重新核对。未评估/失效只显示需人工确认，不产生默认“非危险货物”或虚构 UN 数据。运输法规库 `law:trans` 仍是规划占位，没有自动运输规则。

`src/23z8c-js-sds-release.js` 在发布前阻断 `NOT_ASSESSED` 和 `STALE`，并将完整人工运输结论保存为 `release.review.transportAssessment`；名单部分覆盖和 OEL 缺记录仍只提示。发布时第 14 章正文与 OEL 第 8 章一起进入冻结的 `document.bodyHtml`。已发布预览和 Word 继续只读发布快照，当前 OEL、运输、法规、配方、市场、模板或草稿变化均不改写历史正文；旧发布记录缺运输字段时仍可读。

### 28.4 端到端验收与封板范围

`smoke_compliance_final.py` 在真实构建产物上通过 **46/46**：EU CLP + M-LIST + EU OEL + 人工运输 + 16 章 + 审核发布 + Word；CN 国内名单 + GBZ 2.1 + 人工分类 + 人工运输 + 发布正文；另覆盖数据集版本排除、缺失、运输四态/门禁与发布冻结。以 `beb675e` 为基线的构建产物逐字段比较：**EU CLP Diff 0、M-LIST Diff 0、统一查询 Diff 0、非目标 SDS 章节 Diff 0**。预期变化为中国分类由错误的 EU CLP 自动结果改为 CN_GHS 人工模式、第 8 章读取官方数据集示例、第 14 章由硬编码结论改为人工结论。构建 **27165 行 / 72 分片 / script 55:55**，`check_names.py` 检查 **1634** 个全局符号，无新增重名；菜单 **11** 个一级、**50** 个叶子、注册页 **69** 个，向导仍六步。全量 **47/47** smoke 全绿。

已完成演示能力：法规数据维护、CLP 规则版本与门禁、欧盟混合物计算、名单匹配与统一查询、OEL 第 8 章、SDS 统一草稿评估、人工审核、发布快照冻结、Word 导出、中国人工分类路径、运输人工路径。未实现：全量中国 GHS 自动计算、全球 OEL、自动运输分类、全量 UN 数据库、禁止进出口目录和有毒化学品名录真实数据、物质组自动聚合、Annex XIV 授权及豁免自动判断、真实 PDF 引擎、服务端文档库、电子签名和监管申报。

**合规管理原型自阶段 6 起封板。后续不再新增通用规则框架、法规引擎或快照层；只接受 Bug 修复、法规专业核验后的数据调整及演示体验优化。**
