# SDS 编写功能 vs 真实 SDS 示例文档 —— 缺失项清单

对照文档：`示例：DOWELLISH TOP DW-81038-CLP-SDS-EN 2024 2865.doc`
（CIRS 出的欧盟 CLP SDS，8 页 / 16 章，依据 REACH 2020/878 + CLP 1272/2008 as amended by (EU) 2024/2865）
对照对象：`src/23-js-sds.js` 的生成向导（6 步）+ `draftText(i)` 的 16 章草案文本

---

## ✅ 处理状态（第一轮已完成）

| 轮次 | 已完成 | 说明 |
|---|---|---|
| **第 1 轮** | **编制态 / 交付态分离**（架构级） | 见下方"零号问题"，本轮最重要改动 |
| 第 1 轮 | G1 标签元素（P 语句 / EUH 自动推导） | 新增 H_STMT / P_STMT / P_BY_H / PIC_BY_H / SIG_BY_H 五张映射表 |
| 第 1 轮 | G7 第 16 章 16.1~16.8 子条目 | 含交付版 `deriveTable16()`（与内部版 `deriveTable()` 分家） |
| 第 1 轮 | G8 法规引用（点名 CLP 1272/2008 + 2024/2865） | `legalBasis(mk)` |
| 第 1 轮 | G9 UFI / 纳米形态归位 1.1 + Index Number / REACH 注册号 | 第 1 章重写为 1.1~1.4 结构 |
| 第 1 轮 | G12 应急电话服务时段 + 非工作时间可用 YES/NO | 写入 1.4 |
| 第 1 轮 | **顺带修复**：分类项缺「皮肤致敏」+ 甲醛 CLP 分类漏 Skin Sens. 1 | 见下方"附带发现" |
| 第 1 轮 | A 类多余项清理（8 项）／B 类标签剥离／C 类归位 | 见下方第三、四节 |
| **第 2 轮** | **M 因子专项：数据层打通 + 拆值 + 推导器 + SDS 只读引用** | `COMP_CLP` 取代硬编码 `CLP_PARAM`，维护入口收进组分库 `bd:comp` |
| 第 2 轮 | 毒性数据状态四态细分 `known / unknown / na / unmaintained` | 取代 `ateKnown` / `aqKnown` 布尔值，修掉未知毒性声明虚高 |
| 第 2 轮 | **顺带修复**：甲醛 M 因子与自身分类自相矛盾 | 已归零并加断言锁定 |
| 第 2 轮 | **顺带修复**：悬停气泡继承 `th` 的 nowrap 导致不换行 | 见下方"附带发现" |

### 第 2 轮 · M 因子专项明细

原方案四件事全部落地：

1. **打通** —— `COMP_CLP` 按 CAS 索引维护 SCL/M/ATE，与 `DB_CFG.component`（组分库）
   合流：列表新增「M 因子」「急性毒性数据」两列，编辑弹窗新增「分类参数」分区，
   查看详情也能看到。SDS 侧只通过 `clpParamOf(cas)` 读取。
2. **拆值** —— M 因子从字符串 `'急性 1 / 慢性 1'` 拆成 `mM` / `mC` 两个数值 +
   `mSrc` 来源（Annex VI 法定值 / 供应商 SDS / 企业自测换算 / 专家判定），
   表格里来源直接写在数值下方，不再是裸值。
3. **推导器** —— 放**组分库**（M 因子是物质属性，不是 SDS 属性）。编辑弹窗里
   填 L(E)C50 / NOEC，点「按实测值计算 M 因子」自动换算并回填，
   同时显示推导过程（不是黑箱）。换算：`mAcute()` / `mChronic()`。
4. **只读引用** —— SDS 向导的参数表内**没有任何可编辑控件**；
   遇到组分库没维护过的 CAS，显示红色「未维护」+「去补录」按钮，
   点一下跳到组分库、定位到该 CAS 并自动打开编辑弹窗。

**四态口径**（`TOX_STATE`）：`known` 有数据 / `unknown` 数据未知 / `na` 经评估不适用 /
`unmaintained` 组分库没这个物质。只有 `unknown` 与 `unmaintained` 计入第 3 / 11 章的
未知毒性声明；`na` 是「已确认安全」而不是「不知道」，不计入。
> 修正前：聚氨酯预聚体（占配方 38.65%）被算进未知急性毒性，声明虚高到 38.65%；
> 修正后：0%。这是本轮最有实质性影响的修复。

**剩余**：G2（第 3 章表格化）、G3、G4、G5、G6、G10、G11、G13、G14、G15、G16、G17。
建议下一轮做 **G2 + G4 + G5**（第 3 章表格化、第 11 章毒理端点、第 12 章生态毒性）
—— 这三块都要消费刚落地的 `COMP_CLP` 数据，趁热打铁。

---

## 零号问题 · 编制态与交付态混为一谈（本轮已解决）

对照后看清的本质问题：原型把**内部编制工作台**和**对客户交付的正式文档**混成了一份。
缺失是"不够专业"，多余是"会出事故"。

| 类别 | 内容 | 处理 |
|---|---|---|
| **A 类** | 混进客户文档的内部信息（8 项） | 从正文删除，改由 `secNotes(i)` 承载 |
| **B 类** | 覆盖度 / 需人工审核 / 已编辑标签 | 仅编制视图展示，交付预览剥掉 |
| **C 类** | 放错位置（UFI·纳米形态、SCL/M/ATE） | 归位到 1.1 与第 3 章 |

**A 类明细（已全部清理）**
1. 第 16 章「行政联系信息：公司档案（一次维护、全公司复用）＋ 按目标市场自动带出的责任主体」← 系统自述
2. 第 16 章「数据来源优先级：实测报告 > 供应商 SDS > 法规库 > PubChem」← 内部取数策略
3. B7 表「判定理由：…（时间 · 张工）」← 人名进客户文档
4. B7 表「数据来源：ECHA ED 评估清单 + CLP Annex VI」← 真实 16.4 只有分类 + 方法两列
5. 第 1 章「MAT-00127 / FORM-WPU320-V1.0（已冻结）」← 内部编码与配方快照版本
6. 第 3 章「保密组分的物质身份未被隐藏…」← 内部合规策略
7. 第 11 章「上表占比随配方数据补录自动下降」「不得默认判定为无致敏性」← 系统行为说明
8. **全文 `【需人工审核】` 标记** ← 最危险，导出忘了清就是事故

> ⚠️ 注意：A 类第 1 项删的是**那句系统自述**，不是行政联系信息本身。
> 1.3 节的公司名 / 地址 / 电话 / 传真 / 邮箱 / 应急电话 / 欧盟 OR 是**法规强制内容**，必须保留。

---

## 附带发现（本轮已修复）

**分类项清单缺「皮肤致敏」，且甲醛 CLP 统一分类漏了 Skin Sens. 1**
- 示例 SDS 的**唯一分类结论**就是 `Skin Sens. 1A / H317` —— 皮革涂饰剂最常见的分类
- 原 `CLP_PARAM['50-00-0']`（甲醛）写的是 `Carc. 1B / Muta. 2 / Acute Tox. 3 / Skin Corr. 1B`，**漏了 Skin Sens. 1**
- `buildClassItems()` 里压根没有致敏项 → 推不出 H317 / P261 / P272 / P280 / P302+P352 / P333+P313
- 已补 `sens` 分类项（加和法，通用浓度限值 0.1%，甲醛 0.35% 触发）+ 第 11 章致敏端点
- 教训：分类项清单是标签元素 / P 语句 / H 语句全文 / 第 11 章端点的**共同上游**，缺一项会四处出错

---

> 结论先行：16 章**骨架齐了**，但每个章节内部的**字段颗粒度**跟真实文档差一个量级。
> 真实文档是"表格 + 逐项列举"，原型是"几行概述文字"。
> 已识别 **17 项缺口**，其中 **10 项属于 P0**（缺了文档不可发布或自相矛盾）。

---

## 一、P0 · 法规强制 / 缺了文档不完整或自相矛盾

| # | 缺失项 | 示例文档怎么写的 | 原型现状 | 建议做法 |
|---|---|---|---|---|
| **G1** | **第 2.2 标签元素缺 P 语句与 EUH** | 象形图 / 信号词 / H317 / **P261·P272·P280·P302+P352·P333+P313·P362+P364·P501** / **EUH066** | T[1] 硬编码「警示词：危险 / 象形图：GHS05、GHS07、GHS08」，**无 P 语句、无 EUH**，且象形图与信号词**不随分类结论变化** | 加「标签元素自动推导」：由 H 代码映射象形图→信号词→H 语句→P 语句；EUH 单独维护（EUH066 重复接触皮肤干裂 / EUH071 腐蚀呼吸道 / EUH380·381 ED / EUH450·451 PMT） |
| **G2** | **第 3 章组分表缺 EC No. 与 REACH 注册号，SCL/M/ATE 未进第 3 章** | 列：Chemical Name / **Registration No.** / CAS No. / **EC No.** / Concentration / Classification / **SCL·M-Factors·ATE**（写在组分表内最后一列） | 第 3 章文本只有「名称（CAS）+ 浓度」；SCL/M/ATE 只存在于步骤 4 的 `clpParamTable()`，**没写进草案第 3 章** | ① 组分库补 `ecNo` + `reachNo`；② 第 3 章草案改真表格，末列合并 SCL/M/ATE（复用 B4 已有数据） |
| **G3** | **第 8 章缺职业接触限值（OEL）表** | 8.1.1 表格：Country / Substance / EC No. / CAS No. / ppm·mg/m³（8h）/ ppm·mg/m³（15min）/ Note，**按成员国给值**（Poland、Ireland）+ 8.1.2 附加限值 + 8.1.3 DNEL/DMEL 与 PNEC | T[7] 只有两行硬编码文本，且写的是"乙二醇单丁醚/甲醛"——**跟当前示例配方无关，是别的产品的数据** | 建 OEL 表（按目标成员国带出，含 8h/15min 双时段与 Note 脚注）；8.1.2 / 8.1.3 留位（可填 Not available） |
| **G4** | **第 11 章毒理端点只有 3 项** | 11.1：ATEmix(Oral/Dermal/Inhalation) + **按组分列 LD50/LC50 实测** + 皮肤腐蚀 / 眼损伤 / **致敏** / **致突变** / 致癌 / **生殖毒性** / STOT-SE / **STOT-RE** / **吸入危害** + **Summary of evaluation of the CMR properties** | T[10]：ATE 三项 + 皮肤 / 眼 / 致癌 | 补全 9 个毒理端点 + CMR 评估总结，端点结论**直接复用步骤 4 的 `classItems`**（数据现成，成本低） |
| **G5** | **第 12 章生态毒性太粗** | 12.1 按组分列：急性 LC50(96h Fish) / LC50(48h Crustacea) / EC50(72h Algae) + 慢性 NOEC×3；12.8 Additional information | T[11] 只有一句「LC50(96h, 鱼) 估算 > 100 mg/L」 | 补 3 组急性 + 3 组慢性 NOEC + 12.8；`CLP_PARAM` 已有 `aqKnown` 字段，可扩 `aqFish/aqCrust/aqAlgae` |
| **G6** | **第 15 章缺 Annex XIV 与 15.2 CSA 勾选结构** | Annex XIV（授权清单）/ Annex XV（SVHC 候选）/ Annex XVII（限制）三条 + Other EU regulations（青少年就业限制）+ Other National regulations + **15.2 CSA：YES ☐ / NO ☒ 勾选框** | T[14]：只有 Annex XVII、SVHC、CLP ATP 21；**缺 Annex XIV**；15.2 只有一句"尚未完成 CSA"，无勾选结构 | 补 Annex XIV 条目；15.2 改 YES/NO 勾选（勾选结果进第 6 步校验项） |
| **G7** | **第 16 章子条目缺 16.2 / 16.5 / 16.6 / 16.7 / 16.8** | 16.1 修订说明 / **16.2 缩略语表（17 条）** / 16.3 数据来源 / 16.4 分类推导 / **16.5 H 语句全文（12 条）** / **16.6 培训说明** / **16.7 进一步信息** / **16.8 读者须知** | 只有 16.3 + 16.4（B7 的 `deriveTable()`）+ 一句免责声明 | 16.2 缩略语表做成静态表；**16.5 H 语句全文可从 `classItems` 的 `code` 字段自动汇总**（复用度高）；16.6/16.7/16.8 是固定免责文案 |
| **G8** | **第 16.1 法规依据引用与 B6 自相矛盾** | "REACH (EC) 1907/2006 amended by (EU) 2020/878, **CLP (EC) 1272/2008 amended by (EU) 2024/2865**" | T[15] 只写「REACH Annex II (EU) 2020/878」——**没提 CLP 1272/2008 及其 2024/2865 修订**，但 B6 的 ED/PMT 声明全靠 2024/2865 | 按市场拼接完整法规引用串：EU → REACH 1907/2006 + 2020/878 + CLP 1272/2008 + 2024/2865；CN → GB/T 16483-2008 + GB/T 17519-2013 + GB 30000 系列 |
| **G9** | **1.1 产品标识字段不全，且 UFI / 纳米形态落错章节** | 六项：Identification on the label/Trade name / **Additional identification**（示例写 "Nanoform is NOT covered by this SDS."）/ **UFI** / Identification of the product / **Index Number** / **REACH registration No.** | T[0]：产品名 + 编码 + 用途 + 供应商 + 应急电话。**B3 做的 UFI 与纳米形态声明只写进第 16 章**，真实 SDS 应落在 **1.1** | ① B3 的 UFI / 纳米形态声明改挂到 **1.1**（第 16 章保留索引即可）；② 补 Index Number（CLP Annex VI 索引号）与 REACH 注册号 |
| **G10** | **第 14 章缺 ADN 内河运输，且非表格、缺 14.5~14.7** | 五列表格：Land (ADR/RID) / **Inland waterways (ADN)** / Sea (IMDG) / Air (ICAO/IATA)；行含 14.1~14.7（14.5 环境危害 / 14.6 用户特殊注意事项 / 14.7 IMO 散装海运） | T[13]：ADR/RID、IMDG、IATA 三行文本，**缺 ADN**，无 14.5/14.6/14.7 显式条目 | 改 4 列 × 7 行表格（UN 号 / 运输名称 / 类别 / 包装组 / 环境危害 / 特殊注意事项 / 散装海运） |

---

## 二、P1 · 影响专业度与可信度

| # | 缺失项 | 示例文档怎么写的 | 原型现状 | 说明 |
|---|---|---|---|---|
| **G11** | **未知毒性占比应为区间表达** | `0.8-1.1% of the mixture consists of component(s) of unknown toxicity.`（**区间**，因为配方浓度本身就是 83%-85% 区间） | `38.65%`（单点值，B5 的 `unknownPct()` 是对定值浓度求和） | 严格说原型没算错——原型配方是定值。**但当配方含保密组分（按 `concRange()` 区间披露）时，声明必须跟着变区间**。建议：含保密组分时自动切换区间口径 |
| **G12** | **1.4 应急电话缺服务时段与「非工作时间可用」勾选** | `+86-028-82478177 Only available during office hours (9:00a.m.-17:30p.m.)` + `Available outside office hours? YES ☐ / NO ☒` | 只有一串号码 | B2 的 `emerg` 字段旁加「服务时段」输入 + YES/NO 单选即可，成本很低 |
| **G13** | **第 9 章理化特性只有 8 项** | 9.1 共 **25 项**（大量 Not available）+ 9.2 Other information 4 项（脂溶性 / 表面张力 / pKa / 氧化还原电位） | T[8] 只有 8 项且**硬编码，不随物料变化** | 缺：气味阈值、熔点/凝固点、蒸发速率、易燃性、引燃温度、爆炸上下限、蒸气压、相对蒸气密度、堆积密度、其他溶剂溶解度、log Pow、自燃温度、分解温度、运动粘度、**颗粒特性（与 B3 纳米形态联动）**、爆炸性、氧化性 |
| **G14** | **8.2 缺热危害与 8.2.3 环境暴露控制** | 8.2.2 五项：Eye/face、Hand、Body、Respiratory、**Thermal hazards**；8.2.3 Environmental exposure controls | T[7]：眼 / 手 / 身体 / 呼吸，缺 Thermal hazards，无 8.2.3 | 补两项即可 |

---

## 三、P2 · 功能与流程层面

| # | 缺失项 | 说明 |
|---|---|---|
| **G15** | **SDS 版本管理与修订记录** | 示例 16.1 有 `Version 1.0`。原型全文写死 V1.0；SDS 列表页无版本概念（只有新建/编辑/发布），没有 V1.0→V1.1 迭代、修订记录（改了哪些章节、为什么改）、历史版本回看 |
| **G16** | **导出** | 第 6 步有"导出"字样但无真实导出。至少应能导出可打印 HTML / Markdown，才能和真实 .doc 对得上 |
| **G17** | **多语言一致性** | 示例是 EN 版。`project.oflang`（官方语言）字段已有，但草案正文是中文，没有英文版预览或双语对照 |

---

## 四、示例文档中「原型已覆盖且口径正确」的部分（不用动）

| 章节 | 原型对应 | 评价 |
|---|---|---|
| 1.2 用途 / 1.2.2 限制用途 | T[0] 推荐用途 + 限制用途 | ✅ 一致 |
| 1.3 供应商（制造商 / OR） | B2 的公司档案 + OR_BY_MARKET | ✅ 已按市场带出，比示例还细（有法规依据说明） |
| 2.1 分类结论 | 步骤 4 `classItems` | ✅ 一致 |
| 2.3 其他危害 | `edStmt('2.3')` | ✅ B6 做对了，且示例也确实是「PBT/vPvB + ED」两句 |
| 3 章末未知毒性声明 | `unknownStmt()` | ✅ 有，只是口径待改区间（见 G11） |
| 4 / 5 / 6 / 7 / 10 / 13 章 | T[3]~T[6]、T[9]、T[12] | ✅ 都是法规标准文案，颗粒度够用 |
| 11.2 其他危害（ED） | `edStmt('11.2')` | ✅ 位置正确 |
| 12.5 PBT/vPvB、12.6 ED | T[11] + `edStmt('12.6')` | ✅ 位置正确 |
| 16.4 分类推导方法 | B7 `deriveTable()` | ✅ 比示例还细（示例只写 "Calculation method" 一行，原型有判定依据 + 数据来源 + 判定理由） |

---

## 五、建议的推进顺序

如果按"投入产出比"排序，我建议这么切：

| 批次 | 内容 | 理由 |
|---|---|---|
| **第 1 批** | G8 法规引用 + G9 UFI/纳米形态归位 + G12 应急电话时段 | 都是**小改动、但直接消除"自相矛盾"**。G8 尤其重要——现在 B6 声明 2024/2865 而第 16 章不提，是硬伤 |
| **第 2 批** | G1 标签元素（P 语句/EUH 自动推导）+ G7 第 16 章子条目 | 这两块**数据现成**（`classItems` 已有 H 代码），主要是映射表 + 渲染，见效快且专业度提升最明显 |
| **第 3 批** | G2 第 3 章表格化 + G4 第 11 章端点补全 | 需要动组分数据结构（`ecNo` / `reachNo` / 毒理实测值），工作量中等，但**是真实 SDS 的核心内容** |
| **第 4 批** | G3 OEL 表 + G13 理化特性 | 两块都是"大字段表"，适合做成独立的可编辑表单页 |
| **第 5 批** | G5 / G6 / G10 / G11 / G14 | 收尾补齐 |
| **暂缓** | G15 / G16 / G17 | 属产品能力而非 SDS 内容，可放到 SDS 模块之后再单独立项 |

---

## 六、一个需要用户决策的点

**关于 G13（理化特性 25 项）与 G3（OEL 表）**：
这两块在真实 PLM 里数据来自**检测报告/实验室系统**，不是 SDS 向导里手填的。
原型有两种做法：

- **A（轻量）**：SDS 向导里给一张可编辑的字段表，默认值从物料主数据带出，缺的显示 `Not available`（跟示例文档一致）
- **B（重）**：把理化特性/OEL 做成**独立的"物料理化数据库"模块**，SDS 只做引用

我倾向 **A**——原型阶段不需要再开一个模块，而且示例文档本身就大量 `Not available`，如实留空反而是对的。
