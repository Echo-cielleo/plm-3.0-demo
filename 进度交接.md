# PLM 3.0 演示原型 · 进度交接（详细技术档案）

> 🚀 **新会话请先读 `/workspace/续接指南.md`**（精简入口，约 200 行）。
> 本文是详细档案，记录每一轮的实现细节、踩坑与自检结果，**按需查阅，不必全读**。

> **用途**：会话中断 / 重启 / 升级版本后，新会话读取本文件 + 源码即可无缝续上，不依赖对话历史恢复。
> **最后更新**：2026-09-07（第十轮完成，全量回归 20/20 通过）

---

## 一、当前状态

**产物**：`/workspace/PLM3.0全系统演示原型.html`（1.45 MB，单文件，可直接双击打开）

| 阶段 | 内容 | 状态 |
|---|---|---|
| S0 | 地基：CSS 变量、公共组件、路由、侧栏、ECharts 内联 | ✅ |
| S1 | DOE 主线：列表/详情 6 Tab/4 步向导/结果录入/建模分析/最佳方案/配方记录 | ✅ |
| S2 | SDS 主线：6 步向导、组分库、PubChem、GHS、ZDHC/RoHS·REACH 法规库、影响分析 | ✅ |
| S3 | 首页工作台、客户需求、自研需求、项目列表/详情、应用项目、受限物质 | ✅ |
| S4 | 12 个轻量列表页 + 2 个占位页（菜单已铺满） | ✅ |
| S5 | **首页 + 项目详情页改造** | ✅ 2026-09-02 完成（首页圆圈溢出修复；项目详情 6 Tab + 右侧阶段导航 + 数据层扩展） |
| S6 | **法规库三页说明折叠**（ZDHC / RoHS·REACH / 受限物质管理） | ✅ 2026-09-02 完成（说明文本收进标题右侧「说明」按钮的折叠面板） |
| S7 | **SDS 模块对照《SDS编写信息分工整理》改造** | ✅ 2026-09-03 全部完成（P0 列表页 + P1 向导 + P2 收尾 + 2 个补丁） |

**自检基线**：
- `smoke_all.py` → 全菜单 **29/29 已实现、0 未注册、0 异常、0 JS 错误**
- `smoke_pages.py` → 全注册表 **37/37 渲染正常**
- `smoke_proj.py` → S3 专项（含 S5 后的 6 Tab / 阶段导航 / 总结穿透 / 多对多）**81/81 断言通过**
- `smoke_note.py` → S6 说明折叠专项（3 页 × 收起/展开/重渲染保持/再收起）

---

## 〇、协作偏好（用户明确要求，务必遵守）

1. **不要为了展示而截图** —— 改完 build + 自检通过，直接把产物交给用户预览验收即可。截图、效果对比图属于浪费时间。
2. **执行任务选节约时间的做法** —— 自检脚本跑一次就够，不要重复跑、不要重复验证产物内容。
3. **交付只传产物本身** —— `present_files` 只传 HTML 产物，不要附带一堆截图文件。
4. **工作目录保持干净** —— 临时截图、临时验证目录用完即删，不要留在 `/workspace`。

---

## 一·B S6 说明折叠（2026-09-02）

**需求**：法规库的 ZDHC、RoHS、受限物质管理三页都有一段说明文本，收起来，标题右侧放「说明」按钮，点击展开。

**实现**（做成通用组件，后续任意页可复用）：
- `20-js-core.js` 新增 `NOTE_OPEN` / `noteBlock(key,text,label)` / `toggleNote(key)`；`renderListPage(cfg)` 新增 `note` + `noteKey` 字段（有 note 时不再平铺 `sub`）
- `23-js-sds.js` 的 `sdsHead(id,title,desc,acts,descId,noteKey)` 新增第 6 参 `noteKey`；按钮容器补 `class="page-acts"`
- `renderLawPage(types,title,desc,noteKey)` 新增第 4 参；原页面那条 `.notice.grey` 横幅并入说明面板（标题「清单维护与生效」）
- `03-css-mod.css` 追加 `.note-btn` / `.note-panel` / `.np-in` / `.np-t` / `.np-x` / `.np-n`
- 三页 noteKey：`law-zdhc` / `law-rohs` / `subst`

**注意点**：
- 展开状态存 `NOTE_OPEN`，列表页搜索/翻页重渲染后仍保持（已断言）
- 法规库页下方原有「数据源与更新机制说明」折叠卡片（`lawMechHtml`，默认收起）**保留不动**，其末尾讲的是 SDS 结论依据优先级，与新说明面板的「清单维护与生效」角度不重复
- RoHS 原 desc 末句"均为官方渠道下载后人工导入验证"与横幅重复，已改为"覆盖限制物质、授权清单与申报要求"

---

## 二、用户已确认的决策（S5 实施完毕）

### ① Tab 最终留哪几项

- **用户决策** 6 项：项目表单 / 变更记录 / 审核历史 / 实验记录 / 实验总结 / 文档；**需求删除**
- 项目信息 = 主信息，**位于 Tab 容器之外**（页面上部）
- 项目表单 = 一个 Tab，是"展示每个阶段不同内容的容器"

### ② 右侧竖向阶段导航

- **用户决策** 阶段导航在 Tab 容器内，**仅对「项目表单」Tab 生效**
- 其他 Tab 不显示阶段导航，内容全宽
- 切换阶段 → 只切换该 Tab 内的左侧表单内容（不动 Tab）

### ③ 实验总结层级

- **用户决策** 项目级，挂在项目下，可穿透到关联的实验详情
- 总结卡内含关键指标 + 结论 + 关联实验按钮（点击跳 `exp:detail`）

### ④ 三个按钮行为

- **用户决策** toast 占位（与"新建项目/新建需求"等其他只读按钮一致）
- **项目信息内仍展示「归属部门」字段**（取自 `USERS.dept`，与人员部门保持一致）

---

## 三、S5 实际改造清单

- ✅ 首页待办圆圈溢出：加 `.tl` 容器，紧急度三色配色真正生效
- ✅ Tab 间距：`.tabs` 的 `margin-bottom` 由 0 改为 16px
- ✅ 项目详情：项目信息上移、6 项 Tab、按钮改为返回/更换负责人/修改项目名称/调整部门
- ✅ 数据扩展：`PROJ_STAGES`（5×5=25 条）、`PROJ_CHANGES`、`PROJ_AUDITS`、`PROJ_SUMMARIES`、`PROJ_DOCS`
- ✅ 右侧竖向阶段导航：仅项目表单 Tab 显示，点击切换表单
- ✅ 实验总结：项目级，含关键指标 + 穿透到实验
- ✅ 切换项目时阶段选中态正确重置（自检抓到的真实 bug）
- ✅ 顺手补 `.b` 类的 `font-weight`（S1 阶段遗留）

---

## 四、待修复清单

（暂无。SDS 模块 S7 已全部完成，下一轮由用户提出新需求时启动）

**历史遗留（未排期，等用户挑）**：

- 给 S4 的 12 个轻量列表页补详情页
- 4 个 toast 占位按钮改成真实改数据（项目详情页的返回/更换负责人/修改名称/调整部门）
- **A9**（待改版 diff 对比）—— 用户明确「先不做」

---

## 五、文件地图

```
/workspace/
├── PLM3.0全系统演示原型.html   ← 最终交付产物（由 build.sh 生成）
├── build.sh                    ← 分片 → 单文件拼接（含 script 标签配平校验）
├── src/                        ← 源码分片，按文件名顺序拼接
│   ├── 00-head.html
│   ├── 01-css-token.css        ← CSS 变量 / 基础排版（.page-hd 等）
│   ├── 02-css-comp.css         ← 公共组件（.btn/.card/.tbl/.tl-item 时间线）
│   ├── 03-css-mod.css          ← 业务模块样式（stage-bar、anova、proj-tags）
│   ├── 04-css-sds.css          ← SDS 模块样式
│   ├── 10-shell.html           ← 页面骨架
│   ├── 20-js-core.js           ← $ / $$ / esc / toast / modal / confirmBox / tabs / chart / renderListPage
│   ├── 21-js-data.js           ← USERS / PROJECTS / experiments / 多对多纯函数
│   ├── 22-js-router.js         ← MENU 菜单树 / HL_MAP 侧栏高亮锁定 / PAGES 注册表 / showPage
│   ├── 23-js-sds.js            ← SDS 主线（2116 行，最大分片）
│   ├── 24-js-doe.js            ← DOE 主线
│   ├── 25-js-charts.js
│   ├── 26-js-pages-a.js        ← 首页工作台 / 需求 / 项目管理 / 受限物质
│   ├── 27-js-pages-b.js        ← S4 的 12 个轻量列表页 + 2 占位页
│   └── 28-js-boot.js           ← 启动 + dev:kit 调试页（**绝不可再注册 home**）
├── smoke_all.py                ← 全菜单遍历自检
├── smoke_pages.py              ← 全注册表遍历 + 截图
├── smoke_proj.py               ← S3 专项（含多对多关联验证）
└── _ss/                        ← 29 张截图存档
```

---

## 六、重启后如何接上

新会话开头直接说：**「读 /workspace/进度交接.md，继续」**，我读完后会自动：
1. 追平当前进度与待决项
2. 重读 `src/` 相关分片确认代码细节
3. 等用户给 4 个决策的答案后开工

**即使会话历史完全丢失也不影响**——所有代码、数据、自检脚本、截图都在磁盘上。

---

## 七、踩过的坑（避免重犯）

| 坑 | 说明 |
|---|---|
| **`$()` 是 `getElementById`** | 选择器查询必须用 `$$(sel, root)`，误用会导致静默失效（侧栏高亮曾全程失效） |
| **`28-js-boot.js` 不得注册 `home`** | 该分片最后加载，会覆盖 `26-js-pages-a.js` 的真实工作台（已踩过，自检 home 已改为 `dev:kit`） |
| **ECharts 内联需转义 `</script>`** | `build.sh` 已用 `sed` 处理，否则提前闭合标签导致白屏 |
| **`renderListPage` 首次挂载即渲染** | 返回前已调用 `render()`，调用方无需再手动触发 |
| **zsh 下 `$c[^a-z]` 会被当数学表达式** | 正则检查请改用 Python 脚本 |
| **`class="td"` 是描述文字不是圆点** | 时间线圆点是 `.tl-item::before`，别再误用 |

---

## 一·C S7 SDS 模块改造（2026-09-03 起）

**依据文档**：`/root/uploads/.../SDS编写信息分工整理-wb版.md`（星星整理，基于瑞旭 CIRS 委托调查表）

### 用户已确认的条目

- **A1** SDS 管理改父子菜单：默认落「SDS 文档列表」，列表上放「＋ 新增 SDS 文档」→ 进向导
- **A2** 证据状态灯：红=库内已有新版法规未跟进（确定要改版）／黄=超复审周期 or 来源 pub or data_gap（该复审/仅供参考）／绿=引用最新版+未超复审期
- **A3–A8、A10** 全部做；**A9**（待改版 diff）先不做
- **B1** 配方两条路径：手动（从组分库选）、从实验配方带出；**带出后允许手动增/删/改**
- **B2** 行政联系信息：可做
- **B3** 纳米形态 + 16 位 UFI：**仅欧盟市场需要，且非必填**
- **B4–B7** 按我的判断实现
- **保密口径（用户原话）**：配方保密要求下产品信息里的组分信息要从系统删除，编写时只能人工操作；**从实验配方带出只是省点力，不代表这个配方可以直接生成 SDS**

### P0 已完成（2026-09-03）

| 项 | 实现 |
|---|---|
| A1 | `MENU` 里 SDS 改父子（`sds` → `sds:list` + `sds:wizard`）；`baseOf()` 由 `sds:wizard` 改 `sds:list`；标题「SDS 文档列表」，headActs「＋ 新增 SDS 文档」 |
| A2 | 新增 `sdsEv(r)`：红>黄>绿 顺序判定；`.ev/.ev-green/.ev-amber/.ev-red` 圆点灯 + tooltip |
| A3 | 状态新增「待改版」（purple），数据 2 条（SDS-2026-0151 / 0126） |
| A4 | `sdsRulePack()` 徽标：CLP ATP21（blue）/ GB 30000（green），title 显示法规名+版本 |
| A5 | 筛选加 `rulePack` 下拉（状态 + 规则包双筛选） |
| A6 | `renderListPage` 新增 `rowExpand(row)` + `lpToggleExp()`；行首 ▸ 箭头，行内展开版本历史（`r.versions` 真实数据，不再弹窗写死） |
| A7 | `sdsActs(r)` 按状态区分：编制中→编辑/提交审核；审核中→只读；已发布→导出PDF/Word/申请改版；待改版→开始改版；已归档→只读 |
| A8 | `sdsExport` 加状态校验，非已发布 toast 拦截 |
| A10 | `seedSdsRows()` 末尾按灯排序（红→黄→绿）；`rowCls` 加 `row-red`/`row-amber` 底色；首页动态插入 SDS 证据预警，`sdsFocus(no)` 跳转并 `row-focus` 高亮 |

### 本轮新增的公共能力（可复用）

- `20-js-core.js`：`daysTo(s)` 日期差；`renderListPage` 新增 `rowCls(row)` / `rowExpand(row)`；`lpToggleExp(ri,ev)`
- `03-css-mod.css`：`.ev*` 灯、`.row-red/.row-amber/.row-focus` 行底色、`td.exp-tog`、`.exp-row`、`.ver-box/.tbl.mini`

### 自检基线（S7 P0 后）

- `smoke_all.py` → **30/30**（SDS 拆成两项，菜单 29→30）
- `smoke_pages.py` → **37/37**
- `smoke_proj.py` → **81/81**
- `smoke_note.py` → 说明折叠 3/3
- `smoke_sds_list.py` → **34/34**（P0 专项，新增脚本）
- 全部 0 JS 错误

### P1 已完成（2026-09-03）

| 项 | 实现 |
|---|---|
| A2 修订 | 证据灯由三档改**四档 + 归档熄灯**：`red`(需改版) / `overdue`(待复审) / `due`(临期 ≤30 天) / `green`(正常) / `none`(已归档，熄灯不参与评估)。`SDS_EV_ORD`、`sdsEvLamp()`、`rowCls`、首页预警（`需改版 / 待复审 / 临期` 三档分开统计）、详情弹窗 notice 同步更新 |
| 数据 | 校验：归档文档一律熄灯；`due` 档仅落在距复审 0~30 天内 |
| B1 · 路径① | 手动维护：手填 CAS 自动匹配 / 「从组分库选择」批量勾选 / 「＋ 添加行」。人工录入行视为已核对 |
| B1 · 路径② | 从实验配方引入：新增 `EXP_RECIPE` + `expRecipeOf(id)`（`21-js-data.js`），`24-js-doe.js` 的 `tabRecipe(e)` 改为同源读取（实验详情页配方 = SDS 引入源，不再各写一份）。`fmPickExp()` 选实验 → `fmExpConfirm()` 二次确认（列出组分 + 保密声明）→ 带入 |
| B1 · 保密口径 | 带入的组分 `chk:false` → 行内「待核对」橙标签 + `.row-chk` 高亮；`fmCheck(i)` 单行核对 / `fmCheckAll()` 批量核对；**未核对完 `fmFreeze()` 与 `wzCheck(2)` 双重拦截**。带入后仍可手动增 / 删 / 改 |
| B4 | `CLP_PARAM`（6 个 CAS 的 SCL / M 因子 / ATE）+ `clpParamOf()` + `clpParamTable()`，挂在 `renderStep4()` 顶部（混合物显示，纯物质不显示）。参数缺失行 `.row-chk` 高亮，表下说明「任一参数未维护，对应危害类别的加和法就算不动」 |
| B6 | `buildClassItems()` 开头插入 `ed`（内分泌干扰）与 `pmt`（PMT / vPvM）两项，规则版本 `CLP (EU) 2024/2865`，ED 含 3 个判定选项（Cat.1 / Cat.2 / 不分类），不分类也要求在 2.3 / 11.2 / 12.6 声明 |

### 补丁 · 人工判定弹窗「系统建议」标签（2026-09-03）

**现象**：首次打开判定弹窗显示「系统无法给出建议」，选完保存后再次打开，所选行反而挂上了「系统建议」标签。

**根因**：`adjClass()` 里判定基准用的是 `c.result`，而 `adjSave()` 会把 `c.result` 覆写成用户选的值 ——
**「系统建议」和「当前生效值」共用了同一个字段**，判定完两个就混成一体了。
（另有一个隐藏 bug：`var isSug=(!c.opts||o.o===c.result)`，一旦某分类项没写定制 `opts`，会用默认 `CLASS_OPTS`，
此时 `!c.opts` 为真，**每一行都会被打上「系统建议」**。）

**修复**：

1. `buildClassItems()` 末尾 `.map()` 固化 **`c.sug`**（系统原始建议），`pending` 项置 `null`；人工改判只改 `c.result`，`c.sug` 永不变
2. 行内删除冗余的「系统建议」标签（头部色块已有），改为：改判行标橙色 **「当前判定」**，采纳系统建议时不标
3. 绿色高亮行的判定基准从 `c.result` 改为 `c.sug`，并去掉 `!c.opts` 短路
4. 头部色块：改判时追加 `当前判定：X（已改判）`；表头小注补 `绿色高亮 = 系统建议（本次无建议）`

### P2 已完成（2026-09-03）—— SDS 模块收尾

| 项 | 实现 |
|---|---|
| **B2** | 新增 `COMPANY`（公司档案）+ `OR_BY_MARKET`（EU/UK/TR 责任主体）到 `21-js-data.js`。步骤①新增「行政联系信息」卡片：公司中英文名/地址/邮编/电话/传真/邮箱**只读**（一次维护全公司复用）；应急电话 + 欧盟 OR + PCN 毒理中心电话可覆盖，带法规依据说明。`admSync()` 按市场联动（`_admMk` 记住已带出的市场，市场未变则保留人工覆盖值）、`admDirty()`/`admBadge()`/`admRefreshBadge()`（只刷标记不重绘表单，避免失焦）、`admReset()` 还原公司档案 |
| **B3** | 步骤①新增「产品是否纳米形态」+「16 位 UFI」，**仅欧盟市场渲染、均非必填**。选「是」才展开纳米形态下拉（5 项）。`ufiErr()` 校验 16 位字符（忽略连字符、仅数字 + A–Z）；`wzCheck(1)` 里 UFI 填了才校验、留空放行；声明纳米但未选形态会拦截 |
| **B5** | `CLP_PARAM` 新增 `ateKnown` / `aqKnown` 两个数据完整度字段（水的 ATE 补为真实值）。`unknownPct(kind)` / `unknownNames(kind)` / `unknownStmt(kind)` 生成法规强制的固定声明（中英对照），挂进第 3 章（急性）、第 11 章（急性）、第 12 章（水生）。当前数据：急性未知 38.65%（聚氨酯聚合物豁免）、水生未知 47.50% |
| **B6 收尾** | `edStmt(where)` 生成 ED / PMT 声明，按法规要求落在 **2.3 / 11.2 / 12.6** 三处，标注「即使判为不分类本声明亦不可省略」 |
| **B7** | `deriveMethod(c)`：manual→专家判断 / 含「加和法」→Calculation method / 含「通用浓度限值」→加和法·通用浓度限值 / 含 Annex VI→统一分类查表。`deriveTable()` 在第 16 章逐条列出**推导方法 + 判定依据 + 数据来源**，人工判定项附判定理由与判定人 |
| **B4 联动** | 参数表高亮口径改与 B5 一致（`ateKnown===false`），表下说明点出「橙底行会计入第 3/11 章未知急性毒性声明」，两处形成闭环 |

### 自检基线（S7 P1 后）

- `smoke_all.py` → **30 已实现 / 0 未注册 / 0 异常**
- `smoke_pages.py` → **37 正常 / 0 异常 / 0 报错**（无高亮警告）
- `smoke_proj.py` → **81/81**
- `smoke_note.py` → 说明折叠 **ALL PASS**
- `smoke_sds_list.py` → **38/38**（随四档灯更新断言）
- `smoke_sds_p1.py` → **48/48**（P1 专项，新增脚本）
- `smoke_adjclass.py` → **23/23**（人工判定弹窗专项，新增脚本）
- `smoke_sds_p2.py` → **67/67**（P2 专项，新增脚本）
- 全部 0 JS 错误

### 工作目录清理

- 删除 `smoke_pages.py` 的截图逻辑与 `_ss/` 目录、`_smoke_home.png`，保持 `/workspace` 只放产物与脚本

### 踩坑（S7）

1. **测试选择器误伤**：`querySelectorAll('#lpHost tbody tr')` 会把展开区内嵌套的版本表行也选中（53 行而非 10 行）。判定顶层数据行要用 `!classList.contains('exp-row') && textContent.indexOf('SDS-')===1`
2. **toast 读取串台**：连续调两次 `sdsExport` 再读 `.toast` 会拿到上一条。测试里每次调用前先清空 `#toastWrap`
3. **`sds:list` 原本已注册但菜单无入口**，只能从详情弹窗间接到达 —— 这是 A1 被发现的根本原因
4. **弹窗选择器不是 `.modal-card`**：`openModal` 的容器是 `#modal`（class 就叫 `modal`），确认按钮 id 是 `_cfmOk`。测试里点确认直接 `document.getElementById('_cfmOk').click()`，别按文案找按钮
5. **`c.result` 不要同时当「系统建议」和「当前值」用**：两者一旦共用一个字段，人工判定后系统建议就被吞掉了。凡是「建议 vs 采纳/改判」的场景都要拆成两个字段
6. **Python heredoc 里写 JS 代码，`\n` 必须写成 `\\n`**：直接写 `\n` 会被 Python 当真换行写进文件，JS 字符串未闭合 → 整个分片语法错误但 build.sh 的标签配平检查发现不了。改完分片务必 `node --check src/xx.js`（本次就栽在 `unknownStmt` / `deriveTable` / `edStmt` 三处）
7. **input 的 value 不属于 `inner_text`**：测试里校验只读字段带出要读 `input.value`，不能用 `pg.inner_text("#wzBody")`
8. **测试设 `wz.project` 字段要按真实选项值**：`LANG_EU` 的键是 `'德语 Deutsch'` 不是 `'德国'`，设错会触发 `wzValidate1` 里的联动清空，后续断言全被「请先选择…」拦截
9. **`wz.formula` 是步骤④的输入**：B4 参数表直接遍历它，所以纯物质（formType='pure'）要走单独分支，否则会把 100% 单物料当成加和法输入展示

---

## 本轮（S7 收尾 · 对照真实 SDS 示例文档）

### 输入

用户上传真实 SDS：`示例：DOWELLISH TOP DW-81038-CLP-SDS-EN 2024 2865.doc`
（CIRS 出的欧盟 CLP 正式 SDS，8 页 16 章，依据 REACH 2020/878 + CLP 1272/2008 as amended by (EU) 2024/2865）
用 `antiword -w 200 sample.doc > sample.txt` 提取为 412 行纯文本，逐行对账。

产出《SDS缺失项清单.md》：17 项缺口（P0 十项 / P1 四项 / P2 三项）+ 三类"多余项"。

### 关键认知：编制态与交付态必须分离

对照后才看清的本质问题 —— 原型把**内部编制工作台**和**对客户交付的正式文档**混成了一份。
缺失是"不够专业"，多余是"会出事故"（`【需人工审核】`一旦导出就是事故）。

用户确认的三类处理原则：
- **A 类（混进客户文档的内部信息）** → 从正文删除
  - 注意：A 类第 1 项删的是**第 16 章那句系统自述**（"行政联系信息：公司档案…按目标市场自动带出"），
    **不是删行政联系信息本身** —— 1.3 节的公司名/地址/电话/OR 是法规强制内容，必须保留
- **B 类（覆盖度/需人工审核/已编辑标签）** → 只在编制态展示，交付态剥掉
- **C 类（放错位置）** → 归位（UFI/纳米形态 → 1.1；SCL/M/ATE → 第 3 章组分表）

### 已实现

**1. 编制态 / 交付态分离（架构级）**
- `draftText(i)` 重构为**交付态正文**：剔除全部 `【需人工审核】`、内部物料编码、配方快照版本、
  保密披露策略说明、系统自述、取数策略、B7 表的数据来源/判定依据/判定理由/人名
- 新增 `secNotes(i)`：**内部批注单独承载**，仅编制视图渲染成橙色提示块，标明"不会进入交付文档"
- `renderStep5()` 加视图切换：`wz.view = 'edit' | 'deliver'`
  - 编制视图：覆盖度标签 + 需人工审核标签 + 内部批注块 + 编辑工具条
  - 交付预览：文档抬头（SAFETY DATA SHEET + 版本 + 法规依据）+ 纯 16 章正文，无标签无工具条
- `deliverScan()` 交付前自检：扫描 16 章正文的 `DELIVER_DIRTY` 敏感词表，结果渲染成徽标
- 「需人工审核」判定基准从"正文搜 `【需人工审核】`"改为 `secNotes(i).length > 0 || !s.auto`

**2. G1 标签元素自动推导**
- 新增 `H_STMT`（H+EUH 全文 34 条）、`P_STMT`（P 语句全文 46 条）、`P_BY_H`（H→P 选择表）、
  `PIC_BY_H`（H→象形图）、`SIG_BY_H`（H→信号词）
- `labelParts()` / `labelElements()`：由分类结论自动推导象形图 / 信号词 / H 语句 / P 语句 / EUH，
  **替换了第 2 章和第 4 步"标签要素"框里的硬编码**（原来是写死 GHS05/07/08 + P201/P280/P305…）
- `euhCard()` + `euhAuto()` + `toggleEuh()`：EUH 勾选区；ED/PMT 结论自动带出 EUH380/381/450 且不可取消

**3. G7 第 16 章子条目**
- 补齐 16.1 修订说明 / 16.2 缩略语表（26 条 `ABBR_TABLE`）/ 16.3 数据来源 / 16.4 推导方法 / 16.5 H 语句全文 / 16.6 培训说明 / 16.7 进一步信息 / 16.8 读者须知
- `deriveTable16()` 是**交付版** 16.4（只有分类结论 + 推导方法）；内部版 `deriveTable()` 保留给第 4 步

**4. G8 法规引用**：`legalBasis(mk)` 按市场拼接完整串，欧盟必须点名 CLP 1272/2008 + 2024/2865（消除 B6 自相矛盾）

**5. G9 产品标识归位**：第 1 章重写为 1.1~1.4 结构，UFI / 纳米形态从 16 章移到 1.1；新增 Index Number、REACH 注册号（未填输出 `See section 3`）

**6. G12 应急电话**：新增服务时段输入 + 「Available outside office hours」YES/NO 单选，写入 1.4

**7. 顺带修掉的数据错误**：`CLP_PARAM['50-00-0']`（甲醛）统一分类**漏了 `Skin Sens. 1`**，
且分类项清单压根没有「皮肤致敏」项 —— 而示例 SDS 的唯一分类结论就是 `Skin Sens. 1A / H317`，
皮革涂饰剂最常见的分类。已补 `sens` 分类项（加和法，通用浓度限值 0.1%）+ 第 11 章致敏端点。

### 自测基线（全绿）

| 脚本 | 结果 |
|---|---|
| `smoke_all.py` | 30 页面 / 异常 0 / 总错误 0 |
| `smoke_pages.py` | 37 正常 / 异常 0 / 报错 0 |
| `smoke_proj.py` | 81 / 81 |
| `smoke_note.py` | ALL PASS |
| `smoke_sds_list.py` | 38 / 38 |
| `smoke_sds_p1.py` | 50 / 50 |
| `smoke_sds_p2.py` | 67 / 67 |
| `smoke_adjclass.py` | 23 / 23 |
| `smoke_deliver.py` | **68 / 68**（本轮新增） |

### 踩坑（本轮新增）

10. **交付预览的提示文案里别出现"内部批注""系统覆盖度"这类词**：测试用 `inner_text("#wzBody")` 断言
    "交付预览不含内部标记"，结果被顶部的说明文案（`已剥离系统覆盖度、需人工审核标记、内部批注…`）命中，假失败。
    要断言"不含某物"，先确认说明文案本身没把这个词写出来
11. **P 语句映射表用 `Array.sort()` 排序要自定义比较器**：P 语句形如 `P302+P352`，
    默认字典序会把 `P301+P310` 排到 `P33` 后面。用 `parseInt(x.replace(/\D/g,''),10)` 取数字部分比
12. **发现"分类项清单缺项"要靠端到端反推**：直接看 `buildClassItems()` 不容易发现缺了皮肤致敏，
    是写 G1 测试断言"H317 应推出 P261/P272/P280"时才暴露出来的 —— 分类项清单是标签元素、P 语句、
    H 语句全文、第 11 章端点的**共同上游**，缺一项会在五个地方同时出错

---

## 本轮追加 · 步骤 4 UI 归一（用户直接指定）

### 四处改动

1. **三参数说明改悬停** —— `clpParamTable()` 底部说明里 SCL / M 因子 / ATE 三词走 `term()` 机制
   - 顺带补 `GLOSSARY['SCL']`（原本没有）、`GLOSSARY['EUH']`
   - 修正 `GLOSSARY['M 因子']`：原写"用于水生慢性毒性的加和法"**不准确**，
     改为"只适用于 Aquatic Acute 1 或 Chronic 1；急性与慢性各有一个值；按 L(E)C50/NOEC 换算或采用 Annex VI 表 3.1 法定值"
2. **步骤说明位置上移** —— 新增 `#wzGuide` 容器（夹在 `#wzSteps` 与 `#wzBody` 之间）+ `wzGuide(html)` 函数
   - 第 2~6 步的顶部说明全部从 `wzBody` 内挪到导航下方，**内容区不再有整屏宽横幅**
   - 第 1 步 `wzGuide('')` 清空，避免回退时残留其他步的说明
3. **EUH 说明框移除** —— 说明并入标题的悬停术语：`<h3>补充危害说明（term('EUH')）</h3>`
4. **待判定提示改为标题旁标签** —— 原来是一整条 `notice.warn` 横幅（含一排快捷按钮），
   现在改为「分类证据追溯」标题旁的橙色标签 `N 项待人工判定 ›`，**可点击**：
   `jumpPend()` 滚动到第一个待判定证据卡并描边高亮 1.8s（需给 `.ev-card` 加 `id="evc"+i`）

### 自测

- `smoke_deliver.py` **89 / 89**（新增 UI 归一专项 21 条）
- 其余 8 个脚本全绿，0 JS 错误

### 踩坑（本轮新增）

13. **`.term` 的 `textContent` 含 tooltip 全文**：`term()` 生成的 span 结构里，
    tooltip（`GLOSSARY` 释义 + "🤖 问 AI 助手"）也是 textContent 的一部分，
    所以 `n.textContent.trim()==='SCL'` 永远不成立。断言术语名要读内部的 `.tt`：
    `document.querySelectorAll('#wzBody .notice.grey .term .tt')`
14. **给元素加 `term()` 后，按原文断言会失败**：EUH 标题从 `补充危害说明（EUH）`
    变成 `补充危害说明（EUH?EUH欧盟补充危害说明...）`，测试里要用前缀 `补充危害说明` 匹配
15. **跨步骤的测试状态会串**：测"第 5 步说明"前若前面执行过 `setWzView('deliver')`，
    `wz.view` 仍是 `deliver`，读到的是"第 5 步 · 交付预览"而不是"生成 SDS 草案"。
    断言依赖视图状态的文案前，先显式 `setWzView('edit')`

---

## 本轮追加 · 三参数说明去重 + ATE 缺失口径校正

### 用户指令

> 「SCL / M 因子 / ATE 这三个词在表格的表头上已经有了，直接在表头上加下划线就可以了，
> 从现在的灰色说明里移除吧。聚氨酯预聚体无可用的ATE是因为它是混合物所以才没有吗？」

### 改动

1. **表头加悬停，说明框去重**（`clpParamTable()`）
   - 表头三列从 `SCL 特定浓度限值 / M 乘数因子 / ATE 急性毒性估计值` 改为
     `term('SCL')` / `term('M 因子')` / `term('ATE')`，列宽同步收窄（160 / 120 / 210）
   - 灰色说明框里**移除这三个词的悬停术语**（留 `term('加和法')`），改为
     「表头带下划线的三项释义可悬停查看」
   - 卡片副标题 `.sub` 保留全称展开（它是缩写展开，不是解释机制重复，与表头互补）

2. **橙底行成因文案修正**：`聚合物豁免或未维护` → `缺实测数据或未维护`。
   原表述把两件不同的事混为一谈（见下）

### 事实核查：聚氨酯预聚体为什么没有 ATE

**不是"因为是混合物"，恰恰相反 —— 它是聚合物（纯物质）。**

- `DB_CFG.material` 里 MAT-00128 水性聚氨酯预聚体，形态 = **纯物质（单物料）**
- `DB_CFG.component` 中 remark = 「聚合物豁免，需确认游离 NCO」
- 证据矩阵 `['聚合物豁免注册 (REACH Art.2(9))','reg']`
- 真正的混合物是成品 DW-81038 本身，聚氨酯预聚体是它的一个**组分**

因果链：
1. 聚合物有独立 CAS（9009-54-5），SDS 第 3 章按**单一物质**申报，不是混合物
2. REACH Art.2(9) 给聚合物**注册豁免**（除含 ≥2% w/w 未注册单体等情形）
3. 更根本的是理化层面：分子量大、难穿透生物膜 → 常规急性毒性试验测不到全身毒性
   → 通常 LD50 > 2000 mg/kg，不达分类阈值 → **无实测数据**
4. 因此 `CLP_PARAM['9009-54-5'].ate` 写 `—（聚合物，不适用急性毒性估算）`

### 发现的原型缺陷（已提出，未动手）

`ateKnown` 只用一个布尔值，把三种后果完全不同的情形混成同一个 `false`：

| 情形 | 典型原因 | 加和法处理 | 第 3/11 章声明 |
|---|---|---|---|
| ① 数据未知 | 没做过试验 / 供应商 SDS 第 11 章留白 | 不进分子，计入**未知毒性占比**；>10% 时 Acute Tox. 不能外推 Cat.4 | **必须声明占比** |
| ② 已知不适用 | 已确认 LD50 > 2000、不达阈值（**聚合物属此类**） | 不进分子，但属于**已充分认知**，占满 100% 分母份额 | **不计入未知** |
| ③ 组分库未维护 | 数据存在但没录进系统 | 行为同 ①，但这是**管理缺口** | 应报错 + 跳转补录 |

当前后果：聚氨酯预聚体占 38.65%，被直接算进「未知急性毒性占比」，
使 B5 声明虚高、加和法出现过度保守偏差。严格讲它属于情形 ②。

**建议下一轮把 `ateKnown` 从布尔值细分为 `'unknown' | 'na' | 'known' | 'unmaintained'`**，
与 `aqKnown` 同型改造。已并入下一轮待办。

**附带知识点**：聚合物的急性毒性缺失不代表该组分对混合物分类无影响。
真正要盯的是**游离 NCO 残留单体**（游离 MDI / TDI / HDI），它们往往带
Acute Tox. / Resp. Sens. / Skin Sens.，才是决定混合物分类的关键 ——
这正是组分库 remark「需确认游离 NCO」的含义。供应商 SDS 第 3 章通常会把
聚合物本体与残留单体**分开列**（如 `4,4'-MDI < 0.1%`），此时 ATE 要用
**残留单体的 ATE** 按实际残留浓度参与加和法。

### 自测

- `smoke_deliver.py` **92 / 92**（三参数断言改为查 `table thead .term .tt`，
  另增 2 条：说明框不含这三词 / 橙底行文案已修正）
- 其余 8 个脚本全绿，0 JS 错误

### 踩坑（本轮新增）

16. **改文案后忘了同步改断言**：把 `带下划线的表头可悬停查看释义`
    改成 `表头带下划线的三项释义可悬停查看` 后，断言仍按旧文案匹配而失败。
    断言里对长文案用**正则**（`/表头带下划线.*悬停查看/`）比精确 `indexOf` 更耐改
17. **去重要检查同一容器内所有 `term()`**：说明框里除了三参数还留了
    `term('加和法')`，第一次改完只去掉了 `term('ATE')` 的重复写法，
    结果说明框里仍然命中 ATE —— 断言"说明框不再重复三词"才抓出来

---

## 本轮 · 悬停气泡排版修复（纯 UI）

### 用户指令

> 「SCL这三个悬停时出现的气泡内文本没有换行，超过气泡本身的宽度啦，再改一下呢」

### 根因

`04-css-sds.css:108` `.sds-scope .tbl th{...white-space:nowrap...}`
→ `.tp` 气泡写在 `th` 内部，**继承了 nowrap**，于是 `width:310px` 形同虚设，
文字一路横着流出气泡。

之前气泡都用在正文段落里（父级无 nowrap），所以一直没暴露；
这次把 `term()` 放进**表头**才撞上。

### 改动（`04-css-sds.css` 术语气泡段）

1. `.sds-scope .term .tp` 补 `white-space:normal;word-break:break-word`
   —— 显式声明覆盖继承，**全局生效**，不管气泡落在 `th` / `td` / `.tag` / `.btn`（这些都有 nowrap）里都安全
2. 新增末列右对齐规则，避免窄列上的气泡越出卡片右边界：
   ```css
   .sds-scope .tbl th:last-child .term .tp,.sds-scope .tbl th:nth-last-child(2) .term .tp,
   .sds-scope .tbl td:last-child .term .tp,.sds-scope .tbl td:nth-last-child(2) .term .tp{left:auto;right:0}
   ```
   （ATE 列宽 210px < 气泡 310px，不右对齐会溢出约 100px）

### 自测

- `smoke_deliver.py` **97 / 97**（新增「悬停气泡排版」5 条：
  表头 3 个术语 / SCL 气泡 white-space 为 normal / 三列气泡 `scrollWidth<=clientWidth` /
  末列气泡右边缘不越出卡片）
- 其余 8 个脚本全绿，0 JS 错误

### 踩坑（本轮新增）

18. **子元素的排版属性要防父级污染**：`white-space` 是**继承属性**，
    气泡这类浮层组件放在 `th` / `.tag` / `.btn` 等 nowrap 容器里就会被带偏。
    凡是浮层（tooltip / popover / dropdown），都应在自身规则里**显式写死** `white-space`、
    `text-align`、`line-height`、`font-weight` 这几项，不能靠继承
19. **测 `display:none` 的浮层要用真 hover**：`getComputedStyle` 对隐藏元素返回的
    `left/right` 可能是 `auto` 而非 used value，且 `scrollWidth`/`clientWidth` 全为 0。
    断言浮层排版必须先用 Playwright 的 `.hover()` 让它真正显示（`dispatchEvent` 触发不了 CSS `:hover`）

### 协作口径提醒（用户明确反馈）

> 「ateknown改成什么什么看不懂，聚合体影响分类更是读不懂，我不懂化学专业知识。我们还是聚焦在功能实现上吧。」

**后续与用户对话时**：只讲功能层（改了哪个字段、哪个交互、验证结果），
不展开法规/化学原理。专业依据与数据口径判断只写进本文档（供下一轮实现参考），
不写进给用户的回复。上一节的 ATE 三情形分析即属此类 —— 结论
（`ateKnown` 需从布尔细分为 `'unknown'|'na'|'known'|'unmaintained'`）保留在待办里，
但不再向用户解释其化学含义。

---

## 第 2 轮 · M 因子专项（用户从四项待办里选的，已完成）

### 用户决策

盘点完剩余 11 项缺口后，用选项让用户挑批次，用户选了 **「M 因子专项（推荐）」**。
推荐理由写进回复了：M 因子那批是**数据结构改造**，而 G2/G5 都要消费这些数据，
先做 G2/G5 再改数据结构会返工一遍。

### 交付内容（原方案四件事全部落地）

1. **打通** —— `COMP_CLP`（按 CAS 索引）取代硬编码 `CLP_PARAM`，与 `DB_CFG.component` 合流
2. **拆值** —— M 因子拆成 `mM` / `mC` 数值 + `mSrc` 来源
3. **推导器** —— 放组分库编辑弹窗，`mAcute()` / `mChronic()` 换算
4. **只读引用** —— SDS 侧零可编辑控件，缺值给「去补录」跳转

外加：毒性数据四态 `known / unknown / na / unmaintained`、甲醛数据矛盾修正。

### 关键函数

| 函数 | 位置 | 作用 |
|---|---|---|
| `COMP_CLP` | 23-js-sds.js | 分类参数主数据，8 个组分 |
| `mAcute(v)` / `mChronic(v)` | 同上 | M 因子换算，返回 0 = 不适用 |
| `M_SRC` / `TOX_STATE` | 同上 | 来源字典 / 四态配置（含 `unk` 是否计入未知声明） |
| `fmtNum(n)` / `ateTxt(p)` | 同上 | 千分位 / ATE 展示串由数值派生 |
| `clpParamOf(cas)` | 同上 | **SDS 侧唯一读取入口**，缺失返回 `unmaintained` 兜底 |
| `clpMTag(cas)` / `toxTag(cas,kind)` | 同上 | 组分库列表的 M 因子 / 状态标签 |
| `toxUnknown(q,kind)` | 同上 | B5 声明与表格高亮**共用**的判定，保证两处口径一致 |
| `compClpLoad` / `compClpRender` / `compClpCollect` / `compClpCalc` | 同上 | 组分库编辑弹窗的分类参数子表单 |
| `gotoCompFill(cas)` | 同上 | 缺值引导：跳组分库 + 定位 CAS + 自动开编辑弹窗 |

### 数据结构（COMP_CLP 单条）

```js
'13463-41-7':{name:'吡硫翁锌',
  uni:'Acute Tox. 3 / Eye Dam. 1 / Aquatic Acute 1 / Aquatic Chronic 1',
  scl:'Eye Dam. 1 ≥ 0.05%',
  mM:100,mC:100,mSrc:'calc',lc50:'0.0026',noec:'0.0008',
  ateO:100,ateD:200,ateI:0.5,ate:'经口 100 mg/kg｜经皮 200 mg/kg｜吸入 0.5 mg/L',
  ateState:'known',aqState:'known'}
```

演示数据自洽性：`lc50/noec` 喂给推导器算出的 M，与手写的 `mM/mC` 一致，
用户点「计算」能看到相同结果。`ate` 串由 `ateTxt()` 从三个数值派生，
保存时重新生成 —— 展示串永远不会跟数值脱节。

### 换算算法（边界已断言）

```
mAcute(v):   v>1 → 0；否则 k 从 0 起，每满足 v<=10^-(k+1) 就 k++，返回 10^k
mChronic(v): v>0.1 → 0；否则 k 从 0 起，每满足 v<=10^-(k+2) 就 k++，返回 10^k
```
`mAcute: 2→0, 1→1, 0.5→1, 0.05→10, 0.005→100, 0.0005→1000`
`mChronic: 0.5→0, 0.1→1, 0.05→1, 0.005→10, 0.0005→100`

### 自测

- **`smoke_mfactor.py` 新建，63 / 63**（8 个分组：数据层 / 矛盾修正 / ATE 自洽 /
  换算边界 / 推导器 / 四态维护 / SDS 只读 / 回归）
- 全量 10 个脚本无失败，0 JS 错误
- `smoke_sds_p1.py` 51/51、`smoke_sds_p2.py` 69/69、`smoke_deliver.py` 97/97

### 踩坑（本轮新增）

20. **`dbRender` 不支持 `col.fmt`**：`renderListPage`（20-js-core.js）支持，
    但组分库走的是另一套 `dbRender`，只处理 `col.tag`，我加的 fmt 列全显示 `—`。
    修复：给 `dbRender` 的 `col.map` 加一行 `if(col.fmt)return '<td>'+col.fmt(r)+'</td>';`
    （fmt 返回 HTML，不能再 esc）
21. **组分库每页 8 条不是 10 条**：写断言时按 10 条假设，结果"未维护"的物质在第 2 页，
    第一页全是已维护的，`querySelectorAll('.tag.red').length` 恒为 0。
    翻页用 `dbPageGo(n)`，测完记得 `dbPageGo(1)` 复位
22. **改数据结构前先想清楚谁消费它**：本轮把 G2/G5 排在 M 因子之后，
    就是因为那两块要读 `COMP_CLP`。反过来做会返工 —— 这个排序判断已经写进
    SDS缺失项清单.md 的推进建议里

### 协作口径

用户明确：「我不懂化学专业知识。我们还是聚焦在功能实现上吧。」
→ **只对用户讲功能层**（改了哪个字段、哪个交互、验证结果），
专业依据与法规口径只写进本文档，不写进给用户的回复。
本轮全程按此执行：四态改造只说"修掉了声明虚高 38.65% → 0%"这个可验证的结果，
没有展开化学原理。

---

## 第三轮续接（2026-09-04）：G2/G3 收尾 + 持久化

### 已完成
- **G2 + G3 已交付并验收**（上一轮续接起点）：第3章真表格、第8章 OEL 表、G14 8.2 补全，
  全量回归 0 JS 错误。
- **新增 `smoke_sds_sec.py`，41 / 41**：覆盖 G2 第3章表格 / G3 OEL 表 / EU-CN 联动 / 保密区间 /
  G14 结构 / 回归。
- 修交付视图 bug：保密组分「区间披露」内部标记原本泄漏到交付件，已改为仅交付视图给浓度区间、
  不显示该内部口径词。
- **持久化 + 一键重置（用户确认做，按钮放全局菜单/设置区）**：
  - 新建 `src/23z1-js-persist.js`（独立分片，零侵入旧文件）
  - 只持久化 `wz`（当前 SDS 草稿），不碰主数据 `DB_*`
  - 默认初始：刷新后恢复本人本机草稿；带 `SCHEMA` 版本号，旧缓存自动失效
  - 周期节流保存（800ms）+ `beforeunload` 兜底，防误刷新丢失
  - 包装 `renderSidebar` 注入侧栏底部「重置演示数据」按钮 → 二次确认 → `resetWizard()`
  - 新增 `smoke_persist.py`，11 / 11（保存 / 刷新恢复 / 重置回初始 / 版本失效保护）

### 踩坑（本轮新增）
23. **`wzInitState()` 无返回值却被当作 `var base = wzInitState()` 使用**：
    导致 `base === undefined`，`Object.assign(undefined, o.w)` 抛
    `Cannot convert undefined or null to object`，`wzLoad` 恢复失败、product 留空。
    修复：`wzInitState();` 直接重置全局 `wz`，再 `wz = Object.assign(wz, o.w)`。
    （排查时一度误判是表单同步 `wzValidate1` 或 `wz.project` 被整体替换，
    实测均不是；根因是函数调用约定——`wzInitState` 副作用式赋值、无 return。）

### 全量回归
- `smoke_all` 0 错 / `smoke_sds` 0 错 / `smoke_sds_p1` 51/51 / `smoke_sds_p2` 69/69 /
  `smoke_mfactor` 63/63 / `smoke_sds_sec` 41/41 / `smoke_persist` 11/11
- 合计约 235 条断言全绿，0 JS 错误

### 后续待办（演示优先级）
G4（第11章毒理 3→9 项）> G13（第9章理化 8→25 项）> G5/G6/G10/G11/G14 收尾；
暂缓 G15/G16/G17。持久化+重置已完成，不再阻塞。

---

## 第四轮续接（2026-09-04）：第5步交付预览改文档流 + 导出 Word

### 需求
用户：第5步的交付预览改为「模拟 Word/PDF 的预览页面」；并增加「无交互的导出为 Word 按钮」。

### 已完成
- **交付预览（dv 视图）改为连续文档流**：原 deliver 视图是网页卡片 + 手风琴（acc-item），
  现改为 `.doc-shell` 灰底 + `.doc-page` 白纸（A4 视觉、页边距、阴影）+ `.doc-cover` 封面
  （SAFETY DATA SHEET 大标题 / 产品 / 版本 / 日期 / 市场 / 供应商 / 编制依据 / 交付自检徽标）
  + 16 章**连续正文**（非折叠），第 3/8 章表格内嵌。edit 编制视图保持原工作台手风琴不变。
- **无交互导出 Word 按钮**：交付预览工具条右侧新增「⤓ 导出 Word」，点击即生成 `.doc`
  （Word 兼容 HTML + 内联样式，`application/msword` 下载），**不弹窗、不受发布状态限制**
  （草案态文件名带「_草案」标记）。与步骤6的 `wzExport`（需发布解锁、带进度弹窗）并存，互不替换。
- 新建 `src/23z2-js-sds-export.js`（独立分片，零侵入旧文件）：
  `sdsDocBodyHtml()` / `sdsDocPreviewHtml()` / `exportSdsWord()` + 预览样式注入。
  `renderStep5()` 仅加两处钩子（工具条导出按钮、dv 分支改用 `sdsDocPreviewHtml()`）。
- 新增 `smoke_export.py`，17 / 17（文档流渲染 / 16 章连续 / 表格内嵌 / 导出按钮可见性 /
  无交互下载 / .doc 内容校验）。

### 踩坑（本轮新增）
24. **改 dv 渲染后旧断言失效**：`smoke_sds_sec.py` 用 `#acc2`（手风琴 id）查交付视图组分表，
    改文档流后该元素消失 → 改为查 `.doc-page` 内的 `table`；`smoke_deliver.py` 的
    「交付自检通过」徽标原在 headCard 的 `deliverBadge(dirty)`，文档流封面需显式渲染
    `deliverBadge(dirty)`（已复用，文案「✓ 交付自检通过」保留）。

### 全量回归
- `smoke_all` 0 错 / `smoke_sds` 0 错 / `smoke_sds_p1` 51/51 / `smoke_sds_p2` 69/69 /
  `smoke_mfactor` 63/63 / `smoke_sds_sec` 41/41 / `smoke_deliver` 97/97 / `smoke_persist` 11/11 /
  `smoke_export` 17/17 / `smoke_adjclass` 23/23 / `smoke_proj` 81/81 / `smoke_sds_list` 38/38 /
  `smoke_pages` 37/0/0 / `smoke_note` 0 JS 错误
- 合计约 491 条断言全绿，0 JS 错误

### 后续待办（演示优先级）
G4（第11章毒理 3→9 项）> G13（第9章理化 8→25 项）> G5/G6/G10/G11/G14 收尾；暂缓 G15/G16/G17。

---

## 第五轮续接（2026-09-04）：G4 第11章毒理端点 + G13 第9章理化特性 25 项

### 需求
用户确认先做 G4 + G13（演示门面最显眼的两章：毒理端点 3→9、理化 8→25）。

### 已完成
- **G13 第 9 章理化特性 8 项 → 25 项结构化表格**：新建 `PHYS_PROPS`（25 项，含中英文），
  `physTableHtml()` 渲染「理化特性（9.1）／结果／数据来源」三列表。**已知值**（外观、pH、沸点、
  闪点、相对密度、水溶性、运动粘度、VOC 共 8 项）填实测/物料主数据；**未测得项如实标 Not available**
  （与示例文档大量留空口径一致）；第 22 项「颗粒特性（纳米形态）」联动第 1 章 `p.nano` 判定
  （含纳米 → 描述；否则 Not applicable）。
- **G4 第 11 章毒理端点 6 项 → 9 个 CLP 标准端点表格化**：`toxTableHtml()` 含
  ① 急性毒性 ATEmix（口/皮/吸）② 9 个危害类别端点（皮肤/眼/致敏/突变/致癌/生殖/STOT-SE/STOT-RE/吸入危害，
  结论**直接复用步骤 4 的 `classItems`**，经 `toxResult(id)` 取 `result+code`；`muta/stotRE/aspi` 三类
  classItems 无记录，`classOf` 返回「不分类」，符合真实 SDS 写法）③ 按组分急性毒性数据（LD50/LC50，
  取自各组分 `COMP_CLP.ate`）④ CMR 评估总结（沿用既有口径）。
- 新逻辑全在独立分片 `src/23z3-js-sds-sections.js`（零侵入旧文件）；`draftText` 的 T[8]/T[10]
  改为引导语，**edit 与 dv 两处渲染钩子均加**（与 G2/G3 模式一致，双视图都显示表格）。
- 新增 `smoke_g4_g13.py`，**32 / 32**（edit/dv 双视图、25 项理化、9 端点、组分实测表、纳米联动、
  Not available 口径、0 JS 错误）。

### 实现要点
- `classItems` 共 10 个 id（ed/pmt/skin/sens/eye/stot/carc/aqua/resp/repr），**无 muta/stotRE/aspi**；
  这三类在毒理端点表里显示「Not classified / 不分类（混合物经评估未触发该危害类别）」。
- `repr` 是 pending 状态，`toxResult` 特判显示「待人工判定（须在发布前确认）」。

### 全量回归
- `smoke_all` 0 错 / `smoke_sds` 0 错 / `sds_p1` 51/51 / `sds_p2` 69/69 / `mfactor` 63/63 /
  `sds_sec` 41/41 / `deliver` 97/97 / `persist` 11/11 / `export` 17/17 / `adjclass` 23/23 /
  `proj` 81/81 / `sds_list` 38/38 / `note` OK / `pages` OK / `g4_g13` 32/32
- 合计约 523 条断言全绿，0 JS 错误

### 后续待办（演示优先级）
G5（第12章生态毒性表格）/ G6（第15章 Annex XIV + 15.2 CSA 勾选）/ G10（第14章 ADN 运输表）
> G11（未知毒性区间）收尾；G15/G17 暂缓。
G4/G13 已完成，SDS 16 章「厚度」已与真实文档基本对齐。

---

## 第六轮续接（2026-09-04）：G5 第12章生态毒性 + G6 第15章法规/CSA/Annex XIV + G10 第14章运输

### 需求
用户确认做完 P0 剩余三项：G5（第12章生态毒性表格）、G6（第15章 Annex XIV + 15.2 CSA 勾选）、G10（第14章 ADN 运输表）。

### 已完成
- **G5 第 12 章生态毒性表格化**（`ecotoxTableHtml`）：
  - 12.1 混合物级水生危害结论（复用 `toxResult('aqua')` → 类别 3 / H412）
  - 按组分水生毒性表（物质 / CAS / 水生危害分类 / 急性 L(E)C50 / 数据状态），`lc50` 实测当前配方全空→Not available
  - 12.5 PBT/vPvB 评估（已知结论：混合物不含 PBT/vPvB）
  - 12.2–12.8 子项保留（未测得项 Not available），含 12.6 内分泌干扰（edStmt）+ 水生未知声明（unknownStmt('aquatic')）
- **G10 第 14 章运输信息表格化**（`transportTableHtml`）：
  - ADR/RID/ADN/IMDG/IATA/散装/特殊注意事项 7 行概览表（ADN 内河为其中一行）
  - 当前配方为非危险货物，UN/名称/类别/包装组标注「不适用」，海洋污染物「否」
- **G6 第 15 章法规 / CSA / Annex XIV**（`legalTableHtml`）：
  - 15.1 法规表**按目标市场过滤**（EU→REACH/Annex XVII/SVHC/CLP；CN→GB/T 16483/17519/30000），不再两表同显
  - 15.2 化学安全评估（CSA）勾选框（REACH Art.14，状态「尚未完成」）；CN 市场下标注不适用
  - REACH Annex XIV 授权清单命中（联动配方组分，当前配方无命中→「均未列入」；CN 下标注不适用）
- 新建 `src/23z4-js-sds-sections2.js`（独立分片，零侵入旧文件）；`draftText` 的 T[11]/T[13]/T[14] 改引导语；
  `renderStep5` 的 **edit 与 dv 两处钩子**均加（与 G2/G3/G4/G13 一致）。
- 新增 `smoke_g5_g6_g10.py`，**32 / 32**（双视图、生态毒性表、运输表含 ADN、法规表 EU/CN 过滤、CSA、Annex XIV、0 JS 错误）。

### 踩坑（本轮新增）
25. **draftText 切片替换导致索引错位**：替换 T[11] 用 `L[1859:1874]=...` 删了 14 行，使后续 `L[1879]/L[1880]` 改错位置，把 T[13]/T[14] 旧文本漏改、并在 T[15] 续行内插入 `13:'14...'`，破坏数组语法 → 整个 SDS 脚本块加载失败（wzGo 未定义）。
    修复：改用内容锚点重建段（跳过错位行、补回被覆盖的 16.7 行），`node --check` 通过后解决。
26. **测试读取折叠手风琴的 innerText 为空**：acc 块默认折叠，`innerText` 返回空 → 断言全假；dv 文档流（展开）全真。
    修复：断言改 `textContent`（不受可见性影响）；acc ID = `acc` + 章节索引 i（0-based），即第 N 章 = `acc(N-1)`（此前误用章节号）。
27. **sds_p2 第12章断言查 sec(11) 失败**：声明随 `ecotoxTableHtml` 渲染进 acc 容器，sec 文本块只剩引导语。
    修复：sds_p2 第12章相关断言改查整个 `acc(11)`（新增 acc helper），与 G4/G13「表格承载章节内容」约定一致。

### 全量回归
- `smoke_all` 0 错 / `smoke_sds` 0 错 / `sds_p1` 51/51 / `sds_p2` 69/69 / `mfactor` 63/63 / `sds_sec` 41/41 /
  `deliver` 97/97 / `persist` 11/11 / `export` 17/17 / `adjclass` 23/23 / `proj` 81/81 / `sds_list` 38/38 /
  `note` OK / `pages` OK / `g5_g6_g10` 32/32
- 合计约 555 条断言全绿，0 JS 错误

### SDS 缺口清单 G 项进度（17 项）
- 已落地 14 项：G1/G2/G3/G4/G5/G6/G7/G8/G9/G10/G12/G13/G14/G16
- 剩余 3 项：G11（未知毒性区间表达，P1，细枝末节）、G15（版本管理/修订记录，P2 暂缓）、G17（多语言 EN，P2 暂缓）
- **结论：P0 全部完成，SDS 16 章内容厚度已与真实文档基本对齐，演示无缺章。**

---

## 第七轮续接（2026-09-04）：实验管理 · 实验列表左侧「部门→项目」二级联动树

### 需求
用户：实验列表左侧增加部门（上海实验室一/二、四川实验室一/二等）与项目列表二级联动——
选部门时项目列表只显示该部门项目；选项目时实验列表只显示该项目实验。其余 G 类任务（G11/G15/G17）暂缓。

### 已完成
- **部门树数据建模**：5 个项目补 `dept` 字段（`src/21-js-data.js`），映射
  上海实验室一=PRJ-001/004、上海实验室二=PRJ-002、四川实验室一=PRJ-003、四川实验室二=PRJ-005。
  新增 `DEPTS`（4 个实验室）常量放新分片，不污染旧数据文件主体。
- **左树 UI（二级联动）**：`expTreeHtml()` 渲染「全部实验室」根 + 4 部门 + 其下项目，含计数徽标；
  选部门 → 树只显示该部门节点、项目列表收窄到该部门；选项目 → 实验列表只显示该项目；
  再点同部门/根节点可取消回到全部。`expTreeSelect()` 用内联 onclick 驱动，零额外事件绑定。
- **顶部「所属项目」下拉框部门联动**：`expProjOptions()` 受 `fDept` 收窄（选部门后只列本实验室项目），
  `expTreeSelect` 与下拉 `onchange` 双向同步（改下拉也会刷新左树）。
- **过滤逻辑**：`renderExpRows()` 增加部门过滤（实验任一 projectId 命中该部门项目即计），与类型/状态/关键词叠加。
- 布局：列表页改为左右两栏 `.exp-layout`（左 `.exp-tree` 250px 粘性、右 `.exp-main`），响应式 900px 下堆叠。
- 新逻辑全在独立分片 `src/24z-js-exp-tree.js`（零侵入 `24-js-doe.js`），旧文件仅加最小钩子（状态字段、布局包裹、下拉用 `expProjOptions()`、过滤加部门条件、onchange 同步）。
- 样式追加到 `src/03-css-mod.css`（`.exp-layout` / `.exp-tree` / `.et-*`）。
- 新增 `smoke_exp_tree.py`，**20 / 20**（树渲染 / 4 部门 / 5 项目 / 部门联动收窄 / 项目过滤 / 取消回全部 / 下拉同步 / 叠加筛选 / 0 JS 错误）。

### 踩坑（本轮新增）
28. **下拉框只改 value 不重建 options**：初版 `expTreeSelect` 仅设 `expFProj.value`，导致选部门后
    工具栏下拉仍列全部 5 项目，与「项目列表只显示该部门」不一致。修复：改为重建
    `expFProj.innerHTML='<option>本实验室全部</option>'+expProjOptions()` 后再设 value。

### 全量回归
- `smoke_all` 0 错 / `smoke_sds` 0 错 / `sds_p1` 51/51 / `sds_p2` 69/69 / `mfactor` 63/63 / `sds_sec` 41/41 /
  `deliver` 97/97 / `persist` 11/11 / `export` 17/17 / `adjclass` 23/23 / `proj` 81/81 / `sds_list` 38/38 /
  `note` OK / `pages` OK / `exp_tree` 20/20
- 合计约 575 条断言全绿，0 JS 错误

### 后续
- SDS 缺口 G 项仅剩 G11（P1 细枝末节）、G15/G17（P2 暂缓），均不影响演示，已按用户意见搁置。
- 可继续优化其他核心模块（如项目管理、实验数据分析、质量/检测等）。

---

## 第八轮：实验管理 · 三页重构（2026-09-04）

### 用户需求
> 实验管理菜单改为「实验列表 / DOE 实验设计 / 实验分析」三页，体现两层数据关系：
> 普通实验 → 1 条记录 → 录入数据 → 分析；
> DOE 方案 → 试验矩阵 → 确认下发 → N 条记录 → 各组录入 → 方案统一分析。
> 现有 DOE 分析报告内容保留，但不直接作为菜单首页；保持现有视觉/组件风格，所有按钮可演示。

### 数据层重构（src/21-js-data.js）
- 新增 `doeSchemes[]` 设计层（1 条 = 1 套完整方案，状态：草稿/待下发/执行中/已完成）
- `experiments[]` 执行层（4 普通 + 44 DOE 运行 = 48 条）
- 注入 helpers：`findScheme / findRun / runsOfScheme / pad2 / dataStatOf / schemeDataStat / schemePlanWithRes / analysisTargets / analysisDataReady / analysisDone / analysisKindLabel`
- `expRecipeOf()` 兼容 DOE 运行继承所属方案配方
- 种子方案分布：4 状态 × 4 设计类型（0028/0005 草稿 · 0225/0098 待下发 · 0158 执行中 · 0312/0041 已完成）
- 调整 0041 因子数为 2，0067 改为草稿，控制运行记录总数便于演示

### 菜单（src/22-js-router.js）
- 实验管理下改为 `实验列表 / DOE 实验设计 / 实验分析` 三页
- 侧栏高亮：exp:wizard→exp:doe，exp:report / exp:best→exp:analysis

### 三页实现（src/24z1-js-exp-restructure.js，新增分片）
- `exp:list`：左树联动（沿用 24z）+ 5 个筛选 + 9 列；DOE 行显示「方案号 / 运行序号」；DOE 实验「编辑设计」按钮置灰且不可单独改设计参数
- `exp:doe`：方案层列表；5 筛选 + 10 列；按状态分支按钮（草稿 编辑/生成计划/删除；待下发 查看计划/确认下发；执行中 查看进度；已完成 查看分析；通用 复制方案）
- `exp:analysis`：先选对象列表；4 筛选 + 8 列；分析对象 = 已完成普通实验 + 已下发方案；三态按钮：开始分析 / 查看分析 / 重新分析 / 导出报告，数据不完整时按钮置灰且提示「实验数据未完成」
- 跳转函数：`openWizardNew / openWizardEdit / openEntry / startAnalysis`（旧页面引用统一在此定义）
- `analysisViewObj()`：把 DOE 方案包装成报告页所需的统一视图对象（plan 走 schemePlanWithRes 单向聚合）

### 报告页（src/24-js-doe.js）
- 原 `exp:analysis` 改名 `exp:report`，接收 `{id, kind}` 参数，通过 `analysisTargetById + analysisViewObj` 复用 ANOVA/Pareto/残差/正态图（演示级合成，不依赖真实录入值）
- 页头区分类型：DOE 方案分析报告 / 实验分析报告；按钮区分：分析对象列表 / 所属 DOE 方案 / 详情 / 查看最佳方案
- DOE 报告顶部增加「数据来自各运行记录汇总」横幅
- `exp:best` 沿用 `window._curReportObj` 统一视图对象，回报指向报告页
- `submitEntry` 区分 DOE 运行 / 普通实验：DOE 走所属方案分析（满足全部组→报告，否则回到方案列表）
- `confirmDispatch` 改为写入 doeSchemes（待下发），要求选择所属项目；4 步向导第 4 步标题改为「确认并生成计划」
- `tabDesign` 增加 DOE 运行只读锁定提示；`renderDetailTabs` 适配 DOE 运行记录
- `wizard.draft.projectIds` + 标签选择 UI（wzToggleProj / wzOpenProj / wzSaveProj）

### 兼容更新
- `src/22-js-router.js` HL_MAP 调整
- `src/23-js-sds.js` `fmPickExp()` 改为从 doeSchemes + 普通实验取配方来源；`fmExpConfirm` 支持方案 id
- `src/24z1-js-exp-restructure.js` 扩展 `wzResetConfirm`：一并清掉新建实验与 DOE 方案（try/catch 防 SDS 向导 DOM 误伤）

### 自检脚本
- `smoke_exp_restructure.py` 新增，96/96（含一键重置覆盖两层数据）
- `smoke_exp_tree.py` 更新行数断言（默认 8→48，部门 3→23，项目 2→12）
- `smoke_proj.py` 关联实验改用 `EXP-2026-0402`（方案 id 不再是实验 id）
- `smoke_sds_p1.py` 配方来源断言改用 doeSchemes + 普通实验
- `smoke_pages.py` HL_LOCK 更新
- `smoke_doe.py` 由 Node 版转 Python 版（页面 id 全部对齐两层模型）
- 移除 `smoke_doe.js` 与 Node 依赖

### 全量回归
19 个脚本全 PASS（约 672 条断言，0 JS 错误）：smoke_all / smoke_pages / smoke_doe / smoke_exp_restructure / smoke_exp_tree / smoke_proj / smoke_sds_p1 / smoke_persist / smoke_g5_g6_g10 / smoke_g4_g13 / smoke_mfactor / smoke_adjclass / smoke_deliver / smoke_export / smoke_note / smoke_sds / smoke_sds_list / smoke_sds_p2 / smoke_sds_sec

### 后续
- 用户认可后，可继续优化项目管理页内的实验 Tab（按方案/按运行两级展示），以及项目侧的实验卡片标识 DOE 来源。

---

## 第九轮：实验管理 · DOE实验分析改名 + 新增「实验分析与总结」（2026-09-07）

### 用户需求
1. 菜单「实验分析」改名 **「DOE实验分析」**
2. 新增菜单 **「实验分析与总结」**，用于对多组实验的过程与结果进行对比分析和总结
   - 页面：组织结构树 + 项目树 + 数据列表（行号 / 实验编号（实验员）/ 实验工艺 / 实验总结 / 创建人 / 创建时间）
   - 行操作：详情 / 编辑 / 删除
   - 搜索：实验目的、实验编号、实验员、实验工艺、实验总结、项目负责人、创建人
   - 子页面：标题栏＝实验目的 + 实验工艺；正文＝实验对比区，竖向展示每个实验的
     基本信息 / 原料添加情况 / 过程测试结果 / 成品检测结果 / 总结文本框
3. **用户补充的关键定义修正**（本轮后半段）：
   > 「实验分析」的对象不是某一条实验记录，而是一组**具有可比性**的实验记录。
   > 「同个实验工艺」需放宽：不是所有工艺参数必须完全一样，而是
   > **采用相同的工艺路线或工艺模板，具体工艺参数可以存在受控差异。**

### 设计澄清（与用户确认的 4 项决策）
| 决策点 | 结论 |
|---|---|
| 一份总结报告 = ？ | 一份「对比总结报告」，引用多组实验 |
| 报告数据从哪来 | 从已有实验引用生成，不新建实验 |
| 原料表用哪种 | 实际投料表（名称 / 批次号 / 实际用量 / 单位 / 顺序） |
| 是否纳入一键重置 | 是 |

数据来源三分类：① 可复用（experiments 编号 / 实验员 / 时间、PROJECTS.leader、EXP_RECIPE 骨架）
② 需补字段（实验目的 / 实验工艺 / 实验总结）③ 全新结构（过程测试结果、成品检测结果 ——
现有 QCREPORTS 按样品批次维度且只有「通过 / 部分通过」结论、无指标值，接不上，故独立建模）。

### 数据层（src/21-js-data.js）
- 新增 `expSummaries[]` + `seedExpSummaries()`（6 份种子 SUM-2026-0001~0006）
- 单条结构：
  ```
  {id, purpose, craft, diffNote, projectIds, creator, createTime,
   leader, dept, expIds[], items:[{expId, craftParams, diff,
     basic:{temp,duration,device,operator,date,mat},
     materials[], processTests[], productTests[], summary}]}
  ```
- 辅助：`mkMat/mkProc/mkProd`、`sumMatchExp(sum,fn)`、`sumOwners`、`findSummary(id)`
- 新增 4 条普通实验种子（`EXP-2026-0397/0398/0389/0403`），把 3 份原本只引用 1 条实验的
  总结扩成真正的「可比组」；experiments 48 → 52（8 普通 + 44 DOE 运行）

### 语义修正：工艺路线 vs 受控差异
| 字段 | 语义 | 示例 |
|---|---|---|
| `craft` | **工艺路线 / 模板**，只写路线与工序顺序，不含参数取值 | `三辊涂布 → 热风烘干 → 室温熟化 → 压花` |
| `diffNote` | **受控差异说明**：刻意变化了哪些参数、哪些保持一致 | `受控差异：树脂来源（国产/进口/1:1 混拼）。涂布量 18 g/m²、烘干 120 ℃ × 3 min 三组一致。` |
| `items[].craftParams` | 该组**实际参数取值** | `涂布量 18 g/m² · 120 ℃ × 3 min · 熟化 24 h` |
| `items[].diff` | 该组在**受控变量上的取值** | `树脂：进口 WPU-320（基准样）` |

> 修正原因：原 `craft` 写成完整参数串（如 `三辊涂布 · 涂布量 18 g/m² · 120℃ × 3 min`），
> 按此定义组内参数一变就不算同工艺，只能做重复性验证，无法比较参数影响 —— 已重构。

6 份种子全部按新语义重写，每组 ≥2 个受控变量取值：
0006 树脂来源（3 组）／0005 加脂剂用量（3 组）／0004 染浴温度（3 组）／
0003 手感剂批次（3 组）／0002 加脂剂浓度（2 组）／0001 交联剂加量（2 组）

### 菜单（src/22-js-router.js）
```js
{id:'exp',name:'实验管理',icon:'🔬',prio:'P0',children:[
  {id:'exp:list',name:'实验列表'},
  {id:'exp:doe',name:'DOE 实验设计'},
  {id:'exp:analysis',name:'DOE实验分析'},
  {id:'exp:sum',name:'实验分析与总结'}
]}
```
HL_MAP 追加 `'exp:sum-detail':'exp:sum'`、`'exp:sum-edit':'exp:sum'`

### 页面实现（src/24z2-js-exp-summary.js，新增分片）
- `exp:sum` 列表页：左侧双树（组织结构 4 部门 + 项目树，部门联动收窄项目）+
  5 个搜索框（分两行 toolbar）+ 2 个下拉（项目负责人 / 创建人）+ 重置 + 6 列表格
- 工艺列显示路线名 + 灰色小字「N 组受控差异」
- `exp:sum-detail` 详情子页：`.sum-hero` 标题栏（实验目的 / 实验工艺＋「工艺路线/模板」tag /
  受控差异）+ 对比区（每组：序号 + 本组工艺参数（含受控差异取值）+ 基本信息 +
  原料添加表 + 过程测试表 + 成品检测表 + 总结）
- `openNewSummary / editSummary / saveSummary / syncSummaryItems`：弹窗勾选项目与实验
- **校验**：对比分析需勾选 ≥2 组实验，否则 toast 拒绝保存
- 搜索「实验工艺」改为 `sumCraftHit()`，同时匹配 路线 + diffNote + 各组 craftParams/diff

### 兼容更新
- `src/28-js-boot.js` 启动序列追加 `seedExpSummaries()`（须在 experiments 之后）
- `src/24z1-js-exp-restructure.js` `wzResetConfirm()` 追加 `seedExpSummaries()` 与 `sumListState` 重置
- `src/03-css-mod.css` 追加 `[24z2]` 样式块 + `.sum-craft` / `.sum-diff` / `.sum-route-tag` /
  `.sum-diffnote` / `.sum-diffval` / `.sum-hint`

### 踩坑（本轮新增）
- **列表「实验工艺」列被压成每字一行**：表格默认 `white-space:nowrap` + `·` 分隔符导致字符级断行，
  且「实验总结」列 34% 过宽挤压工艺列。修法：`.sum-craft{white-space:normal}` +
  表头给工艺列 `width:260px;min-width:200px`、总结列降到 26%。修完 206px 宽 / 4 行自然换行。
- 实验种子增加导致 4 处数量断言失效（48→52、部门过滤 23→26），需同步改
  `smoke_exp_restructure.py` / `smoke_exp_tree.py`。

### 自检脚本
- `smoke_exp_summary.py` 新增，**73/73**（菜单 / 数据层 / 受控差异语义 6 条 / 列表 / 7 搜索 /
  双树 / 详情 / 新建编辑删除 / 重置）
- `smoke_exp_restructure.py` 菜单断言改为 4 项（DOE实验分析 + 实验分析与总结）；数量 48→52
- `smoke_exp_tree.py` 数量断言 48→52、23→26

### 全量回归
20 个脚本全 PASS（约 720 条断言，0 JS 错误）：smoke_all / smoke_pages / smoke_doe /
smoke_exp_restructure / smoke_exp_tree / smoke_exp_summary / smoke_proj / smoke_sds_p1 /
smoke_persist / smoke_g5_g6_g10 / smoke_g4_g13 / smoke_mfactor / smoke_adjclass /
smoke_deliver / smoke_export / smoke_note / smoke_sds / smoke_sds_list / smoke_sds_p2 / smoke_sds_sec

### 后续
- 详情页的「原料添加 / 过程检测 / 成品检测」目前为只读展示；如需现场编辑每组明细，
  可再加行内编辑或行级弹窗。
- 可考虑在 DOE 方案详情页加「基于此方案生成对比总结」的快捷入口。

---

## 第十轮：实验分析与总结 · 详情页横排对比 + 整体总结（2026-09-07）

### 用户需求
1. 详情页对比区改成 **横向并列**布局（参考截图：3 个实验左右横排，每组独立成列，纵向字段对齐）
2. 在所有对比数据的最下方增加 **整体总结**：针对全部 N 组实验的结论与建议，贯穿整页

### 设计要点
- **横向 grid 布局**：每行 = 1 字段名 + N 实验值；grid 列模板 `200px repeat(N, minmax(280px,1fr))`
- **列头**：实验编号 + 受控差异标签（蓝色），便于一眼看出"哪一组为什么不同"
- **5 个区块横排**：基本信息 / 本组工艺参数 / 原料 / 过程 / 成品 / 总结（每组）
- **多组数支持**：N>3 时容器 `overflow-x:auto`，每个数据列设 `min-width:280px`，
  通过 `data-cols="N"` 切换 `min-width: 760/1040/1320/...`
- **整体总结**：独立的 `.card` 卡片，贯穿整页底部；标题「整体总结 · 报告 ID」+ 副标「针对本报告 N 组实验的结论与建议」+ 底部署名行（创建人 · 创建时间）
- **保留** 「总结（每组）」：作为对比表的最末一行，每组自己的总结仍按列横排展示

### 数据层（src/21-js-data.js）
- 新增字段 `summaryOverall`（整体总结）
- 6 份种子全部补充具体内容（非简单兜底拼接），例如 0006：
  > 「三种树脂中，进口 WPU-320 手感评分 4.5 最高、涂布流平性最好，是本对比的基准样；
  > 国产 WPU-320 粘度略高但成膜正常，耐干擦 620 次优于标准，可作为替代候选进入中试；
  > 1:1 混拼样手感 4.4 接近进口，但高温下出现轻微橘皮，需将涂布温度控制在 28 ℃ 以内。
  > 综合手感、耐干擦与外观，推荐优先推进国产 WPU-320 单方替代，在中试阶段引入 1:1 混拼作为风险备选。」
- `seedExpSummaries()` 在 map 末尾补兜底拼接，缺省时自动汇总各组 summary

### 页面实现（src/24z2-js-exp-summary.js）
- 删除旧 `sumItemBlock`（竖排卡片渲染）
- 新增 `sumCompareGrid(s)`：组装 5 个区块的横向对比表
- 新增 3 个区块函数：
  - `cmpRowBasic(s)`：6 行基本信息
  - `cmpRowCraft(s)`：受控差异 + 实际参数
  - `cmpRowTable(s,title,getRows,rowFn,thead)`：原料 / 过程 / 成品通用小表
- 详情页 `render` 重写：列头 + 5 区块 + 每组总结 + 整体总结卡片

### 兼容更新
- `summaryFormHtml(s)` 新增「整体总结」textarea 字段
- `saveSummary(id)` 读取并保存 `summaryOverall`

### 自检脚本
- `smoke_exp_summary.py` 详情页断言从「竖排卡片」改为「横排 grid」：
  - `data-cols="3"`、≥7 行、`.cmp-row-multi`×2、`.cmp-row-table`×3
  - 整体总结区块存在 + 内容 >80 字
- 共 **82/82** PASS

### 全量回归
20 个脚本全 PASS（约 730 条断言，0 JS 错误）

### 后续
- 列表页工艺列目前展示路线名 + 差异数小字，可考虑增加一行摘要「整体总结」前 60 字预览（hover tooltip 已够用）。
- 可考虑在新建/编辑弹窗里实时显示「自动拼接的整体总结」，避免用户重复填写。
